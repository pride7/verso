# 更新日志

版本号规则见 [AGENTS.md](AGENTS.md#版本号)：里程碑 → minor +1，里程碑内的修复 → patch +1。

---

## v0.1.0 — M0 地基

第一个能跑的版本。能打开 vault、在文档树上点开笔记、编辑、保存不丢数据。
编辑器目前只是个 `<textarea>`，这是有意的 —— 真正的编辑器是 M1。

### 新增

- **打开 vault**，并记住上次打开的目录，下次启动自动重开
- **文档树**：`X.md` 与同名文件夹 `X/` 在树上合并成**一个**既有内容又能展开的节点，
  这就是 Notion / 思源 的文档嵌套，但底下仍是纯 `.md`（DESIGN.md §2.1）
- **新建文档 / 新建子文档**，建子文档时自动创建同名文件夹
- **编辑与保存**：停止输入 800ms 自动保存，失焦立即保存，`Ctrl+S` 手动保存
- **原子写入**：`写 .tmp → fsync → rename`，断电或崩溃不会留下半个文件（§2.7）
- **frontmatter**：ULID 稳定 `id`、`title`、`created` / `updated` 时间戳（§2.3）
- **打开 vault 时自动 `git init`** 并写入 `.gitignore`（`.folio/` 是派生数据，不进版本库）（§2.8）
- **外部修改检测**：窗口重新获得焦点时比对 mtime，文件被外部程序改过会给出提示，
  可选择加载外部版本或保留本地。有了终端跑 AI 之后这是日常主路径 ——
  没有它，用完 AI 回到编辑器一保存就把它的修改覆盖了（§7.4）
- **面包屑**：文档顶部显示所在层级
- **`VaultFs` 抽象层**：桌面走 `std::fs`。移动端本身是 M6，但文件层现在就不能写死（§1.2）
- **路径越界拒绝**：来自前端的路径一律拒绝 `..` 与绝对路径

### 安全

- 前端传来的路径不可信，全部经 `Vault::resolve` 校验，`../../` 无法读到 vault 以外的文件

### 已知限制（有意留到后续里程碑）

- 编辑器是 `<textarea>`，没有语法高亮、没有 live preview → **M1**
- 没有公式渲染 → **M2**
- 没有搜索、索引、反向链接、database 视图 → **M3**
- 重命名 / 移动 / 删除节点尚未实现（§2.1 的边界情况表列了全部要处理的场景）→ **M1**
- 仅桌面（Windows 已验证），移动端 → **M6**
