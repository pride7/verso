# 更新日志

版本号规则见 [AGENTS.md](AGENTS.md#版本号)：里程碑 → minor +1，里程碑内的修复 → patch +1。

---

## v0.5.24 — database 补上列表、画廊、日历

设计里写了五种视图（`table / board / list / gallery / calendar`），实际只有前两种能用 —— 设置面板里选「列表」，渲染出来还是那张表。这次补齐后三种。

**列表**：一行一篇，标题在左、属性在右。和表格的区别不在样式而在取舍：表格保证每列对齐（好比较），列表放弃对齐换密度和完整的标题（好浏览）。

**画廊**：卡片墙。`cover:` 指哪一列是封面图，写 `attachments/图.png` 或顺手写成 `![[图.png]]` 都认。图找不到时**不显示碎图标**，退回占位块——一面墙上几个碎图标看起来就是软件坏了，而实际只是某张图没同步下来。

**日历**：按某个日期属性摆进月历，**拖到另一天就是改那篇笔记的那个属性**。三件容易被忽略的事：

- 开在**有笔记的那个月**，不是今天。日期全在去年的视图开在今天等于开在一片空白上
- 认不出日期的笔记列在月历下面，不是丢掉不显示
- 按「创建时间」摆时不给拖 —— 那是文件自己的时间，拖了也写不回去，与其拖完弹个错，不如一开始就不给拖

顺带修好一处一直没人碰到的坑：**看板的分组、日历的日期、画廊的封面，这三个键现在会跟着一起取值了**。以前它们不在 `columns:` 里就取不到值，表现是「看板全部掉进未设置」「日历一格都排不出来」，而查询本身还成功着，几乎没法往「取值时被过滤掉了」上想。

另外这三个键**在切换视图类型的地方就会问你**。以前只能手写进代码块 —— 选了「看板」却什么都没变，是这个功能最劝退的一刻。

---

## v0.5.23 — 代码块：语法高亮、行号、一键复制

代码块从「一块灰底等宽字」变成真正能读的代码。

**按语言高亮。** 围栏上标了什么语言就按什么语言上色，`def`、`fn`、`SELECT`
这些关键字终于不再和变量名长得一模一样。语法包**用到才加载** —— 笔记里没有
Rust 代码块，Rust 的那份就一个字节都不读，所以这不会让软件变沉。

配色上克制：只给会改变语义的东西上色（关键字、字符串、数字、注释、名字），
标点和运算符保持正文色。每个 token 都染一遍的方案看着热闹，扫读时反而找不到
重点。深浅两套主题各配了一份。

**行号。** 只在代码块里有，正文仍然没有行号栏 —— 那会让一篇笔记看起来像一个
源文件。**框选整块代码复制出去是干净的代码**，不会把行号带上（行号是 CSS
生成的，本来就进不了剪贴板）。只有一行内容的块不编号：给 `pnpm i` 标个「1」
纯属噪音。

**右上角一个复制按钮。** 点一下整块代码进剪贴板，不含围栏、不含行号，按钮
变成对勾告诉你成功了。它常显但很淡 —— 悬停才出现的话，触摸屏上等于没有。

顺带说明：这**不是**「运行按钮」。笔记可以来自别人，能执行笔记里的代码等于
把「打开一篇笔记」变成「运行一个陌生程序」，那条底线不动（DESIGN.md §7.5）。

---

## v0.5.22 — 新建文档不再先弹窗问名字

按下「新建文档」立刻就有一篇**「未命名」**躺在树里，光标落在它的名字上、
已经全选好——想好了就敲，没想好按 Esc 走人，文档已经在那儿了。

以前是先弹一个窗问名字。那个弹窗最难受的地方不是丑，是它**把顺序搞反了**：
逼着人在还没开始写之前先想好标题，而标题往往是写完才定得下来的。Obsidian
和思源都是先建后改，这次跟上。

同一套输入框也接管了**重命名**（右键菜单、`F2`），一样不弹窗。回车确定，
Esc 放弃，**失焦按确定处理**——改完名点到别处，本意显然是要这个名字，那时候
把刚敲的字丢掉最让人恼火。

几处不显然的：

- **重名往后编号**（`未命名`、`未命名 2`…），由 Rust 在**建之前的一瞬间**
  检查。没让前端去数树里有哪些同名文件——文件被外部程序改动时那个数法会算错
- 「新建」和「改名」撞名时的处理**正好相反**，所以后端分成两个函数：显式给了
  标题却撞名必须报错（改名时静悄悄换个名字等于没听懂），而新建时名字只是个
  占位符，撞了就该自己让开
- **编辑器不再抢正在输入的框。** 这是修出来的：改名框一闪就没了——编辑器
  挂载时 `focus()` 把焦点抢走，而失焦按确定处理。现在挂载前先看当前焦点是不是
  文本输入框。搜索框、属性条同样受益
- 在收起的节点底下新建子文档时那一行根本没渲染，改名框无从谈起——所以被改名的
  节点在谁的子树里，谁就自动展开

### 修好一条一直是红的测试

`cd_works_from_a_canonicalized_cwd` 在 HEAD 上稳定失败。原因是**循环的结束
条件和最后的断言各用了一把尺子**：结束条件数「标记出现了两次」就算跑完，
可 PSReadLine 会为了上色和补全把已经打进去的那行反复重画，两次很容易全是
回显——循环就此跳出，真正的输出还没来，断言看到一片回显，报「命令没跑起来」。

有意思的是同一个测试的下半部分早就写明了「PSReadLine 会反复重画」，只是上半
部分没照着办。现在两处共用一个 `reported_cwd()`。

132 个 Rust 测试 + 309 个 Node 测试 + 198 个浏览器测试通过。

## v0.5.21 — 标签可以钉住了，标签栏也不再挤成一排

### 固定

**双击标签**（或右键 → 固定）把它钉住。固定的标签有三个待遇：

- **排在最前**，跟着一条竖线和其余标签分开
- **「关闭其他」时留着** —— 一份天天要看的索引被一次右键清掉，比多留几个
  标签难受得多
- **不会被「替换当前」换走**。钉住的意思就是「它一直在这儿」，能被悄悄换掉
  的话等于钉了个寂寞

× 照样在，需要关的时候不用先取消固定。把没固定的标签**拖进固定区**也是固定，
拖出去就是取消 —— 固定在数据里就是「前 N 个」，这是唯一自洽的解释。

固定状态跟标签一起存进 `.verso/workspace.json`，下次启动还在。

### 标签栏

作者报的三件事，一次改完：

- **每个标签等宽**（168px），不再跟着标题长短伸缩。一排宽窄不一的标签，眼睛
  每次都要重新找名字和 × 的位置；等宽之后这两样永远在同一个横向位置上。
- **× 常驻**，不再只在悬停时冒出来，图标也大了一号、给了热区和悬停底色
  （移上去变红）。等宽之后右边本来就留着这块地方，藏起来只换来一个
  「要先猜它在哪」的按钮。
- **不再挤成一排**：标签之间留 2px 间距、上方留 5px，去掉原来那条 1px 硬
  分隔线，四角改成上圆角。留白比线更能把东西分开（§6.3）。标签栏底下那条
  横线也去掉了 —— 当前页的背景就是正文的背景，连成一片才说得通「这个标签
  就是下面这块内容」。

### 右键菜单

开在鼠标底下（原来永远钉在最左边），靠近窗口右边缘时往回收。

它一度被拉成一个横跨半个窗口的白盒子：`.side-menu` 的 `right: 0` 排在
`.tab-menu` 的 `right: auto` 后面，同特异性时后者输了，于是内联的 `left` 和
`right: 0` 一起生效、把它撑满。现在用 `.side-menu.tab-menu` 压过去，并且在
browser 测试里钉住了菜单的宽度和落点。

样子也收了收：字号 12.5、行距压紧，「切换状态」和「关掉东西」之间隔一条线
（这两件事的代价差得远，手快时很容易点错），「固定的留着」从按钮文字里挪出来
变成右侧压暗的注脚 —— 写在里面会让整个菜单跟着最长的那一条撑宽。

命令面板里多了「固定／取消固定标签」，可以在设置里给它绑快捷键。

## v0.5.20 — 终端里 `cd` 终于能用了，颜色也看得清了

### `cd` 全线报错，是路径写法的锅

作者报「怎么 cd 都不支持」。原因不在 shell，在我们递给它的工作目录：

vault 根是 `canonicalize()` 出来的（越界检查依赖它），而 Windows 上那会得到
**扩展长度写法** `\\?\D:\Notes`。这个写法对 Win32 API 完全合法，我们自己读写
文件一点问题没有 —— 但 PowerShell 拿它当工作目录之后，会切到 provider 限定名
那个状态：

```text
PS Microsoft.PowerShell.Core\FileSystem::\\?\D:\Notes> cd ..
Set-Location: Cannot process argument because the value of argument "path" is not valid.
```

`cd ..`、`cd 子目录`、`cd ~` 全部报这一句。提示符还长得占掉大半行 ——
「很难看」和「cd 不支持」其实是同一件事的两个症状。

现在**跨出进程边界之前**把 `\\?\` 摘掉（起内嵌终端、调系统终端两条路都摘），
内部照旧用规范化路径。只摘盘符和 UNC 两种确定安全的写法，`\\?\Volume{GUID}\…`
这类设备路径原样留着 —— 摘了它就不指向同一个东西了。

### 字看不清：调色板一直是照黑底配的

终端只设了背景色和前景色，**其余 16 色用的是 xterm 的默认值**，而那套值是按
黑底配的：`yellow` 是 `#cdcd00`、`brightWhite` 是纯白，画在我们 96% 明度的
浅色底上基本等于隐形。AI CLI 的输出里到处是这些颜色。

现在深浅各配了一套完整的 ANSI 调色板。浅色那套里 `white`/`brightWhite` 是
**反过来**的（灰 → 近黑）：ANSI 的"白"意思是"前景里最亮的那档"，在白底上
照字面给白色等于让它消失，Solarized Light 和 GitHub Light 都这么处理。

调色板只管得了 16 色，而 AI CLI 大量用 256 色和真彩色。所以还开了
`minimumContrastRatio: 4.5`（WCAG AA 对正文的要求）：对比度不够的前景色由
xterm 自己提亮或压暗。

顺带三处：

- **光标底下那个字**以前是同色叠同色 —— 实心光标一压，正在输入的字符就没了。
  现在给了 `cursorAccent`
- 行高 1.2 → 1.35。终端里读的是小号等宽字，行距比正文更要紧
- 默认字号 12.5 → 13.5（已经改过这一项的人不受影响，设置 → 终端里随时能调）

## v0.5.19 — 多标签页

正文上方多了一条标签栏。**默认每点一个文件就多开一个标签**；想要「点哪篇换哪篇」
的话，设置 → 编辑器 → 「点侧栏文件时」改成「替换当前」。

无论选哪种，**Ctrl/⌘+点 和中键总是开新标签** —— 那是一个明确的表态，不该被
设置盖掉。已经开着的那一篇总是切过去，不会开出两个指着同一份内容的标签。

新标签插在**当前页右边**而不是队尾：从一篇笔记点进它的链接时，新页紧挨着来源，
读完关掉就回到原处。

### 切回来的时候，光标和撤销历史都还在

每一页离开时会把 CodeMirror 的 `EditorState` 存下来 —— 光标、选区、**撤销历史**
一起。切回来直接接着用，滚动位置也回到原处。

笔记在别处被改过（内容对不上）时会丢掉那份历史重新建：把旧的撤销栈接到一份
不一样的内容上，按一次撤销会把文件改成从来没存在过的样子。

### 其他

- **关掉当前页时接班的是右边那个**，右边没有了才往左找。总是往左的话，
  「从左往右挨个关」会变成每关一次就跳回开头
- 标签可以拖着重排。拖动是在整理标签栏，不换页 —— 当前页跟着它的**路径**走
- 重命名和移动会连**整棵子树**的标签一起改；删除会把它和子树的标签一起去掉
- 右键标签：关闭 / 关闭其他。`Ctrl/⌘+W` 关当前，`Ctrl/⌘+Alt+←/→` 前后切
- 重名的标签会补一段父目录（`数学/索引`、`论文/索引`）—— 一堆同名的「索引」
  排在一起，标签栏就等于没有

### 状态存在 `.verso/workspace.json`

和手动排序的 `.verso-order.json` 分在两边，判断标准是**丢了要不要紧**：

- 手动排序丢了**重建不出来**（那是一个一个拖出来的意图），所以它在 vault 根、
  进版本库、跟着同步走
- 标签丢了只是「下次启动少开几个页签」，重建方式就是再点一次 —— 这正是铁律
  第 6 条说的那种「`.verso/` 整个删掉也能重建」的东西

而且它天然是 per-machine 的：两台机器开着同一个 vault，各自开了什么页签本来
就该各算各的，`.verso/` 在 `.gitignore` 里这一点自动成立。

## v0.5.18 — 文档树和标签有快捷键了，而且每一条命令的键位都能自己改

左边那一竖条上，文档树和标签一直没有快捷键——只有搜索和大纲有。现在补上：

| | |
|---|---|
| `Ctrl/⌘+Shift+E` | 文档树 |
| `Ctrl/⌘+Shift+T` | 标签 |
| `Ctrl/⌘+B` | 展开／收起侧栏 |
| `Ctrl/⌘+N` | 新建文档 |
| `Ctrl/⌘+Shift+P` | 命令面板（原来只是没写在命令表里） |
| `F2` | 重命名当前文档 |

### 设置里多了「快捷键」一页

**每一条命令都能改键位，包括默认没绑的那些**（浮动大纲、折叠全部标题、重建
索引……）。点右边的按钮，然后**直接按下**想要的组合键——不是让你填
`Mod+Shift+O` 这种字符串，那要求你先知道这个软件管 Ctrl 叫 Mod、管方向键叫
ArrowUp，而你没有任何办法知道。Backspace 解绑，Esc 取消。

撞键**不拦**，只把撞在一起的两条标红：换键位天然是「先占上、待会儿把原来那个
挪走」的来回过程，拦下来这个过程根本走不完。不带 Ctrl/Alt 的普通键不收
（F1–F12 除外）——那种键绑上去会把正常打字吃掉。

改过的键位记在 `settings.json` 里，**只存与默认不同的那几条**。所以将来某条
命令的默认键位调整了，没动过它的人会跟着一起变，而不是被自己的配置钉死在
旧值上。

### 顺带修好的

- **终端里的键盘还给 shell 了。** `Ctrl+N/P/E/B` 在 readline、vim、tmux 里各有
  各的意思，之前 `Ctrl+P` 被界面抢去开快速跳转。现在焦点在终端里时只留两条
  出路：`Ctrl/⌘+``（关终端）和命令面板，其余全部下发给 shell。
- **中文输入法开着时快捷键不再乱触发。** 键位现在认物理按键位置，不认字符——
  输入法激活时浏览器报的按键是 `Process`，按老写法会被当成随机的一个键。
- 命令面板、图标栏 tooltip、空白页那几行提示，显示的都是你**当前**的键位。
  改过之后它们跟着变，不会再出现「面板上写着 Ctrl+X，按下去没反应」。

## v0.5.17 — 侧栏头部松快了，宽度也能拖了

头部原来挤着四样东西：视图名、vault 名、排序下拉框、新建按钮。那个原生
`<select>` 一个人就吃掉小一半宽度，是挤成一团的主因。

现在头部只剩**视图名 + 两个 24px 图标按钮**（排序、新建）。排序改成图标加菜单，
当前档位打勾；勾位始终占着，不然选中那一项的文字会比别的往右挪一格。菜单和
右键菜单共用一套材质——界面上只该有一种「浮层」的样子。

**vault 名挪到侧栏底部**，带库图标，上面一条 hairline 隔开。换库是低频操作，
不该占着头部的黄金位置。

### 侧栏可以拖宽了

右边缘 7px 的热区骑在那条分隔线上——窄了拖不中，宽了会盖住树最右边悬停才
出现的「新建子文档」按钮。双击复位。宽度记在 localStorage，跨会话保留。

上限按**窗口的一半**算而不是写死一个像素值：小窗口里 480px 的侧栏会把正文
挤没。窗口变窄时会重新夹一次。下限 180px——再窄文件名一个字都看不见，而且
那根拖杆自己也变得难再抓住。

顺带：滚动改成只有中间那段滚（不然底部 vault 那条钉不住）；拖动时给 `<body>`
加 `is-resizing` 禁选，否则鼠标划过正文会把文字一路选中。

`sidebar.browser.test.tsx` 6 条。拖拽调宽读的是 `clientX` 差值再写进 grid
轨道，没有布局引擎时轨道宽度恒为 0，怎么拖都「通过」——只能在真浏览器里验。

## v0.5.16 — 整个界面不会再跟着手指晃

作者报的症状是「上下左右都能滑，还有回弹」：触控板两指一划，**整个界面**
连图标栏带状态栏一起动，边上露出一块白。

这不是滚动。是**弹性 overscroll（橡皮筋）**——它发生在合成器层，直接把整页
平移一下再弹回来，`overflow` 和 `scrollTop` 都感知不到。所以我按「正文滚动」
查了一整轮，量到的布局全是好的，因为布局本来就没错。

`overscroll-behavior: none`（html/body）关掉它；`.main` 上加 `contain`，
编辑区滚到头之后不再把外层带着一起动。桌面应用的外壳不该有这种网页味的手感。

顺带把外壳收紧了三处，都是「本来就该有、只是一直没露馅」的：

- **`html/body { overflow: hidden }`** —— 页面本身永远不滚
- **`.app { position: relative }`** —— `overflow:hidden` 只裁剪**以自己为
  包含块**的后代。少了它，任何 `position:absolute` 的后代（浮动大纲、折叠
  箭头、图片把手）只要祖先都是 static 就会以视口定位、从这个盒子里逃出去，
  把整个文档撑大
- **`.app` 的 `100vh` 改成 `100%`** —— 两者在缩放、横向滚动条出现时会差出
  几十像素，而差出来的那截正好把界面顶出视口

还加了 `shell.ts`：`overflow:hidden` 只挡用户滚，**挡不住程序滚**。
`scrollIntoView` 和焦点移动照样能把文档顶上去，而且顶上去不会自己回来 ——
CodeMirror 的光标滚动和界面里那些 `.focus()` 都会触发。所以文档一旦被滚走就
立刻拉回去。

### 这次的教训：browser 测试也够不着合成器

`pageScroll.browser.test.tsx` 里那几条钉住的是 `overflow` / `overscroll-behavior`
这些声明本身，不是行为 —— 橡皮筋在 headless 里根本不发生。和上一版
`dragDropEnabled` 是同一类：**测试能覆盖的边界，比想象中窄一层。**

另外记一笔：早先那轮诊断把 App 挂在 `position:fixed;inset:0` 的容器里，等于
给它罩了一层绝对撑不开 body 的壳，页面级的问题会被完整屏蔽。新测试挂进
`#root`、走正常文档流，和 `index.html` 一样。

## v0.5.15 — 拖拽在真 app 里一直是死的

一行配置：`tauri.conf.json` 里加 `"dragDropEnabled": false`。

Tauri 默认在**操作系统层**接管拖放，webview 里的 `dragstart` / `drop` 根本
收不到。tauri-utils 的 `config.rs` 写得很直白：

> Disabling it is required to use HTML5 drag and drop on the frontend on Windows.

文档树的拖拽移动和拖拽排序全靠 HTML5 拖放，所以这两个功能在应用里从来就
没生效过 —— **而 5 条浏览器测试从头到尾全绿**。Playwright 起的是干净的
Chromium，Tauri 加在中间的那一层不在里面。前端代码从头到尾都是对的。

代价：从资源管理器往窗口里拖文件不再走 Tauri 的 `drag-drop` 事件。真要做
那个功能，用 HTML5 的 `drop` + `dataTransfer.files` 自己接。

没法用组件测试盖住这种东西 —— 它是运行时行为。退而求其次，`tauriConfig.test.ts`
把这行配置钉住，并在旁边写清楚删掉它会坏什么。AGENTS.md 里也补了一节：
**功能依赖浏览器和宿主之间的边界（拖放、剪贴板、文件、协议、窗口）时，
先去确认 Tauri 的默认值**，browser 测试够不着那一层。

### 插图：粘贴就进来，拖边缘改大小

`Ctrl/⌘+V` 粘贴图片：落盘到 `attachments/`，在**光标处**插入
`![[attachments/xxx.png]]`。截图工具、浏览器里的「复制图片」、文件管理器里
复制的图片文件都走这一条。剪贴板里的图常常没有文件名，会按时间起一个 ——
满 vault 的 `image.png` 谁也认不出哪张是哪张。**重名不覆盖**，加时间戳后缀。

`![[图.png]]` 现在渲染成真图（以前只是一行蓝字），光标移进去照旧退回源码。
**鼠标移到图上，右边缘会出现一根把手，拖它改宽度** —— 改的是源码里的
`![[图.png|300]]`，也就是 Obsidian 的写法，所以宽度跟着 `.md` 走，拖进
Obsidian 也认。只调宽度，高度让浏览器按比例算。

找不到的图不显示碎图标，而是一行「图片找不到：<文件名>」—— 得知道是哪个文件没了。

显示走 Tauri 的 asset 协议，作用域**只放行当前打开的这个 vault**：笔记可以来自
分享，`![[../../秘密.png]]` 不该能读到 vault 外面去。


### database 视图能操作了：点表头排序、底下加一行

以前这个表只能看 —— 想换个排序、想加一条，只能去手改代码块里的 YAML。

**点表头排序**：升 → 降 → 恢复默认。改的是 ` ```verso-view ` 代码块里的
`sort:` 那一行，也就是说**排序跟着 `.md` 走**，换台机器、换个编辑器打开还在
（§0 第 1 条：视图配置是你的内容，不是我们的界面状态）。只动那一行，你自己
排的键序和注释一个字节都不碰。

**底下多了「+ 新建」**：建一篇笔记并加进这个视图。建在 `from:` 指的那个范围
里 —— 建完不在表里，等于什么都没发生。

样子也照着 Notion 收拾了一遍：左上角标出当前是哪种视图，右上角是「新建」，
表格改成**靠线分行**而不是套一个描边卡片（§6.3：层次靠留白和细线），每行前面
一个文档图标，标题用正文色加粗、悬停才出下划线 —— 一列蓝字会把表格看成一张
链接清单。最后一行是「+ 添加条目」。

列头带**属性类型图标**（数字 `#`、日期钟面、布尔勾、列表标签、文本），类型来自
索引里的 `props.type` —— 索引本来就存着，只是以前没带回前端。

列头的 `⋮` 可以升序/降序/**隐藏这一列**；表头最右边的 `+` 加一列：从这批笔记
已有的属性里挑，或者**起一个新名字**（新属性不会立刻改任何笔记 —— 它是在你往
某个格子里填值那一刻才写进那篇的 frontmatter 的，§2.6 说的「数据来源就是
frontmatter」，没有另一份 schema）。

**隐藏列只改 `columns:`，绝不动任何笔记的属性。** Notion 里删列是删属性、会改
所有条目；一次误点抹掉整个 vault 的某个字段，这种事不能藏在一个下拉菜单里。
真要删属性去那篇笔记的属性条上删。

右上角的齿轮是**视图设置**：视图类型（表格/看板/列表）、来源、筛选（属性 +
运算符 + 值，可加可删）、上限。全都写回代码块。**手写的筛选不接管** —— 用了
`or`、括号这些界面表达不了的写法时，面板只把原文摆出来让你去代码块里改，
在界面上把它规范化成一个语义不同的东西比不做还糟。


### 加列时可以选类型了，列也能重命名

「加一列」现在和 Notion / 思源一样：**先挑类型**（文本、数字、单选、多选、
日期、复选框、网址），给一个默认名 `属性`，当场就能改。

**类型存在 vault 根的 `.verso-props.json`**，和手动排序的 `.verso-order.json`
一个路子：进 git、换台机器跟着走、删掉也不丢任何笔记数据（只是退回按值推断
类型）。不放 `.verso/` 是因为那里按铁律只能放派生数据，而「这一列是单选」
是你的决定，推断不出来。

**列头 `⋮` 里能重命名和改类型。** 重命名是真的重命名 —— 改所有带这个键的
笔记，因为属性是 vault 级的概念，只改「这个视图收到的那些」会留下两个同义的
键。所以改之前会先数一遍再问一句「这会修改 N 篇笔记的 frontmatter」；点取消
就什么都不做。没做成「只改显示名」：那等于在界面和文件之间开一道缝，而这个
软件从源码模式到排序写回，一路都是「你看到的就是文件里的东西」。

关联关系、函数、汇总这些**不做** —— 要一套表达式引擎和跨笔记引用，超出纯
`.md` 文件能承载的范围。


### 单元格按类型给控件了

复选框点一下就改（不用进编辑态再回车），单选给下拉、多选给勾选面板、日期给
日期选择器、数字给数字框、网址显示成能点的链接（右边一个小铅笔负责改 ——
一个东西既是链接又是编辑入口，两件事都做不利索）。单选和多选的值显示成标签，
一眼看出这是「从几个里挑的」而不是随手写的文本。

**类型也决定写进 YAML 的值形态**：声明成多选的列，第一次填值就写成数组，
不用等它碰巧已经是数组；声明成文本的列填 `3` 不会悄悄变成数字（那会让
`where 编号 = "3"` 莫名其妙不匹配）。没声明类型的还是按值猜，和以前一样。

列头 `⋮` 里多了「选项…」，管单选/多选的候选值。**删掉一个选项不会去改任何
笔记** —— 已经写着这个值的照样留着、照样显示。选项表只是「下拉里列出哪些」，
不是一份会反过来清洗数据的约束。下拉里也永远保留当前值：手改过 frontmatter
的值，不该被「打开下拉」这个动作冲掉。

---

## v0.5.14 — 直接拖就能排，不用先去选「手动排序」

上一版有个多余的步骤：默认是「名称 A→Z」，而这个模式下拖动**毫无反应** ——
得先自己找到侧栏那个下拉框、选中「手动排序」，才拖得动。

现在任何模式下拖动都直接生效，并顺手把排序方式切成手动。拖动本身就是
「我要自己定顺序」的表态，不该再要求先声明一次。切过去时会提示一句，
不是静悄悄改掉一个下拉框的值。

重排的**起点是你屏幕上当前看到的顺序**，不是顺序文件里记着的。所以从按名称
排切过来时，除了被你拖动的那一个，别的文件不会跟着跳。

**跨目录拖到某一行的边缘，现在也能用了**：先把文件移过去，再排到那个位置。
以前这种情况下行已经高亮着「插到这里」，松手却什么都不发生 —— 用户不知道
自己做错了什么，是最难受的一种失败。

112 个 Rust 测试 + 210 个 Node 测试 + 112 个浏览器测试通过。

---

## v0.5.13 — 文档树可以自己排，也可以按规则排

侧栏标题右边多了个排序选择器，五档：

| 手动排序 | 拖着调，顺序记下来 |
|---|---|
| 名称 A→Z / Z→A | 自然序 —— `第2章` 排在 `第10章` 前面，不是字典序那样倒过来 |
| 最近创建 / 最近修改 | 新的在前。没写时间戳的笔记回落到文件系统 mtime，不会整档失灵 |

除「手动」外都只影响显示，不写任何文件。

**拖拽分成两件事**：拖到一行的上下四分之一是调顺序（插到它前面 / 后面），
拖到中间一半才是移进去当子文档。不分区的话，想调个顺序永远会变成改层级 ——
这两件事看着像，结果差得很远。落点有三种不同的高亮，松手前就知道会发生什么。

### 手动顺序存在 vault 根目录的 `.verso-order.json`

```json
{
  "": ["数学.md", "论文.md"],
  "数学": ["数学/线性代数.md", "数学/泛函分析.md"]
}
```

它进版本库，所以跟着 vault 一起同步；笔记本身一个字不动；拖一次只改一个文件。

另外两个位置都试过，都不行：

- **`.verso/`** —— 那个目录在 `.gitignore` 里，顺序换台机器就没了。M5 是 git
  同步、M6 是移动端，这基本是判死刑
- **每篇笔记的 frontmatter** —— 除了给每篇笔记塞一个用户没写的 `order`，
  更要命的是**写 frontmatter 会刷新 `updated`**：拖一次就把一整组笔记的修改
  时间改成现在，「最近修改」这一档当场失效

代价是这个文件**可能变陈旧** —— 在别的软件里重命名了笔记，那一条就对不上。
处理方式是认不出的路径当作「没排过」，沉到底部按名字排，而不是报错。Verso
自己的重命名 / 移动 / 删除会同步修好它，包括整棵子树：重命名 `数学.md` 时，
`数学/` 底下所有孙节点的条目都跟着改，否则整棵子树的顺序会一次性全丢。

109 个 Rust 测试 + 207 个 Node 测试 + 108 个浏览器测试通过。新增的浏览器测试
专门盯拖放分区 —— 那个比例算的是 `getBoundingClientRect().height`，在
happy-dom 里恒为 0，比值成了 `NaN`，于是每次拖放都被判成「移进去」而测试全绿。

---

## v0.5.12 — 光标不在时，代码块的 ``` 藏起来

其他标记（`#`、`**`、`>`、`[!note]`）光标移开都会藏，围栏留着就不一致。
现在代码块只显示代码本身，光标一进去立刻露出 ` ```rust `，语言标注照样能改。

顺带修了三个只在「隐藏整行」时才会出现的问题：

- **围栏还没闭合时不藏。** 正在敲新代码块时，解析器认为这个块一直延伸到
  文末，「最后一行」其实是最后一行代码 —— 按老逻辑会把代码本身吃掉
- 围栏的反引号一度被两处代码同时处理，结果是 ` ``` ` 没了但语言标注还留着
- 圆角一度跟着被藏的那一行消失

### Verso 不再往你的文件里写任何东西

新建的笔记从此是一个**空文件**。保存不会补 `id` / `title` / `created`；
`updated` 只在这个键**已经存在**时才刷新 —— 想要时间戳就自己写一行，
不想要就删掉，删了不再长回来。源码模式里把 frontmatter 整块删干净，文件里
连一道 `---` 都不会剩。

以前每次保存都会补齐那四个字段，于是一篇只写了两行字的笔记，头上永远顶着
五行 YAML。改之前把账算了一遍：这四个字段里真正被代码用到的只有排序
（`created` / `updated`），而 `id` 连链接解析都没走它 —— §2.3 承诺的
「重命名后按历史 id 修链」至今没实现，`[[链接]]` 一直是按名字和路径解析的。
**「文档保持简洁、原始」比一个尚未兑现的保险更值钱。**

代价说清楚：以后真要做「重命名不断链」，只有自己写了 `id` 的笔记能享受到。
索引内部仍然需要一个主键，没有 `id` 的笔记用 `path:相对路径` 顶上，搜索、
反向链接、标签、database 视图都照常。

**已有的笔记不动。** 这次改的只是「以后」—— 老笔记里已经写进去的 `id` 和
时间戳原样保留，`updated` 也会继续跟着保存刷新。想清理哪篇就在源码模式里
自己删。

100 个 Rust 测试 + 207 个 Node 测试 + 102 个浏览器测试通过。
---

## v0.5.11 — frontmatter 那一块能整块选中了

上一版把两道 `---` 放在输入框外面，理由是"边界不是内容"。实际用起来才发现
这挡的是**整块操作**：想把整段 frontmatter 选中删掉，`Ctrl+A` 只圈得到中间的
YAML，两道围栏永远留在那里。

现在围栏也在框里，源码模式下那一块和文件里长得完全一样，选中、复制、整块
删掉都照常。删光的效果是「清掉全部自定义属性」—— `id`、`title`、`created`
会补回来，没有 id 的笔记没有稳定标识，重命名之后指向它的链接会断。

围栏被删掉一道甚至两道也不会出事：中间那段照样交给 YAML 解析器，写错了仍然
是报错不写盘。

---

## v0.5.10 — 标题折叠；属性和表格都能改了

### 标题折叠

长文档里这是刚需。标题行左边悬停出现箭头，点它收起整节；`Ctrl+Shift+[` / `]`
也行，命令面板里有「折叠／展开当前小节」「折叠全部」「展开全部」。

**按层级收**：折叠 `##` 会把它下面的 `###`、`####` 一起收走，碰到下一个 `##`
或 `#` 才停。只找「下一个标题」的话，折叠 `##` 只能收到第一个子标题为止，
那基本没用。

代码块里写的 `# 示例` 不会被当成标题——那里冒出折叠箭头、折叠范围横跨代码块
边界都是很烦的。

箭头**平时不显示**，鼠标移到那一行才浮现；已经折叠的那个始终可见，因为它是
状态指示，藏起来会让人找不到自己收走的内容。源码模式下折叠照常可用——那时
正是在长文里找东西的时候。

### 属性可以改了

属性条展开后：点值改值，**点名字改名**，行尾悬停出现删除按钮，底部一行
「+ 添加属性」。回车保存，Esc 取消。保存失败会留在编辑态并显示原因，
不会把你改的内容丢掉。

改名是真正的重命名，不是「删旧键 + 建新键」——后者会把 `tags: [甲, 乙]`
压成字符串，还会把这个键挪到 frontmatter 末尾，在 git diff 里显示成一大片
改动。现在保留原值、原类型、原位置。

`id` 和 `created` 不显示也改不了：改掉 id 等于换掉这篇笔记的身份，所有指向
它的链接都会断。

### 源码模式里的 frontmatter 可以直接改了

v0.5.9 只是把 frontmatter 显示成源码，不能动。现在那一段是可编辑的：点进去
就能改，**停手 800ms 自动存**（和正文一个节奏），失焦或 `Ctrl/⌘+S` 立刻存。

两道 `---` 有意留在输入框外面 —— 它们是 frontmatter 的边界不是内容，能删掉
边界只会换来一句「我的属性怎么全没了」。

**YAML 写错了不会吃掉东西。** 解析不过就在下面挂一条红字，文件一个字节都不写，
你敲的内容也原样留着 —— 这时候把人刚写的东西回滚掉是最糟的处理。另外 `id` 和
`created` 被删掉时会从旧值补回来：换个 id 等于换了这篇笔记的身份，指向它的
链接会全断。

正文和 frontmatter 是**两半各写各的**，两条写入路径都从磁盘读另一半，谁也盖不掉
谁；改属性前会先把正文冲一次盘，避免两个「读—改—写」同时在飞。

### 修复

- **表格渲染之后点不进去，等于写完就锁死。** 上个版本的表格注册了
  `atomicRanges` 又让 widget 吞掉事件，光标既进不去也点不进去。现在点一下
  回源码，方向键也能走进去
- 折叠箭头曾经完全不显示，然后又不居中——两次都是几何问题：绝对定位的元素
  伸到了内容盒外面被裁掉；`transform: rotate` 加在容器上会把包围盒撑大，
  向左多探出近十个像素

99 个 Rust 测试 + 182 个 Node 测试 + 95 个浏览器测试通过。

---

## v0.5.9 — 源码模式补两处

**frontmatter 也退回源码了。** 之前源码模式下正文全是源码，最上面的属性条却
还渲染着 —— 页面上唯一一块自相矛盾的东西。现在显示的是文件里两道 `---` 之间的
**原文**：键序、缩进、注释都和文件里一模一样（属性条用的是解析后的映射，
那三样都留不住）。

这一块目前是**只读**的，可以选中复制。要改属性就切回预览模式，那边的属性条
可以直接点。让源码模式里的 YAML 也能手改，需要一条「整篇原样写回」的写入
路径，会牵动保存、大纲行号、字数统计几处口径 —— 单独做一次更稳妥。

**左下角多了一个源码模式按钮**（图标栏动作组的第一个，`</>`）。Obsidian 和
Typora 都给了这个开关一个常驻按钮：只藏在 `Ctrl/⌘+E` 和命令面板里的话，
不知道它存在的人永远不会用到。开着的时候按钮和状态栏那枚小标一起亮。

---

## v0.5.8 — 源码模式

**`Ctrl/⌘+E` 在预览和源码之间切**（命令面板里也有）。源码模式下 `##`、`**`、
`$`、`- [ ]` 这些标记不再被藏起来，公式、表格、callout、database 视图都退回
源码原文 —— 想改一段表格、抠一个链接、或者看看 AI 到底往文件里写了什么，
不必再把光标一格一格挪进去逼它显出源码。

**高亮还在。** 源码模式要看的是 Markdown 源码，不是纯文本：标题该大还是大，
代码该等宽还是等宽，只是标记露出来了。

**切模式不丢任何东西** —— 光标、选区、撤销历史、滚动位置都留在原处。它走的是
CodeMirror 的 compartment 而不是重建编辑器；一个切一次就把这些丢光的开关，
没人会切第二次。

开着的时候状态栏左下角有一枚「源码模式」小标，点它就退出 —— 光看正文分不清
「现在是源码模式」和「这篇笔记本来就没排版」。这个状态跨会话保留。

---

## v0.5.7 — 大纲

**侧栏多了第四个视图：大纲**（图标栏最下面那个，`Ctrl/⌘+Shift+O`）。列出当前
这篇的全部标题，按层级缩进，点一下跳过去；正文滚到哪一节，哪一条就亮起来。
标题是**边打边更新**的 —— 不等自动保存。

**正文右侧多了一条浮动目录。** 收起时只是一排右对齐的短横线，长度按标题层级
递减，不看也知道这篇文章有几节、结构多深；鼠标移上去才展开成标题文字。它不
占版面，所以侧栏可以继续留给文档树。少于两条标题时不显示 —— 那不叫目录，
只是一块挡视线的东西。窗口窄于 900px 时也不显示，那时候正文两侧本来就没有余量。

不想要它的话，命令面板里有「隐藏浮动大纲」，关掉的状态跨会话保留。

两个细节：

- `#标签`（§2.4）不会被当成标题 —— ATX 标题要求 `#` 后面有空格，满篇标签
  才不会变成满篇一级标题
- 缩进按**文档里实际出现的最浅那一级**归零。很多人把一级标题留给笔记标题
  本身、正文从 `##` 开始，照 `#` 个数缩进的话整个大纲会平白往右缩一格

---

## v0.5.6 — 界面与正文渲染大修

### 界面

**图标全部换成自绘的 SVG。** 原来用的是 Unicode 字符（▤ ⌕ # ▣ ⌘ ⚙），
它们来自不同的字符区块、由不同字体渲染，粗细和大小对不齐 —— 这是界面
"看起来没做完"最主要的来源。现在统一 16px 网格、1.5 描边。

**层次改成靠背景分层，不靠边框。** 侧栏和正文区原来只差 2% 亮度，全靠
1px 硬边框撑着。现在分成四层（正文 / 侧栏 / 浮层 / 分隔线），硬边框基本
去掉了 —— 这是界面观感变化最大的一项。

**选中态改成实心圆角条。** 半透明色块会让底下的背景透上来，深色主题的
浮层上几乎看不见。文档树、搜索、标签、命令面板、符号面板现在一致。

圆角成体系了：四档令牌，**盒子越大圆角越大**。callout、代码块这类大块
用 14px，列表行用 6px —— 同一个半径放在 24px 的按钮和 600px 的面板上，
观感完全不同。

其他：细滚动条（不再是 Windows 那条带箭头的）、键盘焦点环、正文顶部留白。

### 正文渲染

- **表格**终于会渲染了。表头浅底、只画横线不画竖线（竖线会让它变成网格纸）、
  支持 `:---:` 三种对齐。单元格里的行内标记（代码、粗体、公式、链接）也渲染
- **callout**（`> [!note] 标题`）渲染成带图标、左侧色条和淡底色的块。支持
  Obsidian 那套类型别名，但颜色只归六个色族。不认识的类型退回「说明」而不是
  当成普通引用
- **任务列表**的 `- [ ]` 变成能点的复选框，勾掉的整行加删除线
- **代码块**有了圆角底色，围栏行淡化但不隐藏（藏了就改不了语言标注）
- 普通引用有了灰色竖线，和 callout 区分得开
- 无序列表的 `-` 显示成圆点

### 输入

**打完 ` ``` ` 自动补上收尾围栏**，光标停在开围栏之后 —— 下一步通常是敲
语言名。正在收尾一个已打开的块时不会重复补。

**`/` 命令菜单的样式修好了。** 之前它一直用 CodeMirror 的默认外观：正文
字号、Windows 原生滚动条、底部还露半行。根因是这些样式写在 `EditorView.theme`
里，而补全面板渲染在编辑器 DOM 之外，那里够不到。

### 修掉的三个 bug

都不是这次新写的代码引入的：

- **live preview 不在解析推进时重建。** 构造时语法树往往还是空的，要等
  用户碰一下才补上。平时看不出来，只有"打开笔记后什么都不做"才露源码
- **`getChild` 找不到嵌套节点**，导致所有 callout 退化成普通引用
- **负外边距把块撑出正文栏**，产生横向滚动，而左侧色条恰好画在被推出
  可视区的那一段上 —— 表现是"竖线怎么调都看不见"

### 内部

加了**视觉工作台**：Playwright 起独立的 headless Chromium，用假数据渲染真实
界面并截图，改 UI 不用再靠想象。包含一个 2 倍放大的场景 —— 圆角、色条这种
几个像素的东西，在整屏截图里根本分辨不出来。

152 个 Node 测试 + 51 个浏览器测试通过。

---

## v0.5.5 — 任务栏图标更清晰

放大图标主体，减少透明留白，使 Verso 在 Windows 任务栏及其他小尺寸场景中
与相邻应用的视觉大小更协调。

同时修复开发构建不会跟随 `icon.ico` 更新 Windows 可执行文件资源的问题；
以后调整图标后重新构建即可生效，不会继续显示旧图标。

---

## v0.5.4 — Verso 正式图标

Verso 不再使用 Tauri 的默认图标。新图标用一块翻起的纸页表现
*verso*（一叶纸的背面），用与界面强调色一致的冷蓝作为唯一主色。

Windows、macOS、Linux、iOS 和 Android 所需的全部尺寸都由同一张
透明母版生成，任务栏、Dock 和安装包里的品牌标识现在保持一致。

---

## v0.5.3 — 清掉改名留下的兼容代码

改名已经完成，`folio` 在代码里不再有任何痕迹。

- **只认 ` ```verso-view `**，旧的 ` ```folio-view ` 不再识别
- 去掉了「旧 vault 的 `.gitignore` 自动补 `.verso/`」那段迁移
- 目录也已经是 `verso/`

test-vault 里那 3 个旧写法的代码块和 `.gitignore` 已经一并转换过来，
所以什么都不会失效。**如果你还有别的 vault 是改名前建的**，里面的
` ```folio-view ` 块现在会显示成普通代码块，`.gitignore` 也还写着 `.folio/`
（意味着索引数据库不再被忽略，可能被误提交）。两处都是查找替换就能修好。

改名前的 `.folio/` 目录可以直接删掉——纯派生数据，`.verso/` 已经重建过了。

91 个 Rust 测试 + 143 个 Node 测试 + 21 个浏览器测试通过。

---

## v0.5.2 — 改名：Folio → Verso

### 为什么改

**FOLIO（folio.org）是一个活跃的开源图书馆管理平台**，2026 年 3 月还在发新版本，
占着 folio.org 域名和 GitHub 的 `folio-org` 组织名。同属「开源软件」这个语境，
是最容易被混淆的一类重名。此外 "folio" 还是 portfolio 的常用简写，作品集模板
泛滥，搜索环境很差。

**时机只有一次**：bundle identifier 发布后不可改（改了系统会当成另一个应用，
用户数据和权限授权全部丢失）。发布前改成本接近零。

### Verso 是什么意思

书籍学里指一叶纸的**背面**，也就是翻开一本书时左手边那一页（正面叫 recto）。
词源上 verso 是拉丁语 `verso folio`（"翻过这一叶"）的省略——所以它和原来的
名字出自同一个短语，不是推翻，是取了另一半。中文名 **页**。

### 你的笔记不受影响

- **` ```folio-view ` 代码块照常渲染。** 解析器同时认新旧两种写法。那些写法
  在你的 `.md` 里，是你的数据不是我们的实现细节——软件改名不该让已经写下的
  笔记失效。`/` 菜单从现在起只插入 ` ```verso-view `
- **旧 vault 的 `.gitignore` 会自动补上 `.verso/`。** 不补的话索引数据库会变成
  未忽略文件，很容易被误提交进笔记仓库。只追加、不改动你已有的任何规则
- **`.verso/` 会重建**，`.folio/` 留在原地不动（它是派生数据，可以直接删掉）
- 侧栏开合、终端高度这些界面状态会重置一次

94 个 Rust 测试 + 145 个 Node 测试 + 21 个浏览器测试通过。

---

## v0.5.1 — 左侧图标栏

原来七个按钮全挤在侧栏标题里，vault 名被截成了「test-v...」。现在拆成
VS Code 那种左侧一竖条。

**上半部切换侧栏显示什么**：文档树、搜索、标签。点已经选中的那个会收起
侧栏，把编辑区拉到最宽。**下半部是直接执行的动作**：终端、命令面板、设置。
两组用留白和选中竖标分开 —— 不分开的话，点「设置」会让人以为侧栏要变成
设置面板。

侧栏开合状态和当前视图都跨会话保留。

### 搜索改成侧栏面板

原来是弹窗，而弹窗**会挡住正文**——搜索结果恰恰是要一条条点开看的，
每点一次就得重新打开、重新输入。现在结果留在侧栏里，点着看过去就行。

`Ctrl+Shift+F` 仍然是全文搜索：不在搜索视图时切过去并聚焦输入框；
已经在搜索视图时只把焦点送回输入框，**不会把侧栏关掉** —— 再按一次的
意图是「回到搜索框」，不是「关掉搜索」。

### 标签面板（新）

按 `#嵌套/标签` 展开成树。点一个标签，下面列出带它的笔记，**包括子标签下的**
—— 否则点父标签永远是空的，嵌套就白分了。

只写过 `#项目/写作` 的话，索引里其实没有「项目」这一条，面板会把中间层
补出来，不显示计数，表示「它自己没有笔记，但下面有」。

vault 名移到了侧栏标题右边，点它切换 vault。

---

## v0.5.0 — M4 打磨

### 深浅主题

三态：跟随系统、浅色、深色。跟随系统时会随系统实时切换，不用重启。
两套主题用**同一组色相**、只翻转明度 —— 色相一致是观感统一的来源。

终端也跟着变色。它的配色不走 CSS，所以是单独处理的。

### 设置界面

`Ctrl+,` 打开，或者从侧栏的齿轮进。四个页签：

- **外观** —— 主题、界面字号、界面与正文字体、等宽字体
- **编辑器** —— 正文字号、行高、正文栏宽
- **终端** —— 终端字号、终端字体（留空跟随等宽字体）
- **公式 snippet** —— 自己写的 snippet，格式与 Obsidian Latex Suite 相同，
  可以把现有配置整段粘过来

**改一下立刻生效，没有「保存」按钮**：调字号这种事必须边看边调。
每一项旁边有「恢复默认」，底部有「全部恢复默认」。

自定义 snippet 会逐条校验并告诉你第几条、哪个字段有问题。**一条写坏了不会
连累其他** —— 剩下的照常生效。改完不会打断你正在写的东西，光标和撤销
历史都在。

设置存在应用配置目录，跟着人走 —— 换一个 vault 不用重新调一遍。

### 命令面板

`Ctrl+Shift+P`。所有功能都在里面，**每条都显示自己的快捷键**。

这是有意的：Verso 不做插件系统，功能全靠内置，命令面板是唯一能让人发现
「原来还能这样」的地方。搜索用的是快速切换器那套模糊匹配，手感一致。

### 其他

- 空状态不再只有一行字，列出了常用快捷键
- 状态栏右侧显示**字数**而不是内部 id。中文按字数、西文按词数，
  代码块和块级公式不计入
- 补全面板（`[[` 和 `/`）重做了样式 —— 之前用的是 CodeMirror 的默认外观，
  深色主题下会变成白底黑字的一块
- 快捷键提示按平台显示：Mac 上是 `⇧⌘P`，其他平台是 `Ctrl+Shift+P`
- 尊重系统的「减弱动态效果」设置

**macOS 的透明标题栏没做** —— 需要在真机上确认交通灯不会压住侧栏内容，
而开发机是 Windows。默认标题栏是安全的，留到能验证时再说。

86 个 Rust 测试 + 134 个 Node 测试 + 21 个浏览器测试通过。

---

## v0.4.2 — `/` 菜单真正修好；database 视图确认正常

### 修复

**打 `/` 什么都不弹。** 上个版本改的是触发条件，方向不对 —— 菜单其实一直被
算了出来，只是被 CodeMirror 又滤了一遍滤空了。它会拿 `/` 到光标之间的整段
文本去模糊匹配每个选项的名字，而 `/` 不出现在「一级标题」「待办」这些名字里，
于是全部落空。现在告诉它「我已经筛过了，别再筛」，顺带 `/###`、`/table`
这种按符号或英文搜也能用了。

`[[` 链接补全一直是好的 —— 纯属因为它交给 CodeMirror 的是 `[[` 之后的文字。

### 确认没有问题

**database 视图打开笔记就渲染，不需要先点一下。** 上个版本记的「未解决」
是错的：在真实浏览器里跑下来，打开即渲染、一篇多个视图、光标进出切换源码、
现打出来的视图，四种情况全部正常。之前查不清是因为手上没有能跑真实编辑器的
测试环境。

### 内部

**加了浏览器测试。** `pnpm test:browser` 用 Playwright 拉真实 Chromium 跑
编辑器 —— Tauri 在 Windows 上用的就是 Chromium 内核，结论和应用里一致。

这是这两个 bug 一直查不出来的根本原因：CodeMirror 的补全、面板定位、块级
渲染的时序都依赖真实的布局引擎，而原来的测试跑在没有布局的模拟 DOM 里，
只会给「测试通过、应用里坏的」这种假象。`/` 的 10 个单测全过，正是因为它们
直接调函数看返回值，CodeMirror 的二次过滤根本没参与进来。

95 个 Node 测试 + 12 个浏览器测试 + 80 个 Rust 测试通过。

---

## v0.4.1 — 修 `/` 菜单；database 视图仍未修好

### 修复

**`/` 命令菜单在段落中间和行尾不触发。** 触发条件写成了「`/` 之前整行必须为空」，
而最常见的用法恰恰是在写到一半时按 `/`（Notion 里任何位置都能用）。
改成只看紧邻的前一个字符是不是空白 —— 那已经足够挡住 `a/b` 和 URL 里的 `/`。

补了 10 个补全测试（`completion.test.ts`），直接喂 `CompletionContext` 验行为 ——
「什么时候该弹、什么时候不该弹」用截图根本验不了。

### ⚠️ 仍未解决

**database 视图打开笔记时不渲染，点一下才出来。** 已经排除：匹配逻辑
（4 个测试证明 `build()` 在新建 state 上能找到全部块）、Rust 侧、扩展装配、HMR。
四条修复思路都试过并失败，记录在 AGENTS.md，包括下一步该怎么查 ——
关键矛盾是「`build()` 找得到节点，但 `field.create()` 像是没产出 decoration」。

95 个前端测试 + 80 个 Rust 测试通过。

---

## v0.4.0 — M3 索引与 database

你最初就说「最好用的其实是数据库功能」的那个东西，现在有了。

### 索引（§2.5）

SQLite + FTS5，全部是派生数据 —— `.verso/index.db` 删掉会重建，只丢时间不丢内容。
打开 vault 时建索引，保存/新建/改属性时增量更新，重命名/移动/删除后整库重建。
**500 篇索引 < 1s，搜索 < 50ms**，都在 §7 的验收线内。

### 全文搜索（`Ctrl+Shift+F`）

**中文两字词也能搜到。** trigram 分词器要求查询至少 3 个字符，而「矩阵」
「线性」「函数」恰恰是中文最常搜的 —— 短查询退回 `LIKE` 全表扫描，
慢一个数量级但万级笔记的正文也就几 MB，实测仍在验收线内。

搜索结果的 `<mark>` 按标记切开渲染，**不用 `dangerouslySetInnerHTML`** ——
片段里含笔记正文，而笔记可能来自分享或 AI 生成，直接塞进 DOM 等于把
「打开一篇笔记」变成「执行一段陌生 HTML」（与 §7.5 同一条边界）。

### 反向链接

挂在编辑器底部，带出处上下文。别名链接 `[[特征值|特征向量]]` 正确算在
「特征值」头上。代码块里的示例链接不会被索引 —— 否则一篇讲 Markdown 语法的
笔记会把自己的示例全变成噪音。

### 文件监听（§2.7）

`notify` + 300ms 去抖 + 回环抑制。有了终端跑 AI 之后，「文件被外部程序修改」
从偶发边界情况变成日常主路径 —— 而且比「窗口聚焦时比对 mtime」更及时：
AI 在终端里改文件时窗口一直是聚焦的，那条旧路径根本不会触发。

### 输入辅助

- **`[[` 补全**：模糊匹配笔记名；没匹配时给一条「新建这篇笔记」，
  链到还不存在的笔记是 wiki 式写作的常态
- **`/` 命令菜单**：17 种块插入，不用记 Markdown 语法。
  公式里的 `/` 是除号和 `rac` 的触发词，不弹菜单

### database 视图（§2.6）

` ```verso-view ` 代码块渲染成表格或看板。数据来源是每篇笔记的 frontmatter，
底下仍然只是一堆 `.md`。

- `from` 路径 glob、`tag` 过滤、`where` 条件（`= != > >= < <= contains`，`and` 连接）
- `sort` 按属性排序，数值优先 —— 否则 `"10" < "9"`
- **可写**：点单元格直接改，写回那篇笔记的 frontmatter。
  §2.6 说「这是它好不好用的分水岭」
- `id` / `created` 拒绝修改 —— 改了 id 等于换掉笔记身份，所有指向它的链接都会断

### 三个只在特定条件下才暴露的坑

**块级 decoration 的时序。** 打开笔记看到的是源码，点一下才渲染。
根因是 `EditorState.create` 时文档尚未解析（view 还不存在），StateField 拿到空树；
等解析完成、ViewPlugin 构造时，树已是最终形态、此后再不「变化」，
只靠比较永远不会触发。解法是构造时**无条件刷新一次**。
公式和 database 视图都受此影响。

**中文属性名让 Rust panic。** `where` 表达式解析用字节下标做 `s[i..]`，
属性名是 `难度`、`状态` 时会切在多字节 UTF-8 字符中间。改成按 char 边界遍历。

**contentless FTS5 表不可用。** 设计文档里 schema 写的 `content=''` 不支持
`snippet()`（搜索结果需要命中上下文），删除也要特殊语法。改成普通 FTS 表。

### 已知限制

- `where` 只支持 `and` 连接，没有括号和 `or` —— 视图定义是人手写的，
  复杂表达式既难写对也难读懂，用几个视图分开表达更清楚
- 看板不支持拖拽改分组
- 索引里不存正文行，反向链接的上下文是现读的

---

## v0.3.0 — M2 公式快速输入

整个项目的成败点。验收标准是盲测：抄一页教材公式，比在 Obsidian 里快。

**✅ 盲测已通过**（作者手测）。默认库的手感继续按 DESIGN.md §5.4 长期打磨。

代码侧有 43 个引擎测试跑在真实的 CodeMirror state 上、逐字符模拟输入 ——
保证「接进编辑器之后真的会发生」，而不只是「算得对」。

### snippet 引擎

- **格式与 Obsidian Latex Suite 兼容** —— 现有配置可以直接粘过来。
  一个练了两年肌肉记忆的人，不会为了换编辑器重学 200 个触发词
- `m` 仅公式内 / `t` 仅正文 / `A` 自动展开 / `r` 正则触发 / `w` 词边界
- **跳转点**：`$0` `$1` … 展开后光标落在 `$0`，Tab 依次前进，
  在跳转点里打的字会留在原地、后面的跳转点跟着移动
- **tabout**：跳转点走完后，Tab 跳出最近的闭合括号。
  写完 `\frac{a}{b}` 不用按四次方向键 —— 这个小功能对手感的贡献被严重低估
- Shift-Tab 退回起点，Escape 放弃当前这组跳转点
- 光标移出 snippet 范围就自动放弃，不会在你写别处时突然跳回旧位置

### 默认库（135 条）

希腊字母、分式根式、上下标、求和积分极限、导数偏导、关系符与逻辑符、
数集、自适应括号、标准函数名、矩阵与分段函数环境。全部带中文描述。

- `//` 分式、`xsr` → `x^{2}`、`Ainv` → `A^{-1}`、`@a` → `\alpha`
- `sin` `arctan` 等标准函数名自动补反斜杠（一条正则覆盖 21 个）
- **`pmat3x3` 按尺寸生成矩阵骨架**，Tab 逐格填。
  手写一个 3×3 要敲 8 个 `&` 和 2 个 `\\`，是抄板书时最烦的一步

### 符号面板（`Ctrl+/`）

覆盖 snippet 记不住的长尾。**用中文搜**「积分」「叉乘」「属于」「偏导」，
回车插入。最近用过的排在最前 —— 实测命中率很高。

### 两个设计层面的修正

**数学模式检测不能只看语法树。** §5.2 原本写「不要数 `$`，查语法树就对了」。
但**正在输入中的公式是未闭合的** —— 敲下 `$` 之后、配对的 `$` 之前，
解析器不会（也不该）建出 InlineMath 节点，而那正是最需要判断「我在公式里」
的时刻。改成混合方案：语法树负责已闭合的公式和**否决代码上下文**，
剩下的情况在本行内数未转义的 `$`。关键在于语法树先把代码挡掉了 ——
原先反对数 `$` 的理由（`` `$PATH` ``、代码块里的 `$x`）正是被这一步消除的。

**前缀关系的触发词不能都自动展开。** `pmat` 与 `pmat3x3` 是前缀关系，
两个都带 `A` 的话，打到第 5 个字符 `pmat` 就先炸了，带尺寸的那条永远轮不到。
`pmat` / `bmat` / `vmat` 改为 Tab 触发 —— 这也正是 `A` 标志本来就设计了
「不带它」这一半的原因。

### 修复

- **公式里的变量名被吃掉。** 写 `point` 时末尾的 `oint` 命中环路积分变成
  `p\oint`，`print` 变成 `pr\int`。凡是英文词形的触发词都补上了 `w`（词边界），
  只有 `sr` `cb` `inv` `trn` 这类**故意紧贴前一个 token** 的缩写例外。
  加了一条回归测试钉住 23 个常见标识符，以后新增 snippet 忘了加 `w` 会立刻失败

### 已知限制

- `\text{...}` 内部仍算数学模式，里面写英文句子可能误触
  （Latex Suite 也有同样问题，需要解析 LaTeX 内部结构才能根治）
- 货币写法 `$5 起，精装 $20` 之间的光标处会被判成「在公式里」，
  影响 snippet 触发但不影响渲染
- 用户自定义 snippet 还只能改代码，设置界面要等 **M4**

---

## v0.2.2 — vault 的 git 初始分支改为 main

- **新建 vault 时初始分支是 `main`，不再是 `master`。**
  libgit2 默认用 `master`，而且**不读 `init.defaultBranch` 这项 git 配置** ——
  必须显式指定，否则新建的 vault 与 GitHub 等平台的默认分支对不上，
  第一次推送就得手动改名
- **已有的空仓库会自动迁移**：打开 vault 时若发现它在 `master`
  且**没有任何提交、没有配置远端**，就改成 `main`，并在侧栏给出提示。
  这种状态下分支名纯粹是个名字，改它不破坏任何东西
- **有提交或有远端就绝不碰。** 悄悄给用户改分支名可能打断他们与远端的
  对应关系，也可能人家就是想用 `master`

---

## v0.2.1 — 内嵌终端

原本排在 M5，提前做了。理由是终端 + AI 是日常要用的东西，早点有才会真的每天用，
反而能更快暴露真问题（DESIGN.md §7.1）。

- **底部终端面板**，`Ctrl+\`` 切换，可拖拽调整高度，高度与开关状态跨会话保留
- 面板只占编辑区下方，侧栏保持通高 —— 与 VS Code 一致
- 工作目录默认 vault 根目录：AI 工具通常需要看到整个 vault 才能做跨笔记的操作
- Windows 上优先用 PowerShell 7（`pwsh`）—— 它默认 UTF-8，
  老的 `powershell.exe` 在中文环境下用 GBK，AI CLI 的输出会直接变乱码
- 侧栏终端按钮**右键**仍可调起独立的系统终端窗口（原方案 A）

**中文输出不乱码**：PTY 输出是原始字节，一个多字节 UTF-8 字符可能被读取边界切开。
Rust 侧用 base64 原样传给前端，由 xterm 自己解码，而不是在 Rust 里拼字符串。

**输出洪水防护**：Rust 侧按 16ms 时间窗聚合再推送，单帧上限 64KB。
读取与刷新分成两个线程 —— `read()` 会阻塞，若在读取线程里做超时刷新，
最后一小段输出会卡到下次有数据才送出，表现就是命令跑完了最后几行迟迟不显示。

### 修复（都会导致「面板打开了但一片空白」，且都不报错）

- **DSR 握手时序。** shell 启动时先发 `ESC[6n` 问光标位置，**收到回答才打印提示符**。
  xterm 的回答从 `onData` 出来，而原来的代码是先建 PTY 再注册 `onData` ——
  这段窗口里的回答丢了，shell 就永远停在等待状态。改成先注册、未就绪时攒着后补
- **初始 fit 时机。** `term.open()` 之后立刻 `fit()` 时面板刚插入 DOM、布局未完成，
  算出 0 列 0 行，PTY 以 2×2 启动。改用 `ResizeObserver`，拿到真实尺寸才开 PTY

### 与设计文档的偏离

§7.3 原写「关闭面板不杀进程」，实现改成**杀掉**：留一个用户看不见也管不到的
孤儿 shell 比中断一个任务更糟。想让 AI 长跑就别关面板。

### 已知限制

- 只有一个终端实例，没有多标签
- 终端字体固定为 JetBrains Mono / Cascadia Code / Consolas。
  用 Nerd Font 图标的提示符主题（oh-my-posh 等）会显示成方框，
  等 **M4** 有设置界面后可配置

---

## v0.2.0 — M1 编辑器

从「能存文件」变成「能写字」。CodeMirror 6 取代了 M0 那个 `<textarea>`，
公式、链接、加粗在光标移开时就地渲染成最终形态。

### 编辑器

- **Markdown 方言解析器**（`markdownExtended`）—— 整条链路的地基。识别行内/块级公式、
  `[[内部链接]]`、`![[嵌入]]`、`#标签`、`==高亮==`、callout，且不破坏标准 Markdown 与 GFM
- **Live preview** —— 光标移开就渲染，移进去就露出源码。作用于公式、链接、粗斜体、
  标题的 `#`、高亮、行内代码、删除线
- **KaTeX 公式渲染**，结果按公式源码做 LRU 缓存（1000 条），滚动来回不重复渲染
- 点击公式把光标送回源码，可以直接改
- 公式语法错误时显示错误信息，而不是让整块变红消失 —— 正在输入的公式几乎总是暂时不合法
- **frontmatter 属性条**，默认折叠成一行，不用手写 YAML，也不会误删 `id`

### 导航

- **快速切换器**（`Ctrl+P`）—— 模糊匹配笔记名与路径，中文两三个字直达。
  全量清单一次取回、在本地匹配，每次按键零 IPC
- **可点击的面包屑**
- **点击 `[[链接]]` 跳转**，目标不存在时可以直接按名字新建
- 记住上次打开的 vault **与笔记**，重启回到原处

### 文档树

- **重命名 / 移动 / 删除**（右键菜单 + 拖拽移动）
- 重命名是事务性的：`X.md` 与同名文件夹 `X/` 一起改，任一步失败就回滚 ——
  否则会留下父文档和子文档在树上断开的半吊子状态
- 删除父文档时可选择是否连同子文档；只删文档时子文档保留，节点降级成纯文件夹
- 删掉最后一个子文档后，空的同名文件夹自动清理
- 拒绝把文档移进它自己的子树（那会让整棵子树从树上消失）

### 其他

- **`Ctrl+\`` 在系统终端中打开 vault** —— §7.3 方案 A，用来跑 AI CLI
- **错误边界** —— React 未捕获错误会卸载整棵树变成白屏，桌面应用里这是最糟的
  失败模式（没有地址栏可以刷新）。现在至少把错误显示出来

### 修复

- **中文标点后的标签识别不出来。** `标签：#线性代数` 是中文写作里极常见的写法，
  但全角冒号不是空白字符，而原判据是「`#` 前必须是空白」。改成「前一个字符不是
  标签字符」，同时把 CJK 标点排除出标签字符集 —— 这也顺带修了 `#标签。` 会把
  句号吃进标签的问题
- 保存外来 `.md`（无 frontmatter）时补齐 `id`，否则笔记没有稳定标识，重命名即断链
- frontmatter 时间戳改为秒级精度，不再带 9 位小数在 git diff 里制造噪音

### 已知限制

- callout 只识别不渲染外观，`[!note]` 仍原样显示 → **M4**
- 属性条只读，可编辑的属性表单要等 **M3**（那时 database 视图需要回写 frontmatter，
  两者共用同一套控件才不会做两遍）
- 代码块不做语言高亮
- 图片嵌入 `![[fig.png]]` 只显示占位符

---

## v0.1.0 — M0 地基

第一个能跑的版本。能打开 vault、在文档树上点开笔记、编辑、保存不丢数据。
编辑器目前只是个 `<textarea>`，这是有意的 —— 真正的编辑器是 M1。

### 新增

- **打开 vault**，并记住上次打开的目录，下次启动自动重开
- **文档树**：`X.md` 与同名文件夹 `X/` 在树上合并成**一个**既有内容又能展开的节点，
  这就是 Notion / 思源 的文档嵌套，但底下仍是纯 `.md`（DESIGN.md §2.1）
- **新建文档 / 新建子文档**，建子文档时自动创建同名文件夹
- **编辑与保存**：停止输入 800ms 自动保存，失焦立即保存，`Ctrl+S` 手动保存
- **原子写入**：`写 .tmp → fsync → rename`，断电或崩溃不会留下半个文件（§2.7）
- **frontmatter**：ULID 稳定 `id`、`title`、`created` / `updated` 时间戳（§2.3）
- **打开 vault 时自动 `git init`** 并写入 `.gitignore`（`.verso/` 是派生数据，不进版本库）（§2.8）
- **外部修改检测**：窗口重新获得焦点时比对 mtime，文件被外部程序改过会给出提示，
  可选择加载外部版本或保留本地。有了终端跑 AI 之后这是日常主路径 ——
  没有它，用完 AI 回到编辑器一保存就把它的修改覆盖了（§7.4）
- **面包屑**：文档顶部显示所在层级
- **`VaultFs` 抽象层**：桌面走 `std::fs`。移动端本身是 M6，但文件层现在就不能写死（§1.2）
- **路径越界拒绝**：来自前端的路径一律拒绝 `..` 与绝对路径

### 安全

- 前端传来的路径不可信，全部经 `Vault::resolve` 校验，`../../` 无法读到 vault 以外的文件

### 已知限制（有意留到后续里程碑）

- 编辑器是 `<textarea>`，没有语法高亮、没有 live preview → **M1**
- 没有公式渲染 → **M2**
- 没有搜索、索引、反向链接、database 视图 → **M3**
- 重命名 / 移动 / 删除节点尚未实现（§2.1 的边界情况表列了全部要处理的场景）→ **M1**
- 仅桌面（Windows 已验证），移动端 → **M6**
