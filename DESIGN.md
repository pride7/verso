# Verso 设计文档

> 一个本地优先、排版考究、公式输入极快的笔记软件。桌面 + 移动端。

**已定决策**：名称 Verso ｜ Tauri 2（桌面 + 移动）｜ 纯 Markdown 文件存储 ｜ CodeMirror 6 编辑器 ｜ 页面级 database ｜ 同名文件夹实现文档嵌套 ｜ Git 作为同步/备份/发布底座 ｜ 附件默认统一放 `attachments/`（可配置）

### 命名与标识

**Verso** —— 书籍学里指一叶纸的**背面**，也就是翻开一本书时左手边的那一页（对应的正面叫 recto）。中文名 **页**。

词源上，verso 是拉丁语 `verso folio`（"翻过这一叶"）的省略。所以这个名字和原来的 Folio 出自同一个短语 —— 不是推翻，是取了它的另一半。

**为什么从 Folio 改过来**（v0.5.2）：

- **FOLIO（folio.org）是一个活跃的开源图书馆管理平台**，2026 年 3 月还在发新版本，占着 folio.org 域名和 GitHub 的 `folio-org` 组织。同属"开源软件"语境，是最容易混淆的一类碰撞。
- "folio" 还是 portfolio 的常用简写，作品集模板泛滥，搜索环境很差。
- 改名的时机只有一次：**bundle identifier 发布后不可改**。发布前改成本接近零，发布后等于放弃全部已安装用户。

改名发生在发布前，作者是当时唯一的用户，笔记与 vault 都当场转换完毕，**所以代码里没有留任何向后兼容**（v0.5.3 已清干净）。这条决定的前提很具体 —— 唯一的用户在场、数据量是三个代码块。发布之后再改任何写进用户 `.md` 的语法，就必须带迁移路径了：那些内容是用户的数据，不是我们的实现细节（§0 第 1 条）。

M0 就要固定下来的标识符（发布后改动代价很大）：

| 项 | 值 | 说明 |
|---|---|---|
| Bundle identifier | `app.verso.desktop` | **发布后不可改** —— 改了系统会当成另一个应用，用户数据与权限授权全部丢失 |
| 仓库 / 产品名 | `verso` | |
| Vault 私有目录 | `.verso/` | 纯派生数据，可整个删除，默认进 `.gitignore` |
| database 视图代码块 | ` ```verso-view ` | 见 §2.6。这是写进用户 `.md` 的语法，发布后再改必须带迁移路径 |
| macOS App 名 | `Verso.app` | |
| 图标 | 一张翻起一角的纸 | 单色、几何、16px 下可辨 |

---

## 0. 设计原则

按优先级排序，冲突时上位原则胜出：

1. **用户数据永远可以脱离本软件存在。** 关掉软件，用记事本也能读懂全部内容；直接拖进 Obsidian 也能用。这条否决一切「把内容存进数据库」的方案。
2. **公式输入速度是第一差异化点。** 目标：写一页手写公式的速度，不慢于在纸上写。
3. **默认即美观。** 不靠主题市场，不靠用户自己调 CSS。开箱的排版就是最终形态。
4. **不做插件系统（至少 v1 不做）。** 插件系统会立刻把 UI 一致性和性能拱手让人 —— 这正是 Obsidian 变丑变慢的原因。

---

## 1. 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 应用壳 | **Tauri 2** | 同一套代码出桌面 + iOS + Android，安装包 ~10MB、内存约为 Electron 一半 |
| 后端 | Rust：`rusqlite`(bundled + FTS5)、`notify`(文件监听)、`serde_yaml` | 文件 IO / 索引 / 监听 |
| 前端 | React 19 + TypeScript + Vite | 生态与招人成本 |
| 样式 | Tailwind v4 + CSS 变量做主题 | v4 的 `@theme` 直接生成 CSS 变量，主题切换零 JS |
| 编辑器 | **CodeMirror 6** | 见 §4.1 —— 已确定，公式手感优先 |
| Markdown 解析 | `@lezer/markdown`（前端增量解析）+ `pulldown-cmark`（Rust 侧批量索引） | 两处都要解析，用途不同 |
| 公式渲染 | KaTeX 0.16+ | 同步渲染、无布局抖动；MathJax 的异步渲染会导致 live preview 闪烁 |
| 状态 | Zustand | Redux 太重，Context 会导致编辑器重渲染 |

### 1.1 WebView 差异 —— 提前知道的坑

Tauri 用**系统 WebView**：Windows = WebView2（Chromium），macOS/iOS = WKWebView（Safari），Android = Android WebView（Chromium），Linux = WebKitGTK。字体渲染、`backdrop-filter`、CJK 断行行为都有差异。

对策：
- 视觉设计以 **WebView2 为基准**（开发环境），每个 milestone 结束在 macOS + iOS 上过一遍 —— 这两个是同一个引擎，一起验。
- 避开 WebKit 支持不稳的特性：`text-autospace`、`::highlight()`。一律做**渐进增强**，不能是布局的必要条件。
- Linux 的 WebKitGTK 落后最多，v1 只声明支持 Windows / macOS / iOS / Android。

### 1.2 移动端的三个真实代价

**(a0) 窄屏布局先行（v0.6.2）。** 打包还没做之前，窄屏这一半就能做、也能验：
断点 640px，**侧栏从「占一列」变成「盖一层」**（390px 的屏上，252 的侧栏加
44 的图标栏之后只剩 94px 给正文，那不是窄是没法读），点开一篇之后抽屉自己
收起来，正文边距按 §6.1 收到 20px。

**要分开的是三件事，不是两件**（v0.7.21 把第三件拆出来）：

| 跟着什么走 | 决定什么 | 判据 |
|---|---|---|
| **视口** | 布局：抽屉、底部导航、单栏 | `max-width: 640px` |
| **平台** | 能力：有没有终端、有没有目录选择器 | `platform_is_mobile` |
| **指针** | 尺寸：点击目标要多大 | `hover: none`，或平台是手机 |

三者经常同时为真，但**把桌面窗口拖窄的人仍然握着鼠标**，不该被撑成 44px
的行距；触屏笔记本反过来，屏幕很宽却需要大目标。

指针这一层落成 `<html data-touch="on">` 加两个尺寸令牌：`--tap`（44px，
手指接触面直径，Apple HIG 与 Material 同一个数）和 `--tap-sm`（32px，嵌在
已有行里的次要图标 —— 撑到 44 会把那一行整个顶开）。桌面上两者都是 `0px`，
于是 `min-height: var(--tap)` 自动失效。

**为什么是属性开关而不是 `@media (hover: none)`**：headless Chromium 报的是
`hover: hover`，媒体查询里的那一套在浏览器测试和视觉工作台里**永远不会生效**
—— 移动端的尺寸问题长期没人看得见，正是因为它在这里量不出来。开关由入口
（`main.tsx`）在 React 之前按指针能力同步打上：编辑器第一帧就要读它决定要不要
抢焦点，晚一帧软键盘已经弹起来了。

**触摸设备上打开一篇笔记不自动抢焦点。** 在桌面上那是白拿的便利（光标就位，
直接能打字），在手机上代价很实在：每开一篇键盘就弹一次，吃掉半屏、把公式
工具条顶上来，而多数时候用户只是想读一眼。

**(a) 公式输入必须是独立的一套 UX。** snippet 引擎的全部前提是物理键盘 —— `//` 自动展开、Tab 跳 tabstop、tabout 跳出括号。手机软键盘没有 Tab 键，也不存在"打字快"。移动端方案见 §5.5：软键盘上方的自定义工具条。这是两套输入交互，不是一套的响应式适配。

**(b0) 安卓上 vault 放哪儿（v0.6.5 定的）。** 两家参考对象走的不是一条路：

| | 做法 | 代价 |
|---|---|---|
| **思源** | 申请「所有文件访问权限」（`MANAGE_EXTERNAL_STORAGE`），工作空间是 `/storage/emulated/0/siyuan` 这样的**真实路径** | 用户要在系统设置里手动授权一次 |
| **Obsidian** | SAF 目录选择器，拿到的是 `content://` URI | 不是文件路径，`std::fs` 用不了，`VaultFs` 要在安卓上用 JNI 整套重写，libgit2 也读不了它 |

**走思源那条。** 真实路径意味着 Rust 侧一行都不用改 —— `std::fs`、git2、文件
监听全部照常；而且笔记在文件管理器里看得见、Syncthing 之类同步得了。SAF 那条路
留给真需要「任意文件夹」时再说。（安卓的 git 同步曾因 OpenSSL 的死结缺席，
后来用 rustls 自定义传输层补上了，见 §2.8。）

**手机上不问「选哪个文件夹」**：Tauri 的目录选择器在移动端没有实现，点下去
毫无反应（v0.6.4 的安卓包就是这个症状）。既然答案基本唯一，就别摆选择题 ——
默认 `/storage/emulated/0/Verso`，没授权时退回 App 私有目录（能用，只是别处
看不见）。**授权页不强制、不循环弹**：一个把人锁在授权页上的笔记软件更糟。

判断「这个目录能不能用」要**真的写一个文件**：安卓上 `create_dir_all` 在没
权限时也可能返回 Ok，而「目录建得出来、文件写不进去」是最难查的一种坏法。

**(b) iOS 文件访问是最硬的约束。** iOS 沙盒里没有"一个可自由读写的文件夹"。可行路径基本只有 iCloud Drive 的 ubiquity container —— Obsidian iOS 版就是这么做的，也是它被骂最多的地方。Android 用 SAF（Storage Access Framework）宽松得多。

**(c) 同步从可选变成必须。** 桌面端可以说"你自己挂 OneDrive"，加了手机端这话就说不通了。v1 仍不自建同步服务，但要保证：iOS 走 iCloud Drive、Android/桌面走用户自选的同步盘，且**外部修改处理必须绝对可靠**（§2.7）。

**架构上现在就要做的两件事**（避免以后返工）：
1. 文件系统层抽象成 `VaultFs` 接口，桌面走 `std::fs`，iOS 走 ubiquity container，Android 走 SAF。业务代码只依赖接口。
2. 编辑器交互不假设有键盘。任何只能用快捷键完成的操作，必须有等价的可点击入口。

---

## 2. 数据格式与 Vault 结构

### 2.1 目录布局与文档嵌套

**文档嵌套用「同名文件夹」实现** —— 一个文档若有子文档，就在旁边建一个同名文件夹装它们：

```
MyVault/
├── 数学/
│   ├── 线性代数.md              ← 父文档本身
│   ├── 线性代数/                ← 同名文件夹 = 它的子文档
│   │   ├── 特征值.md
│   │   ├── 奇异值分解.md
│   │   └── 奇异值分解/          ← 可继续嵌套
│   │       └── 计算方法.md
│   └── 泛函分析.md
├── attachments/                  # 默认附件目录，可配置
├── AGENTS.md                     # 给 AI CLI 的约定说明（§7.7）。不进普通文档树
├── CLAUDE.md                     # 一行兼容指针，同样不进普通文档树
├── .verso-order.json             # 文档树的手动顺序。进版本库，跟着 vault 走
└── .verso/                    # 本软件私有，可整个删除
    ├── index.db                  # SQLite 索引缓存，纯派生数据
    ├── config.json
    ├── snippets.json             # 用户自定义公式 snippet
    └── workspace.json            # 打开着的标签页（v0.5.19）
```

**标签页**（v0.5.19）。`{tabs: 路径[], active: 下标}`，存在上面那个
`workspace.json` 里。和手动排序的 `.verso-order.json` 分在两边，判断标准是
**丢了要不要紧**：手动排序丢了重建不出来（那是一个一个拖出来的意图），
而标签丢了只是「下次启动少开几个页签」—— 正是铁律第 6 条说的那种可重建的
派生状态。它天然也是 per-machine 的：两台机器开着同一个 vault，各自开了什么
页签本来就该各算各的，`.verso/` 在 `.gitignore` 里这一点自动成立。

行为上的几个决定（状态迁移全在 `src/core/tabs.ts` 的纯函数里，规则本身是一堆
边界情况，放进 React 状态就验不动了）：

| 决定 | 为什么 |
|---|---|
| 默认**开新标签**，设置里可改成替换当前 | 作者定的默认。Ctrl/⌘+点 和中键在两种模式下都强制开新 —— 那是明确表态，不该被设置盖掉 |
| 已经开着的那一篇总是**切过去**，不开第二个 | 这一版没有分屏，两个标签指着同一份内容只会让人分不清改的是哪个 |
| 新标签插在**当前页右边** | 从一篇笔记点进它的链接时，新页紧挨着来源，读完关掉就回到原处 |
| 关掉当前页时接班的是**右边那个** | 总是往左的话，「从左往右挨个关」会变成每关一次就跳回开头 |
| 每页缓存 CodeMirror 的 `EditorState` | 切回来时光标、选区、**撤销历史**都在。文档对不上（笔记在别处被改过）就丢掉重建 —— 把旧的撤销栈接到不一样的内容上，撤销会把文件改成从来没存在过的样子 |
| 重命名 / 删除连**整棵子树**一起处理 | 和 `vault/order.rs` 的 `rename_path` 同一个道理：改 `X.md` 时 `X/` 底下所有孙节点的路径都变了 |

**UI 上把 `X.md` 和 `X/` 合并成树上的同一个节点**：

```
▼ 数学
  ▼ 线性代数            ← 点文字打开文档，点箭头展开
      特征值
    ▼ 奇异值分解
        计算方法
    泛函分析
```

磁盘上是文件 + 文件夹两个对象，用户看到的是一个「既有内容、又能展开」的节点 —— 这就是 Notion / 思源 的文档树，但底下仍是纯 `.md`。这个 vault 直接拖进 Obsidian 或资源管理器都完全正常，只是显示成两个节点，数据零损失。

**需要正确处理的操作**

| 操作 | 行为 |
|---|---|
| 新建子文档 | 若 `X/` 不存在则创建，再在其中建文件 |
| 重命名 `X` → `Y` | 同时改 `X.md` → `Y.md` 和 `X/` → `Y/`。先改文件夹再改文件，任一步失败则回滚 |
| 拖拽移动节点 | `.md` 与同名文件夹一起移动 |
| 删除父文档 | 弹窗询问「仅删除本文档」/「连同 N 个子文档一起删除」。仅删文档时保留文件夹，UI 降级显示为纯文件夹节点 |
| 纯文件夹（无同名 `.md`） | 正常显示为文件夹，提供「创建为文档」把它升级成文档节点。删除 / 新建子文档 / 重命名 / 移动同样可用（v0.6.30）—— 否则它就是个只能看的死节点；删除即删整个目录，弹窗写明连同几个子文档 |
| 子文档为空时 | 自动删除空的同名文件夹，避免残留 |

**为什么不用 `X/index.md` 那种（Hugo `_index.md`）布局**：那样每个文档都得有一个文件夹，或者要维护「普通文档」和「文件夹文档」两种形态的相互转换，逻辑更绕；而且在 Obsidian 等其他工具里打开会看到一堆同名的 `index.md`，非常难用。

**一键折叠 / 展开整棵树**（v0.7.32）：头部除了排序和新建，还有一个折叠按钮。
一棵树展开几层之后，想看清结构只能一行行手动收 —— 而「全收起来，再展开我要的
那一支」正是找东西的常用手法。**第一下是收起**：树的初始状态就是「顶层展开、
下面收着」，那时候人想要的必然是继续收。

展开状态归**每一行自己**（展开一个节点不该惊动整棵树重渲染），所以这个按钮
发的是一个带时间戳的信号，每一行各自响应；连按两次同一个方向也要生效 ——
中间手动展开过的那一支同样要收回去。

**文档树的排序**（v0.5.13）

五档，存在设置里（`treeSort`）：手动、名称 A→Z、名称 Z→A、最近创建、最近修改。除「手动」外都只影响显示，不写任何文件。名称一档走自然序 —— `第2章` 要排在 `第10章` 前面，纯字典序排出来没法看。时间一档缺时间戳的沉到底部，不能当成 1970 年排最前。

手动顺序存在 **vault 根目录的 `.verso-order.json`**：

```json
{
  "": ["数学.md", "论文.md"],
  "数学": ["数学/线性代数.md", "数学/泛函分析.md"]
}
```

键是父目录的相对路径（根是空串），值是这一组兄弟的完整相对路径。三个候选位置各自的问题：

| 位置 | 问题 |
|---|---|
| `.verso/` | 在 `.gitignore` 里，换台机器就没了 —— M5 是 git 同步、M6 是移动端，这基本是判死刑。且铁律第 6 条要求 `.verso/` 能整个删掉重建，而手排的顺序重建不出来 |
| 每篇笔记的 frontmatter | 顺序跟着文件走不会丢，但每篇笔记都被塞进一个用户没写的 `order`；更要命的是**写 frontmatter 会刷新 `updated`** —— 拖一次就把一整组笔记的修改时间改成现在，「最近修改」这一档当场失效 |
| **vault 根的单文件** | 笔记一个字不动，文件进版本库所以跟着 vault 同步，拖一次只改一个文件 |

代价是它**可能变陈旧** —— 在别的软件里重命名笔记，那一条就对不上了。处理方式是优雅降级：认不出的路径当作「没排过」，沉到底部按名字排，而不是报错。Verso 自己的重命名 / 移动 / 删除会同步修好这个文件，包括整棵子树（重命名 `数学.md` 时，`数学/` 下所有孙节点在别的分组里的条目都要跟着改，否则整棵子树的顺序一次性全丢）。

拖拽时前端提交的是**整组兄弟的完整清单**，不是只报被拖的那一个 —— 这一组可能压根没排过，或者顺序文件已经陈旧，整组重写才能保证结果稳定。侧栏行的上下 25% 是重排（前 / 后插入），中间 50% 是「移动进去」。

**任何排序模式下都能拖**（v0.5.14）。拖动本身就是「我要自己定顺序」的表态，再要求先去下拉框里选一次「手动排序」纯属多余 —— 默认是按名称排，最常见的情况恰恰是拖了没反应。所以拖完由前端顺手把 `treeSort` 切成 `manual`，并提示一句（静悄悄改掉一个下拉框的值，用户下次会莫名其妙）。

两个细节：

- 重排的**起点是屏幕上当前的顺序**，不是顺序文件里记着的。从规则排序切过来时这一点是关键 —— 按名字排的时候拖一下，除了被拖的那个，别的都不该动
- **跨目录拖到边缘 = 先移过去，再排到那个位置**。行已经高亮着「插到这里」了，只因为来源目录不同就静悄悄什么都不做，是最难受的一种失败。唯一放弃的情况是目标那一组的父节点是纯文件夹 —— 没有同名文档就当不了「父文档」

**所有拖拽都依赖 `tauri.conf.json` 里的 `dragDropEnabled: false`**（v0.5.15）。Tauri 默认在操作系统层接管拖放，webview 里的 `dragstart` / `drop` 收不到 —— 拖拽移动和拖拽排序会一起变成「拖了毫无反应」，而浏览器测试（纯 Chromium，没有这一层）照样全绿。代价是从资源管理器往窗口里拖文件不再走 Tauri 的 `drag-drop` 事件，要做那个功能就用 HTML5 的 `drop` + `dataTransfer.files` 自己接。

**标签页**（v0.5.19）

编辑区上方一条标签栏。点侧栏文件时**默认开新标签**，设置里可以改成「替换当前」；两种模式下 `Ctrl/⌘+点` 和中键都强制开新标签 —— 那是一个明确的表态，不该被设置盖掉。已经开着的那一篇总是切过去，**不会开出第二个**：这一版没有分屏，两个标签指着同一份内容只会让人分不清改的是哪个。

新标签插在**当前页右边**而不是队尾，这样从一篇笔记点进它的链接时，新页紧挨着来源，读完关掉就回到原处。关掉当前页时接班的是**右边那个**；反过来（总是往左）会让「从左往右挨个关」变成每关一次就跳回开头。

状态迁移的规则全在 `src/core/tabs.ts` 的纯函数里，组件只管画。这类规则是一堆边界情况（关了跳到谁、拖动之后当前页是哪个、重命名连不连子树），放在 React 状态里没法单独验。

**切回一个标签时，光标、选区、撤销历史、滚动位置都还在。** 做法是把离开时的 CodeMirror `EditorState` 按路径存起来，切回来直接接着用；文档对不上（笔记在别处被改过）就丢掉重建 —— 把旧的撤销历史接到一份不一样的内容上，撤销会把文件改成从来没存在过的样子。

**只有当前页的正文在内存里。** 切页之前一定会先落盘，所以别的页没有未保存的东西要护，不必留一份正文。

| 存在哪 | 为什么 |
|---|---|
| `.verso/workspace.json` | 丢了只是「下次启动少开几个页签」，重建方式就是你再点一次 —— 正是铁律第 6 条说的那种派生数据。而且它是 per-machine 的：两台机器同一个 vault，各自开着什么页签本来就该各算各的，`.verso/` 在 `.gitignore` 里这一点自动成立 |

对比一下 `.verso-order.json`（手动排序）为什么在 vault 根、进版本库：那是用户一个一个拖出来的意图，**丢了重建不出来**。判断标准是「丢了要不要紧」，不是「像不像 UI 状态」。

**仓库切换与管理**（v0.6.24）

桌面端每次成功打开一个 vault，都把它记进应用配置目录的 `recent.json`。这份清单
跟设置一样属于「这台设备上的使用习惯」，不能写进任何一个 vault：在还没有打开
vault 时就要能读它，而且换库正是它存在的原因。旧版只记 `lastVault`，升级后要把
那一条自动迁进清单，不能逼用户重新选一次目录。

侧栏底部仍显示当前仓库，但点击后先打开一个向上的轻量菜单，而不是立刻调文件选择器：

- 当前仓库带勾；其他已记录仓库一点即切换，按最近使用排序；
- 同名仓库显示路径帮助消歧，目录已经移走的条目标成「位置不可用」，不静默删除；
- 最后一项是「管理空间…」，进入完整清单后可以打开新目录、切换，以及从清单移除
  不再使用的条目。移除只是忘记入口，**绝不删除目录或任何笔记**；
- 欢迎页也列出已记录仓库。上次那个目录失效时，仍可直接打开其他记录，不必再进
  文件管理器寻找。

切换仓库等同于结束当前工作区：先保存当前笔记、保存 `.verso/workspace.json`，并在
「关闭时自动记录版本」开启时完成一次本地版本记录；任一步失败就停在当前仓库。
新仓库加载自己的标签页、历史状态与终端工作目录，不能把前一个仓库的 UI 状态串过来。
**移动端也能有多个仓库（v0.7.21）**，但结构不一样：那边没有目录选择器
（§1.2 b0），所以仓库不能散落在任意位置，而是统一放在**一个容器目录**里 ——
`/storage/emulated/0/Verso/`，每个子文件夹是一个仓库。手机的文件管理器里
一眼看得懂：一层目录，一个文件夹一个仓库。

- **判据是 `.git`，不是「像不像仓库」。** `Vault::open` 一定会 `ensure_repo`，
  所以每个仓库根都有 `.git/`。不能用「有没有 `.md`」那类启发式 —— 同名文件夹
  嵌套意味着 `数学.md` 旁边就有一个 `数学/`，而那是一篇笔记的子目录；用启发式
  的话，用户每建一篇带子文档的笔记，仓库列表里就多出一条假的。
- **列表来自磁盘，不是「打开过的」。** 桌面的仓库列表是 `recent.json`（用户
  自己选过的目录），手机上那份在新装的机器上永远是空的 —— 而容器里明明有
  仓库。两边共用同一个界面，数据来源不同。
- **老版本的笔记就地迁移。** v0.7.21 之前容器本身就是仓库，笔记直接躺在
  `Verso/` 下。迁移**不逐个搬文件**（搬到一半失败会留下说不清的半截状态），
  而是两次整目录改名：`Verso → Verso.migrating`、建新 `Verso`、
  `Verso.migrating → Verso/默认`。每一次都是原子的，中间失败原样退回。
  上一次留下的中转目录还在时**直接报错不猜** —— 两边都可能有用户的笔记，
  任何一种合并规则都可能悄悄盖掉一份。
- **手机上不出现「打开其他文件夹」和「管理空间」**：两者都要目录选择器，
  留着就是按下去没反应的按钮。换成「新建仓库」——只要一个名字，位置由后端
  在容器里定。名字要挡掉路径分隔符（会建到容器外面去）和 Windows 建不出来
  的字符（vault 会同步到桌面，那边建不出目录就整个炸掉）。
- 输入框**就地长在菜单里，不用 `window.prompt`** —— 安卓 WebView 上它可能
  根本不弹，那时「新建仓库」就是个按下去没反应的按钮。

#### 在树中收起一整支（v0.7.38）

一个项目或知识库底下可能有几十篇子文档，它们在文档树里铺开只会把别的全挤下
去 —— 而那些内容本来就该从它自己那一页进去（项目总览、database 视图）。右键
选择「在树中收起」之后，这一行在树里**当成没有子文档**：没有展开箭头，名字右边
只留一个数字说明收了几篇。

- 标记写进那篇笔记自己的 frontmatter（`collapsed: true`，声明成 checkbox 所以
  落成真布尔），不写 `.verso/`：换台机器、拿别的编辑器打开都该跟着走（§0 第 1 条）
- 树的每一行都要知道这件事，所以走索引查一次、随树一起下发，和 `icon` 同一条
  路 —— 逐篇读 frontmatter 等于刷新一次树就把整个 vault 读一遍
- **那个数字不能省**：没有它，「这一篇没有子文档」和「它被收起来了」在树上
  长得一模一样

### 2.2 导航 —— 嵌套之外真正解决「找不到」的东西

文档嵌套解决的是**组织**，不完全是**查找**。日常导航主力其实是：

1. **快速切换器**（`Ctrl/Cmd + P`）—— 模糊匹配笔记名 + 路径，两三个字母直达。熟练用户九成的跳转靠它。**优先级高于文件树**，M1 就要有。
2. **面包屑** —— 文档顶部显示 `数学 / 线性代数 / 奇异值分解`，每级可点击。深层嵌套不迷路的前提。
3. **全文搜索**（§2.5）
4. **反向链接面板** —— 从"谁引用了我"反向找回来
5. **最近打开**

### 2.3 单个笔记的格式

```markdown
---
id: 01J8XKQ2M4N7P9R3T5V8W1Y2Z0
title: 奇异值分解
created: 2026-07-31T09:12:03+08:00
updated: 2026-07-31T14:20:11+08:00
tags: [线性代数, 矩阵分解]
status: 草稿
难度: 4
---

任意矩阵 $A \in \mathbb{R}^{m \times n}$ 都可以分解为

$$
A = U \Sigma V^{\mathsf{T}}
$$

其中 $U$ 的列是 [[特征值|左奇异向量]]。

![[fig-svd-1.png]]
```

**Verso 不往你的文件里写任何你没要求的字段。**（v0.5.12 定）

新建的笔记是一个**空文件**；保存不会补 `id` / `title` / `created`；`updated` 只在这个键**已经存在**时才刷新。删掉整块 frontmatter 就是真的删掉，下次保存不会长回来。

早先的做法相反 —— 每次保存都补齐那四个字段，于是一篇只写了两行字的笔记，头上永远顶着五行 YAML。反过来算了一笔账才改：这四个字段里真正被代码用到的只有排序（`created` / `updated`），而 `id` 连链接解析都没走它。**「文档保持简洁、原始」比一个尚未兑现的保险更值钱。**

**frontmatter 字段约定**（下面这些都是「你写了才有」）

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | ULID | 稳定标识，给「重命名后按 id 修链」用。**不再自动写入** —— 想要就自己写一个 |
| `title` | string | 缺省时回退到文件名 |
| `created` / `updated` | RFC3339 带时区 | 文档树按时间排序的依据；缺省时回落到文件系统 mtime（同步工具会破坏 mtime，所以有这两个字段时优先用它们）。`updated` 在已存在时每次保存自动刷新 |
| `tags` | string[] | 与正文内的 `#标签` 合并 |
| `publish` | bool | 是否发布到静态站点。**缺省为 false**（安全默认），见 §2.9 |
| `icon` | string | 文档图标，一个字符（通常是 emoji）。见下 |
| **其他任意键** | 标量 / 数组 | 全部索引进 `props` 表 —— **这就是 database 的数据来源**（§2.6） |

链接解析策略：**链接在文件里写成人类可读形式 `[[线性代数]]`，`id` 只作为消歧和重命名恢复的依据。** 解析顺序为：按路径/标题匹配 → 失败则查 `links` 表记录的历史 `target_id` → 找到就自动修复链接文本。既保持文件好读，又不会因重命名丢链接，且重命名是 O(1) 而非全库改写。

**这条策略的后半段至今没实现**：`resolve_links` 只按标题和路径匹配，`rename_note` 也不改别处的链接文本。等它真做出来时，只有自己写了 `id` 的笔记能享受到 —— 这是「不自动写 id」明码标价的代价。索引内部仍然需要一个主键，没有 `id` 的笔记用 `path:相对路径` 顶上（`Index::note_record`）。

编辑器里 frontmatter **默认折叠成一行属性条**，不占视觉空间；点开是可编辑的属性表单，不需要手写 YAML。

**没有 frontmatter 也要留一个低调的「添加属性」入口**（v0.7.4）。不能让已有属性
的人可以继续加，而真正从零开始的人反倒只能切源码、套模板或去 database 绕一圈。
空白笔记不摆一张空表单，只显示一行淡文字；点击后直接聚焦属性名，回车即创建
第一条属性。

**文档图标**（`icon`，v0.6.13）

思源、Notion 都有的东西，作用是**认得快**：一竖排文字里，一个图标比读三个字快得多。
显示在文档树、标签栏、快速切换器和面包屑上，入口是面包屑最前面那个图标位和树的右键菜单。

三个决定：

| 决定 | 为什么 |
|---|---|
| 存 frontmatter 的 `icon`，不存 `.verso/` | §0 第 1 条：图标是用户对自己文档的标注，得跟着文件走。存进 `.verso/` 的话换台机器、拖进 Obsidian 就没了，而第 6 条要求那个目录能整个删掉重建 |
| 值是**一个 emoji 字符**，不是自定义图标名 | `icon: book-open` 只有 Verso 认得，记事本里打开是个查不到含义的字符串。emoji 是 Unicode，哪儿都显示得出来。代价是渲染归系统字体管，同一个图标在 Windows 和 macOS 上长得不一样 —— 图标的用处是分辨，不是精确的视觉设计 |
| 候选是一张**手挑的表**（`src/core/emoji.ts`），不是全量 emoji | 全量三千多个，绝大多数（食物、国旗、肤色变体）会把真正有用的那几十个淹掉。搜不到的可以直接粘一个字符进去 |

写入走 `prop_set` —— 和属性条、database 视图**同一条路径**，不给它开第二条写文件的路。
读取走索引（和排序键一样）：图标要显示在树的每一行上，逐篇读 frontmatter 等于刷新一次树
就把整个 vault 读一遍。

**没设图标的行也占那个位置**，画一个很淡的默认图标 —— 只给设过的行加图标会让名字参差成
两列，而参差比多一个淡图标显眼得多。留着位置，设过的那几行才真的一眼跳出来（§6.2：颜色只做重音）。

### 2.4 Markdown 方言

坚持 CommonMark + GFM 为基础，只加这几个扩展（都与 Obsidian 兼容，便于双向迁移）：

| 语法 | 含义 |
|---|---|
| `$...$` / `$$...$$` | 行内 / 块级公式 |
| `[[笔记名]]`、`[[笔记名\|显示文本]]`、`[[笔记名#标题]]` | 内部链接 |
| `![[文件名]]` | 嵌入（图片、其他笔记、笔记片段） |
| `#标签`、`#嵌套/标签` | 正文标签 |
| `==高亮==` | 高亮 |
| `> [!note] 标题` | callout。渲染成带图标、左侧色条和淡底色的块；类型可以多（与 Obsidian 一致的别名），但**颜色只归六个色族** —— §6.2 说多彩配色是业余感的最大来源。不认识的类型退回「说明」而不是当成普通引用 |
| `^块id`（行尾，**按需生成**） | 块引用锚点 —— 只在真的引用某个块时才写入 |
| ` ```verso-view ` | database 视图定义（§2.6） |

**明确不加**：自创的表格语法、自创的组件语法、自创的样式语法。每加一条自创语法，就多欠一份可移植性的债。

### 2.5 SQLite 索引 schema

```sql
-- 全部为派生数据，可从 .md 文件完整重建
CREATE TABLE notes (
  id            TEXT PRIMARY KEY,
  path          TEXT NOT NULL UNIQUE,   -- vault 相对路径，正斜杠
  parent_id     TEXT,                   -- 同名文件夹推导出的父文档，NULL = 顶层
  title         TEXT NOT NULL,
  created       TEXT,
  updated       TEXT,
  mtime_ms      INTEGER NOT NULL,       -- 快速判断是否需要重解析
  size          INTEGER NOT NULL,
  content_hash  TEXT NOT NULL           -- blake3，mtime 不可信时的兜底
);
CREATE INDEX idx_notes_parent ON notes(parent_id);   -- 文档树靠这个

CREATE TABLE links (
  src_id      TEXT NOT NULL REFERENCES notes(id) ON DELETE CASCADE,
  target_text TEXT NOT NULL,            -- [[]] 里的原始文本
  target_id   TEXT,                     -- 解析成功的 note id；NULL = 悬空链接
  block_id    TEXT,                     -- 块引用时的 ^id
  kind        TEXT NOT NULL,            -- 'wiki' | 'embed' | 'markdown'
  line        INTEGER NOT NULL
);
CREATE INDEX idx_links_target ON links(target_id);   -- 反向链接面板

CREATE TABLE tags (
  note_id TEXT NOT NULL REFERENCES notes(id) ON DELETE CASCADE,
  tag     TEXT NOT NULL
);
CREATE INDEX idx_tags_tag ON tags(tag);

-- frontmatter 展开成键值对 —— database 视图的查询基础
CREATE TABLE props (
  note_id TEXT NOT NULL REFERENCES notes(id) ON DELETE CASCADE,
  key     TEXT NOT NULL,
  value   TEXT NOT NULL,
  type    TEXT NOT NULL   -- 'string' | 'number' | 'bool' | 'date' | 'list-item'
);
CREATE INDEX idx_props_key ON props(key, value);

CREATE VIRTUAL TABLE notes_fts USING fts5(title, body, content='', tokenize='trigram');
```

**关于中文分词**：FTS5 内置的 `unicode61` / `porter` 不切分中文，整段中文会被当成一个 token，搜索基本失效 —— 这是很多人做到一半才发现的坑。`trigram` tokenizer 做三字符切分，天然支持中文子串检索且无需词典，是不引入外部分词器时的正确选择。代价是索引体积约为原文 3 倍、不支持前缀查询。万级笔记完全可接受；真到需要词典分词的量级再换 `jieba-rs` + 自定义 tokenizer。

**M3 实测补充 —— trigram 有个对中文很致命的硬限制：查询短于 3 个字符时永远匹配不到。** 而「矩阵」「线性」「函数」这些两字词恰恰是中文里最常见的搜索词。所以 `Index::search` 分两条路：

| 查询长度 | 路径 | 说明 |
|---|---|---|
| ≥ 3 字符 | FTS5 `MATCH` + `bm25` 排序 | 走索引，快 |
| < 3 字符 | `LIKE '%q%'` 全表扫描 | 慢一个数量级，但万级笔记的正文总量也就几 MB，实测仍在 50ms 验收线内 |

LIKE 路径没有 `snippet()`，自己截取上下文并补上同样的 `<mark>` 标记，前端只需处理一种格式。用户输入里的 `%` `_` 必须转义，否则 `%` 会匹配一切。

**另一处与本节原文的偏离**：schema 里写的是 `content=''`（contentless 表）。实现时改成了普通 FTS5 表 —— contentless 不支持 `snippet()`（搜索结果需要命中上下文），删除也得改用 `INSERT INTO t(t, rowid, …) VALUES('delete', …)` 的特殊语法。让 FTS 自己存一份正文，多占的空间相对 trigram 索引本身微不足道，而索引整体本就是可随时重建的派生数据。

### 2.6 database 视图 —— 核心功能，非附加功能

不引入新的存储形态，全部建立在 frontmatter 之上。

**数据来源**：`props` 表（即各笔记的 frontmatter 键值对）
**视图定义**：写成笔记里的一个代码块

~~~markdown
```verso-view
from: "论文/**"            # 路径 glob，也可用 tag: / linked-from:
where: status != "已读" and 难度 >= 3
sort: created desc
view: table                # table | board | calendar | gallery | list
columns: [title, 作者, status, 难度, created]
group-by: status           # board 视图必需
```
~~~

路径来源有两种最常用的层级语义：`目录/*` **只取该目录的直属文档**，
`目录/**` **递归包含全部后代**。SQLite 原生 `GLOB` 的 `*` 也会匹配 `/`，实现不能
直接把它透传给数据库，否则两种写法会得到几乎相同的结果。设置面板要把这两种
写法显示成「当前层 / 包含子层」的层级选择；原始 `from:` 输入仍保留，供用户写
更自由的 glob。

筛选面板用「等于 / 不等于 / 大于 / 大于等于 / 小于 / 小于等于 / 包含」这些
人能直接读懂的名称，写回文件时仍保持 `= != > >= < <= contains`。多个条件之间
固定是**并且**（全部满足），界面必须明确显示这一点；复杂查询不要偷偷扩展语法，
需要时拆成多个视图，手写且无法安全解析的表达式保持原样。

#### 时间筛选：内置列 + 相对时间（v0.7.37）

**`created` / `updated` 也能筛。** 它们是 `notes` 的列，不在 `props` 表里（上面
那条把它们排除在用户属性之外），所以查询要单独接一支 —— 不接的话
`where updated < …` 会返回空表，但查询本身不会报错，也不会提示原因。
多数笔记没有手写时间戳，缺了要**回落到文件的 mtime**，和文档树排序同一条规矩
（`Index::sort_keys`）；不回落的话一条时间筛选会把整个 vault 判成空。

**值可以写成相对时间**：`updated < "90d ago"`，单位 `d`/`w`/`m`/`y`。

- **为什么必须有**：写死日期的筛选下个月就过期。「长期未更新」那张清单如果每月
  都要手动修改日期，就无法长期维护 —— 内容长期未更新、逐渐被遗忘，才是知识库
  失效的主要原因，而不是分类不够细致
- **写进文件的是 ASCII**，界面上才显示成「90 天前」。中文一旦进了 `.md`，
  这篇笔记就不能拖进别的编辑器用了（§0 第 1 条）
- **时间比较统一按天**。索引里的时间戳带时区偏移，整串按字符串比会在跨时区时
  出错；而「三个月未更新」关注的是日期，而不是具体时刻
- **无法识别的值按字面值处理** —— 用户可能确实要查找 `updated = "2026-05-01"`
- 面板中明确显示「读作 90 天前，每次打开都按当天重新计算」。否则用户会误以为
  它与固定日期筛选等价

#### 光标停在块的边界上不算「进去了」（v0.7.38）

视图块和公式一样：光标进去露源码，移开就渲染。但「碰到就算」（`from <= 光标
<= to`）意味着**在它上下两行点一下、或者从上一行按一次方向键**，整块视图就
变回一段 YAML —— 而那两个位置恰恰是最常落到的地方，表格上下总要有正文。

改成必须**严格落在里面**。边界不算之后方向键再也进不去（整块是 atomic
range，光标一步跨过去），所以工具条上必须有一个**「视图源码」按钮** —— 那是唯一
进得去的路，每一种视图的工具条都要有，少一个那种视图就再也改不回源码了。

#### `/` 菜单里的「知识库」骨架（v0.7.37）

一次插入三个视图，因为它们分别解决不同的问题，共同构成完整的知识库总览：

1. **分类总览** —— 按 `分类` 分列的看板。分列的依据是**分类而不是状态**：积累类的
   笔记没有进度，用 `status` 分列的话每一列都不动，看板将退化为占用空间较大的
   分组列表。按分类分列，才能查看各类别的内容分布以及空缺类别
2. **最近更新** —— 表格，按更新时间倒序
3. **长期未更新** —— `updated < "90d ago"`，按更新时间正序。上面的时间筛选能力
   正是为这个视图提供支持

同时将 `分类` 声明为**单选**：如果保留为自由文本，半年后可能同时出现「HPC」「hpc」
和「高性能」等不同写法，后续检索会变得困难。

**知识库不提供独立的专用界面。** 它是一篇普通 Markdown 文档，其中包含三个视图
代码块。项目总览（§2.10）之所以值得做成专门视图，是因为项目**会结束、有进度、
有阻碍**；知识领域没有明确终点，也不适合套用同一套进度字段。重复使用相同结构，
只会增加用户的学习成本，并使两份数据逐渐产生差异。

**必须可写** —— 这是它好不好用的分水岭：在表格里改一个单元格 → 直接改对应笔记的 frontmatter → 文件落盘。看板里拖动卡片 → 改 `status` 字段。加一行 → 新建一篇笔记并写入对应属性。

**拖是快捷方式，不是唯一的路**（v0.7.29，§1.2 铁律「不能假设有鼠标」）：HTML5
拖放在触摸屏上根本不产生 `dragstart`，所以「拖卡片改列」「拖条目改日期」在
手机上等于不存在，而那恰恰是这两个视图最该好用的场合。每一处拖动都要有一个
点得到的等价入口：看板卡片的 `⋯` 里选一列，日历条目旁边的日期按钮。

**加一行不弹窗问名字**（v0.6.4，和 §4.1 的新建文档同一条规矩）：直接建一篇
「未命名」，那一行的标题格立刻变成输入框就地改。弹窗那一步会逼人在什么都
还没有的时候先想好名字，而在一张表里加一行时，名字往往是填完别的格子才定
得下来的；取消弹窗还会让人以为「没建成」。**建完不跳走** —— 在表里加一行
的人正在整理这张表，把他弹进那篇笔记会打断这件事。

**几个键在界面上显示成中文**（v0.5.34）：`created` → 创建时间、`updated` →
更新时间、`tags` → 标签、`title` → 标题。界线很清楚：

- 只给**我们自己定义的**（`created`/`updated` 是内置列，压根不在 frontmatter 里）
  和**跨软件约定俗成的**（`tags`/`title`）键起中文名
- `status`、`作者`、`难度` 这些是用户自己起的名字，**一个字都不能动**
- **写进文件的永远是原键名** —— `columns: [created]`、frontmatter 里的 `tags:`
  一律保持英文。中文名一旦进了 `.md`，笔记就不能拖进 Obsidian 用了（§0 第 1 条）
- **属性条上不做这个映射**：那里点键名就是重命名，显示的必须是真名

**属性类型**：从 `props.type` 推断，UI 按类型渲染编辑控件（文本框 / 数字 / 日期选择器 / 单选 / 多选标签）。类型定义存在 `.verso/config.json` 里作为 UI 提示，但**不作为真源** —— 删掉它只是退化成文本输入，不丢数据。

**五种视图，分两类**（v0.5.24 补齐后三种）：

| | 视图 | 能改什么 |
|---|---|---|
| 可写 | `table` | 单元格就地编辑、加列、改类型、全库重命名属性 |
| 可写 | `board` | 拖卡片 = 改 `group-by` 那个属性 |
| 只读 | `list` | —— |
| 只读 | `gallery` | —— |
| 半可写 | `calendar` | 拖到另一天 = 改 `date-field` 那个属性 |

**点列头开的是那一列的菜单，不是直接排序**（v0.5.37）。点一下就改文件的设计，
误触的代价是一次真实的改动；而且排序之外的操作（重命名、改类型、隐藏、宽度
复位）会被迫全塞进一个 `⋮` 图标里，那个图标既小又没人认得。菜单里升序/降序
排在最前 —— 它仍然是最高频的那一项，只是多按一下。思源、Notion 都是这个路子。

**设置浮层不能越过正文边界**（v0.7.3）。宽表时仍靠设置按钮右边对齐；空表或
窄表的顶栏比浮层窄时，浮层改为贴 database 左边，不能继续向左伸进 CodeMirror
的裁剪区。窄屏下宽度还要收进视口 —— 标签、输入框和操作按钮少掉半截，比整个
面板都不出现更难判断是布局问题。

**列宽可以拖**（v0.5.36），宽度写成代码块里的一行 `widths: title=240, status=90`
—— 和排序、列的显隐一样是**用户内容不是界面状态**（§0 第 1 条）：跟着 `.md` 走，
换台机器还在。`.verso/` 放不了，那里只放推断得出来的派生数据。

两处不显然的：**按下那一刻要把所有列的当前宽度都量下来**（一旦有了显式宽度，
表格切到定宽布局，没量过的列会立刻重排，整张表在手底下跳一下）；**松手才写回
代码块**（每动一像素写一次文件的话，撤销历史会被冲掉）。双击那条线复位，
和侧栏拖杆一个手势。

列表视图里**属性摆在标题右边、同一行**（Notion 的列表就是这个样子）。摆在
标题下面的话一条占两行，而列表存在的理由恰恰是密度。窄栏里属性最多占 55%，
放不下就整体截断 —— 一行里真正要读的是标题。

只读的三种**有意不做就地编辑**：它们的信息密度低，是用来浏览的；要改值切回表格。为一个 148px 宽的画廊瓦片实现一整套编辑控件，换来的是每种视图都要维护一遍编辑态。

各自还需要一个键才能工作，**这个键在切换视图类型的地方就要问**（以前只能手写进代码块，选了「看板」却什么都没变）。看板那个键还要更进一步（v0.7.29）：切到看板时**自动补上一个分组属性**（挑第一个离散值的：数字和日期基本每篇一个值，分出来一列一篇），真的没有属性可挑时**视图里直接说缺什么并列出候选** —— 缺键时默默退回一张表格是最坏的一种，界面上没有任何地方说得出为什么，人只会认为这个功能坏了。三个键的取值规则：`board` 要 `group-by`、`calendar` 要 `date-field`（不写就用文件的创建时间）、`gallery` 要 `cover`（值是图片路径，找不到就退回占位块，见 §4.4）。这三个键**必须跟着一起取值**，哪怕不在 `columns:` 里 —— 漏了的表现是「视图空的」，而查询本身还成功着。

日历只摆得下标题：一格是正文栏的七分之一宽，再塞属性就成了马赛克。认不出日期的笔记单独列在月历下面，不是丢掉不显示 —— 否则人会在月历里翻找一篇根本不会出现的笔记。

**窄屏（≤640px）下日历换成议程**（v0.7.29）：七列的月历在 390px 上一格只有
50px 宽，标题剩四个字、条目高 18px，读不了也点不准。按天竖排、一天一段、
一条一行，只列有条目的日子（空日子在竖排列表里全是废行）。手机上的日历应用
一律是这个样子，不是把月历缩小。月份导航和「没有日期」那一栏两边都保留。

这样拿到了 Notion database 的绝大部分实用价值（论文清单、项目跟踪、读书记录、课程笔记），而底层依然只是一堆 `.md` 文件。

**不做块级属性查询**。块引用（`^blockid`）只支持「引用/嵌入某个段落」，按需生成 ID，文件保持干净。「查询所有含属性 X 的块」不做 —— 它要求给每个块都预分配 ID 和属性，那就必须放弃纯 Markdown（思源正是为此改用 `.sy` JSON 块树，代价是它的笔记文件用记事本打开是一堆 JSON）。

### 2.10 项目日志 —— 「一篇笔记记一个项目」怎么才不塌

拿一篇笔记跟一个项目，写着写着就没法看了：**最新的状态被埋在最下面**，
而上面一大半是已经翻篇的过程。

GitHub Issues 之所以顶得住，靠的是三件事：一件事一个条目、能关闭、
以及**追加式的评论流**（新的往上冒，旧的自然沉下去）。前两件 Verso 已经有了
——一篇笔记 + frontmatter 的 `status` + database 视图筛选——缺的是第三件。

**一条进展 = 一个标题文字以日期开头的普通标题**（`## 2026-08-01 14:30`）。
不自创「日志块」语法：它是普通标题，所以天然能折叠、进大纲、进思维导图、
被别的编辑器认得；自创一种就要把这些全重做一遍（§0 第 3 条）。

两个动作：

- **记一条进展**（`Mod+Shift+Enter`，或 `/` 菜单）：在**已有记录的最前面**插一节，
  光标落在标题下面。打开笔记第一眼就是最新状态，不用滚到底。一条记录都没有时
  排在正文末尾——那时上面是项目描述，记录应当排在它后面
- **只看最新**：打开笔记时把除最近 N 条之外的记录**折叠起来**（设置里可调，
  0 = 关掉）。**文件一个字节都不改**，改的只是打开时的默认视图

「最近 N 条」按时间戳算而不是按位置：默认新的加在上面，但用户完全可能自己
整理成正序，那时按位置取会把最旧的几条留着展开。

**更细的粒度用子文档。** 一件事一篇（`项目 X/某个问题.md`），项目笔记里嵌一张
只列未关闭条目的表（`/` 菜单里的「未关闭的条目」会照当前笔记的路径填好
`from:`）。`Mod+Shift+N` 在当前笔记下面新建子文档。这条路上没有任何新机制 ——
它就是 §2.1 的父子文档 + §2.6 的 database 视图，日志则用在每个条目内部。

#### 项目中心与单项目总览（v0.7.17）

项目记录不能退化成一条越来越长的时间线。**项目主页只回答「现在怎样」**，完整
过程放到同名目录里的分类笔记；主页因此始终能在一屏内读完，半年后的项目也不会
比第一周更难打开。

项目有两层界面，不能混成一个页面：

- **项目中心**是仓库级入口，汇总所有 `type: project` 的笔记。它显示项目总数、
  仍在推进和已经结束的数量，并可按状态或关键词筛选；点项目卡片后进入该项目。
- **项目总览**只回答单个项目「现在怎样」。打开项目笔记时默认进入这一层，正文
  仍是普通 Markdown，可由右上角源码按钮进入。

项目中心在左侧图标栏有常驻入口，即使当前没有打开笔记也能进入；默认快捷键为
`Mod+Alt+J`，与单项目总览的 `Mod+Shift+J` 成对。新建项目会创建
一篇普通未命名笔记并加上 `type: project`，随后仍按文档树的就地改名规则命名；
当前打开的是普通笔记时，中心同时提供「将当前笔记设为项目」，保留正文并只补项目
属性。这两条路径不能合并：前者从空白开始，后者把已有内容纳入项目管理。
项目卡片只呈现状态、摘要、下一步和阻碍，不在中心里复制完整内容。这个名称专门
留给跨项目页面，不能把单个项目的页面也叫「项目中心」。

一篇笔记加上 `type: project` 后默认显示「项目总览」。总览显示：

- 当前状态与一句当前结论
- 接下来最多三件事、当前阻碍
- 最近三条进展，以及仍在进行的记录（「决策」「资料」是归档型，不算推进中）

详细记录仍是普通 Markdown，按下面的层级落盘：

```text
项目.md
项目/
  进展.md
  实验.md
  实验/某次实验.md
  问题.md
  问题/某个问题.md
  决策.md
  决策/某项决定.md
  资料.md
  资料/某份资料.md
```

`进展.md` 使用上面的日期标题追加规则；其余每类一条一篇，用 frontmatter 的
`type`、`status`、`summary` 供项目总览汇总。分类首页只是普通父文档，不引入隐藏
数据库。用户可以离开总览，直接在文档树中打开、移动和编辑这些文件。

##### 分类由项目自己决定（v0.7.27）

实验 / 问题 / 决策 / 资料只是**默认**分类，不是固定的四格。做工程的项目也许要
「复现」和「上线」，读文献的项目根本用不上「实验」—— 一套写死的分类只能让人把
内容硬塞进最不别扭的那一格，而分类本来就该顺着这个项目的做事方式。

**分类表的唯一真源是项目笔记 frontmatter 里的 `sections`**（一个字符串列表，
顺序就是总览里的顺序）。它必须写在笔记里而不是配置或 `.verso/`：这份归类是
用户内容的一部分，换台机器打开、或者拿别的编辑器看，都得跟着走（§0 第 1 条）。

- 没有 `sections` 键 = 还没动过分类，用默认四类。**旧项目因此不需要迁移**
- 写成空表 = 用户明确一个分类都不要。这跟「没写过」必须分得开，否则删光之后
  下一次打开又冒出四类

**一篇记录属于哪个分类，先看它在哪个分类目录下**，`type` 只作为退路 ——
用户可以在文档树里直接搬动这些文件，路径才是那个跟得上手的事实。自定义分类的
记录把分类名写进 `type`（内置四类仍用英文 slug，不破坏已有的 database 视图）。

**删掉一个分类不删任何文件。** 那一类连同它的文档只是不再出现在总览里，
`项目/资料/` 原样留在文档树；把分类名加回来，记录就都回来了。总览是一层视图，
不该成为删用户数据的入口 —— 真要删，文档树里有正常的删除操作。

**排序走指针事件的拖拽，不走 HTML5 拖放。** 后者在触摸屏上根本不派发，
而分类管理在手机上同样要能用（文档树为此另做了一条免拖拽的路，§2.7）；
`pointerdown/move/up` 一套代码同时覆盖鼠标和手指。三条要点：

- **落点先画出来再提交**：拖动中只显示一条插入指示线，松手才写文件。跟着指针
  实时重排看着更炫，但卡片高矮不一，重排会把指针底下的卡片换掉，于是插入点
  在两个位置之间反复横跳
- **前后关系按当前列数判断**：两列时看左右，窄屏单列时看上下。轴判错的话插入点
  会跟着另外半边屏幕跑，而这在截图里完全看不出来 —— 只有量得到
- **手柄要 `touch-action: none`**，否则手机上按住拖只会把页面滚走

拖拽不能是唯一的路：手柄本身是个可聚焦按钮，上下方向键同样能挪一格（§9 那条
「交互不能假设有键盘」反过来也成立 —— 也不能假设有精细指针）。

内置四类的模板与状态词表按名字命中（叫「实验」就有实验那份骨架）；自定义分类
没有建议结构，状态先给「进行中 / 已完成 / 已归档」，其余仍可现场新增。
**「进展」不能当分类名** —— 那是日志，不是一类记录。

「记录」分成两条明确不同的路径。**进展是快速记录**：只问刚发生了什么，直接
插入 `进展.md`，保存后留在总览。**其余每一类都是完整项目文档**：
弹窗只问标题与可选的一句摘要，随后创建分类首页和独立子文档，并立即进入正常
编辑器。不能让用户在小 textarea 里写一份可能包含公式、图片、代码、表格和引用的
实验记录；小窗只负责创建，完整编辑器负责内容。摘要只写入 frontmatter 的 `summary`，
供项目中心和总览使用，**不能再复制成正文第一段**；否则同一句话会在属性和正文出现
两次，而用户删除正文里的副本后还会误以为摘要也被删了。

新文档**默认空白**。内置四类可选一份轻量 Markdown 骨架：实验提示目标与假设、方法与
设置、观察与结果、结论与下一步；问题提示问题、背景与事实、已尝试、当前判断、
下一步；决策提示备选方案、证据与权衡、决定、影响与复查；资料提示来源、关键内容、
与项目的关系、后续使用。模板必须由用户主动勾选，不因类型而强制；每个人的记录习惯
不同，分类只负责归档，不负责规定写法。项目子文档里触发「项目总览」时回到最近的
项目祖先，因此写完详情不必去文档树里重新寻找。

打开 `type: project` 的项目文档时默认进入总览，而不是先展示空白或说明用的 Markdown
正文；正文仍可由总览右上角的源码按钮进入。总览除了当前事项与最近进展，还要给
每一个分类稳定的入口。`status` 是可编辑的单选属性：内置常见状态，
也允许现场新增选项；新增值进入 vault 的属性 schema，之后在别的项目条目中可复用。
创建或打开项目及其条目时要主动把 `status` schema 补成 `select`；普通笔记属性条也必须读取
这份 schema 并显示选项菜单，不能只有项目总览是单选、打开同一篇 Markdown 后却变回
任意文本框。
状态菜单始终按类型的固定顺序排列，当前值只用勾选标明，不能因为选择变化而重排。
每条记录在总览里仍应像一篇笔记：整条标题区域负责打开文档，状态是独立的辅助属性；
不再额外放一个含义重复的进入箭头。

信息的默认展开必须有硬上限：每类最多露三条，旧记录和已完成条目先折起，用户
主动点「查看全部」才展开。这个上限是产品规则，不随记录数量增长；否则总览
最终仍会变成换了皮的长文档。

##### 置顶与换分类（v0.7.35）

**「最重要」和「最近」是两回事。** 排序默认按最近改动，配上每类只露三条的硬
上限，最要紧的那条只要几天没动就沉到「查看全部」后面去了。所以要有置顶。

- **置顶写在那条记录自己的 frontmatter 里**（`pinned: true`，声明成 checkbox
  所以落成真布尔）。不记在项目笔记的一张路径清单里 —— 那份清单在改名、移动
  之后都要跟着维护，早晚对不上；写在记录自己身上，搬去别的分类、换台机器、
  用别的编辑器打开都还在（§0 第 1 条）
- **排序键是（置顶，最近改动）**，「正在推进」和分类卡片共用同一份规则
- **三条的上限不动。** 置顶只是排到前面，不是额外多露一条 —— 否则钉五条就把
  上限废掉了，总览又变回长文档

##### 状态只有三档（v0.7.40）

以前每一类各有一套说法：问题是待解决/研究中/已解决/已搁置，决策是待决定/
已决定/已废弃，资料是待整理/已收录/已归档……十几个词分散在四张表里。代价是
你得记住在哪一类里「完成」叫什么，而跨分类问一句「还剩多少没做完」根本说不
清楚。

**统一成三档：未开始 / 进行中 / 已完成**，所有分类共用一套。它正好是四档语义
色里的三档主色，另两档（卡住了、已归档）留给现场新增的词 —— 词表本来就是
用户的，要更细的说法自己加。

- **老 vault 的旧词一次性收回。** 那十几个词是 Verso 播的种、不是用户打的字，
  所以由 Verso 收回；用户自己加过的一个都不动。判据是「还留着旧词，而且三档
  还没进来」—— 迁移一跑，`未开始` 就成了「这件事做过了」的标记，用户事后再把
  某个旧词加回来也不会被二次清掉
- **已经写着旧词的笔记一个字不改。** 那一条照常显示、照常上色，只是菜单里
  不再列它（和删状态同一条规矩）
- **新建的记录一律「未开始」**，不再按分类给不同默认值
- **「正在推进」从此只看状态，不看分类。** 以前「决策」「资料」整类被排除在
  外，理由是它们的默认状态本来就不是推进态；三档统一之后这条不成立了 ——
  一条还在定的决策就是正在定，那正是要推进的事

**排序是「置顶 → 还没结束的 → 创建时间」，不是按最近改动**（v0.7.38 修正）。
改一个状态就是改那篇笔记的 frontmatter，文件的修改时间跟着变 —— 于是「把某条
标成已解决」的结果是它**跳到最上面**，而那正是你刚处理完、最不需要再看见的
一条。按修改时间排等于让「碰过它」压过「它要紧吗」。

创建时间取自笔记的 **ULID**：前 10 个字符就是毫秒时间戳，字典序即时间序。
§2.3 起 Verso 不往 frontmatter 里写 `created`，而文件系统的创建时间在同步之后
并不可靠 —— ULID 是这里唯一一个不会因为改了一个字就变的时间。

**换分类 = 真的移动文件。** 分类就是目录，把一条决策改归到「问题」就是把
`项目/决策/某项决定.md` 搬到 `项目/问题/` 下面。两件事必须一起办：

- 目标分类首页可能还不存在（分类在 `sections` 里声明，没建过记录的分类在磁盘
  上没有目录），要先建出来
- **`type` 要跟着分类走。** 不改的话，移到「问题」底下的那篇仍写着
  `type: decision`，database 视图里 `where type = "decision"` 还会把它捞出来

**入口是菜单里的二级菜单，不是拖拽。** HTML5 拖放在触摸屏上根本不派发（§2.10
上面那条已经为此把分类排序改成了 pointer 事件），跨卡片拖拽要自己实现落点判定、
指示线和自动滚动，而文档树里已经有一套能拖的移动了。菜单里列出本项目的分类
（当前这一类不在候选里 —— 移到自己那一类是一次什么都不会发生的操作），鼠标
和手指同一条路。真需要手感时再在桌面上补拖拽当加速通道，那是第二步。

##### 状态要分得开，改名不必去文档树里找（v0.7.34）

**状态分四档上色。** 全部用主题色淡底的话，「已解决」和「待解决」长得一模一样
—— 状态是这一屏里唯一需要**扫**而不是**读**的东西，同色等于把它退回成纯文字。
四档是：还没开始（中性）、正在推进（主题色）、卡住了（琥珀）、有结论了（绿）；
归档与废弃再退一档，连底都不要，只留一圈发丝线 —— 它已经离场了。

- **语义色相写死**，不跟着主题色走（和 `--warn`、`--danger`、代码高亮同一个
  做法）。「完成」的含义不能因为用户把主题调成橙色就变成另一种意思
- **主题色只留给「正在推进」那一档。** §6.2 说「一块青绿不该被一个状态占用」，
  它真正反对的是**平均分给五个状态**；这一屏里唯一还在发生的事值得那点重音
- **按词形归档，不查固定表。** `status` 可以现场新增，「复现中」要落到推进档、
  「待复核」落到未开始档、「等外部数据」落到卡住档。查表的话自定义状态会全部
  退成同一个中性块，而那正是这次要修的毛病。认不出来的一律当「还没开始」
- **新状态不会分到一个新颜色。** 一共就这四档（加归档那一档的无底样式）。
  五个固定含义能扫，十几个各不相同的颜色只能挨个读 —— 那时颜色又变回了装饰
- **新增状态时当场说清楚它落到哪一档**：输入框下面一行「算作卡住了」加一个
  该档的圆点，边打边变。分档是按词形猜的，猜错了必须在按下「添加」之前就看得见，
  而不是建完之后自己去数颜色

**状态可以删。** 菜单里既然有「添加状态…」，就必须有对应的删除，否则试错一次
就永久多一个词。三条规则：

- **删的是词表，不是数据。** 还写着这个词的记录一个字都不改，它在自己那一行里
  照常显示、也照常上色；只是菜单里不再列出来。和「删掉一个分类不删任何文档」
  同一条 —— 总览是一层视图，不该成为改用户文件的入口
- **内置词表只在第一次播种。** 播过种之后就以 vault 里那份为准，否则删掉的状态
  下次打开又回来，而用户完全不知道是谁加的。「播过种」的判据是词表里还剩至少
  一个内置状态（`PropDef` 只有类型和选项，没有多余的位置放标记）；五个全删光会
  被当成没播过种、下次重新给一份 —— 那时用户手上一个状态都没有，给回默认是对的
- **正在用的那个不给删。** 删了它还得留在菜单里（这条记录仍写着它），看着像没
  删掉。先换成别的，再回来删

**每条记录在总览里就能改名。** 此前唯一的改名入口是文档树里的就地改名 ——
人正看着这条记录，却要先去侧栏把它找出来。总览里右键（手机上长按）弹出菜单：
在新标签页打开、重命名；改名框就长在原来那一行上，回车提交、Esc 放弃。

- **改名要认「哪一行」，不能只认路径。** 同一条记录同时出现在「正在推进」和它
  自己的分类卡片里，只按路径匹配会同时长出两个输入框，两个都 autofocus，后出现
  的把先出现的挤掉、先出现的失焦即提交 —— 表现是改名框一闪就没
- **先改界面再等落盘。** 后端要改文件名、重建索引、把文档树推回来，中间那几十
  毫秒让刚改完的行还顶着旧名字，看着像没生效
- 改名本身仍走文档树那一条（`note_rename`），标签页路径、手排顺序都跟着修

### 2.7 文件监听与写入策略

外部修改（git pull、其他编辑器、iCloud/OneDrive 同步）必须被正确处理。**移动端加入后这一节的重要性显著上升** —— 同一份 vault 会被多设备并发修改。

- Rust 侧 `notify` 监听 vault，**去抖 300ms**（同步盘会产生事件风暴）
- 收到事件 → 比对 `mtime_ms` 和 `content_hash` → 变了才重新解析
- 若该文件正在编辑器中打开：
  - 本地无未保存改动 → 静默重载
  - 有未保存改动 → 顶部条提示「文件已被外部修改」，提供 *保留我的* / *加载外部* / *对比*
- **自己的写入要打标记**：写文件前把路径塞进 `pending_writes: HashSet<PathBuf>`，监听到自己造成的事件就丢弃，避免回环
- **移动端补充**（M6 落地）：App 从后台恢复（`visibilitychange` 转 visible）时主动全量刷新 —— 重建索引、刷新文档树、比对打开笔记的 mtime。iOS 后台期间收不到文件事件，安卓共享存储那层 FUSE 的 inotify 本来就不可靠。桌面不走这条：窗口聚焦那条路已覆盖同样场景

**保存策略**：停止输入 800ms 后自动保存 + 切换笔记/失焦/App 进入后台时立即保存。写入用 `write to .tmp → fsync → rename` 保证原子性，避免断电产生半个文件。

**关窗也算一个保存时机**：点 X 的那一刻，最后敲的几个字可能还在那 800ms 的
窗口里，而窗口一销毁前端连一个 tick 都没有。所以关窗要先拦下来（Rust 侧的
`CloseRequested`），让前端落盘、按 §2.8 记一个版本，再真的关。**必须有兜底**：
前端卡住时最多等 5 秒就强关 —— 一个关不掉的窗口比丢一次自动提交严重得多。

### 2.8 Git —— 同步 / 备份 / 版本历史 / 发布的统一底座

vault 本身就是一个 git 仓库。这一个机制同时解决四件事：

| | |
|---|---|
| **同步** | 全平台一致，不依赖任何厂商网盘 |
| **备份** | 远端仓库即备份 |
| **版本历史** | 每篇笔记完整修改史，可回滚到任意时刻 |
| **发布** | push 即触发 GitHub Pages 构建（§2.9） |

**为什么是 git 而不是 iCloud/OneDrive**：§1.2(c) 提到加了移动端后同步变成必须，而 iCloud Drive 只覆盖苹果生态，Android 和 Windows 都在外面。git 是唯一在 Tauri 支持的四个平台上都能跑的方案 —— Rust 核心直接编译 `libgit2`（`git2-rs`），iOS/Android 同样可用。**Obsidian 的 git 插件在移动端一直不好使，正是因为它是 JS 实现的；我们在 Rust 层做是天然优势。**

网盘同步仍然保留为可选项（vault 放进 OneDrive 目录即可，软件不需要知道），但不是主推路径 —— 网盘对大量小文本文件的同步既慢又容易产生冲突副本。

**对用户隐藏 git**：界面上只有一个「同步」按钮和一个状态点。底下执行 `pull --rebase` → 自动 commit → `push`。只有真冲突时才浮出 UI。不暴露 branch / rebase / stage 这些概念。

**M5 分两步走。第一步（v0.5.43）只做本地历史**：

- 状态栏一个点，显示「N 个改动」/「已记录」，点它立刻记一个版本
- **停手 5 分钟自动记一次**（设置里可调，0 = 关掉）。§2.8 那条「按时间窗聚合」
  在这里就是它：保存是停手 800ms 就发生的，每次保存都提交的话一小时能造出
  上百个提交，历史反而没法用
- **提交说明要说得出「哪一篇」**（v0.5.44）：三条以内直接列名字
  （`更新「线性代数」`），再多才报数目（`新增 4 篇`）。一屏 `git log` 全是
  「更新 1 个」等于没记 —— 人回头翻历史，找的正是哪一篇。正文再逐行列出
  完整路径，任何 git 工具里都读得到
- 命令面板里还有一条「记一个版本并写说明…」：自动说明只说得出**动了什么**，
  说不出**为什么**，而做完一件完整的事时后者价值大得多
- **没有改动时绝不产生空提交** —— 自动提交每 5 分钟醒一次，空提交会把历史
  冲成噪音
- 用户没配过 `user.name` 时回落到 `Verso <verso@localhost>`：那在一台没写过
  代码的机器上是常态，不能让「保存」这件事失败
- **侧栏里能翻历史，能把某一篇回退到某一版**（v0.5.45）：一条一条列出提交，
  展开看动了哪几篇，每篇后面一个「回退」。回退只写一篇、不动别的，且
  **覆盖前先把现状记一个版本**，所以「回退」本身也是可回退的
- **AI 改完能直接看差异**：历史侧栏顶部是「当前改动」，列出还没记进版本的
  文件；点当前文件看 `HEAD ↔ 工作区`，点历史里的文件看「这一版 ↔ 上一版」。
  对比是只读的，临时占用正文区，关掉就回到原笔记，不制造一个假的文档标签。
  版本列表用节点和纵线画成一条时间轴；只有一条主线，不为好看伪造不存在的分支。
  行本身保持紧凑；鼠标停住片刻再浮出完整说明、作者、相对与准确时间、文件数、
  增删行数和短版本号，信息多时在卡片里滚，不把侧栏永久撑宽。
  宽屏左右对照，长行在各自栏内自动换行；窄屏改成单列。只传 Git 已裁好的
  差异段，不为几行修改加载两份完整长文。附件等二进制文件只说明「内容有变化」，
  不假装能逐行比较。「当前改动」默认按内容高度展开，版本记录占下面剩余空间；
  两者之间的横线可以上下拖，双击复位，调好的高度留到下次打开。两个区域各自滚动，
  不能让改动一多就把版本记录整个挤出侧栏
- **当前改动可以逐文件撤销**：改过或删掉的文件恢复到最近一次记录；尚未记录的
  新文件则删除。它不像历史回退那样能先替当前内容留一版，所以必须明确确认，并把
  「新文件删除后无法从历史找回」说出来。撤销当前打开的笔记时，编辑器、标签和差异
  页必须一起跟上，不能被正常的离页自动保存重新写回来
- **也可以只撤销其中一处**（v0.7.32）：对比页上每一处改动，在左右两栏中间
  那条缝上浮出一个按钮，点它就把这一处改回上一个版本 —— VSCode 的差异编辑器
  就是这个位置和这个粒度。AI 一次改动里好的和不要的常常混在一起，而此前只有
  「整篇留下」和「整篇丢掉」两个选择：为了留下三行去手动重打另外三十行，人
  只会干脆全留着，于是历史里攒下一堆自己都不认的内容。
  四条边界：
  1. **按「处」不按行** —— 中间隔着上下文的两段是两件事，一个按钮管两段
     等于逼人把不想撤的那段一起撤掉
  2. **只在「当前改动」上出现**（历史里的某一版是已记下的事实，在那上面挑
     几处没有意义，要退整篇有「回退到这一版」）
  3. 左右对照里「改了一行」是**一对**（删掉的 + 加上的），点一下必须两条
     一起走，否则会留下半行
  4. **不弹确认框，改成先留后路** —— 审阅一次改动要点掉好几处，每处弹一个
     「确定吗」会把这个功能变成苦差事。照历史回退那条先例：**这一轮的第一次
     撤销之前先把现状记一个版本**，于是撤销本身也是可回退的。仓库还没有版本
     记录时才退回问一句
  组装那一半在 Rust 里（`compose_partial_revert`）：**保留每一行原来的行尾**，
  用 `lines()` 再 join 会把没动过的行的 CRLF 也统一掉，于是「撤销一处」在下一次
  对比里变成整篇都改了。撤销之后停在对比页上不弹回编辑器 —— 审阅往往是一处
  一处来的
- **仍然对用户隐藏 Git 操作模型**：这个入口叫「当前改动 / 版本记录」，不是
  Source Control；没有暂存区、分支、变基、提交框。这里的任务是回答「AI 改了
  什么」，不是在 Verso 里复制一套开发工具
- **自动提交的时机可调**（v0.5.45）：除了空闲 N 分钟，还有「切走时也记一次」
  的开关。合上电脑就走的人和整天开着窗口的人，需要的触发点根本不是同一个
- **而且能整个关掉**（v0.7.32）：一个总开关管住上面那三条，外加一条命令
  （`Ctrl+P` 里的「停用/启用自动记录版本」）。让 AI CLI 在仓库里干活的人需要它 ——
  改到一半被自动记一版，会把一次完整的改动切成几段，而分别关掉三个开关才能
  停掉这件事，等于把一个是非题拆成三个。关掉之后状态栏那个点的提示里要说明白
  「只有你点它才记」，否则「改了半天怎么一直没记」会被当成 bug
- **空闲指最后一次真实写入之后**：同一篇文件被外部 AI 连续改写时，改动文件数
  始终是 1，不能因此让计时器从第一次写入就一直跑。每次内部保存或文件监听事件都
  重新计时。正在查看差异时暂停自动记录，关闭差异后再计完整一段时间；手动、失焦、
  关窗和同步触发提交时，也只有编辑器确实存在未落盘内容才先写文件，不能拿内存里的
  旧正文覆盖外部 AI 刚写好的版本
- **关软件之前也记一次**（v0.5.46，默认开）：合上电脑就走的人，「停手 5 分钟」
  这一档对他等于不存在 —— 他停手的那一刻正是关窗的那一刻。三档触发点各自
  对应一种收工方式，所以是三个独立开关而不是一个下拉框

**第二步（M5b，v0.6.1 起）：远端。**

- 界面上只有三样东西：**仓库地址**、**令牌**和**提交署名**。没有 branch、
  没有 remote 名字、没有 push/pull 的开关 —— 那些概念对笔记软件的用户没有
  意义，而且各自都只有一个合理取值
- **提交署名在界面上就能配**（v0.6.32 起）：写进 vault 仓库级 `.git/config`，
  跟着 vault 走，**不动全局** —— 笔记软件没有资格改用户整台机器的 git 身份。
  留空时回落顺序：本机全局配置 → 「Verso」。不给这个入口的话，不用命令行的
  人的提交永远署名「Verso」，历史侧栏里多台机器的改动分不清是谁写的
- 一个「同步」按钮做完整条链：**提交本地改动 → 取远端 → 接到一起 → 推上去**
- **接的方式是变基不是合并**：自动提交每几分钟一个版本，两台机器各攒一串再
  合并的话，历史里会插满 `Merge branch 'main'`，而侧栏那个列表正是给人翻的。
  代价是重写本地那几个提交的 id —— 对 vault 没有影响，没人基于它们做开发
- **冲突：检测 → 交给人选 → 带着选择重放**（ConflictView）。撞上冲突仍然
  整个撤回、工作区一个字节不动，但会把每篇的本地/远端完整内容带回来，
  弹出逐段（hunk）对比：点哪边用哪边，也可整篇一键选边；一边删除的文件
  退化成整篇二选一。**所有段落都选完才能提交，不做默认选择** —— 默认就是
  自作主张合一半。提交走 `vault_sync_resolve` 重放整条链，变基撞上定稿
  覆盖的文件就用定稿落解；两次之间远端又动了就报新一轮，UI 原样再走。
  定稿的拼装是纯函数（`lib/conflictMerge.ts`）：基底永远是本地全文，
  选了远端的 hunk 按行号区间替换 —— hunk 之间的未变部分不在 patch 里，
  只有拿全文当底才不丢内容
- **令牌进系统钥匙串，不进设置文件**：设置是明文 JSON，会被备份、会被为了
  分享排版整个发出去，而一个仓库令牌等于那个仓库的写权限。前端只问「存过
  没有」，**有意没有「读令牌」这个命令** —— 传出去就等于印在 IPC 日志里
- **只支持 https + 令牌，没有 ssh**：git2 的 `ssh` 特性会链进 libssh2，而
  链进去之后的二进制在开了 Smart App Control 的 Windows 上直接跑不起来
  （`os error 4551`）。那是签名/信誉问题，在给安装包签名之前无解
- **安卓的 https 走自定义传输层**（`vault/transport.rs`）：安卓那份 libgit2
  没编 https 后端 —— 它要 OpenSSL，而在 Windows 主机上给安卓交叉编 vendored
  OpenSSL 是死结（Cargo.toml 里有全过程）。解法是给 libgit2 注册一个 smart
  传输层：HTTP 走 ureq、TLS 走 rustls + webpki 根证书，纯 Rust，NDK 直接编。
  认证由传输层自己放进 `Authorization` 头 —— libgit2 的凭据回调不会为自定义
  传输层工作

冲突 UI 留给第三步。这样切分是因为**本地历史自己就成立**：
用 AI 改完一整篇之后能一眼 diff、一键回退，那是这个软件最容易丢数据的路径
（§7.4），而它不需要任何远端。

暂存必须 `add_all` **加** `update_all` 两步：前者收新增和修改，后者才收得到
**删除**。只调 add_all 的话，删掉一篇笔记永远提交不上去 —— 而这种漏提交要
等到换台机器才发现。

**必须处理的现实问题**

| 问题 | 方案 |
|---|---|
| 自动 commit 产生噪音历史 | 按时间窗聚合：空闲 5 分钟 / App 进入后台 / 手动点同步时才提交。commit message 自动生成（`更新 3 篇笔记`） |
| Markdown 合并冲突 | 交给 git 的行级三方合并，多数情况自动解决。真冲突时给对比 UI，**粒度是段落而非整文件** |
| 附件撑大仓库历史 | 单文件 > 5MB 时警告；提供 git-lfs 可选开关 |
| 移动端凭据 | 安卓：keyring 没有后端（会**静默退回内存 mock**，令牌重启就丢），落在应用私有目录的文件里（按 uid 隔离）；Android Keystore 加密等真有需求再上。iOS：Keychain（未做） |
| 用户不会 git | 引导式配置：粘贴仓库 URL + token 即可。仓库不存在时可代为创建 |
| Windows 的 `core.autocrlf=true`（装 Git 时的推荐项）会让**拉取悄悄改写笔记的行尾**（LF checkout 成 CRLF，diff 满屏「整篇都改了」） | vault 仓库一律仓库级 `core.autocrlf=false`（`ensure_repo`），不动全局配置。笔记的字节原样进原样出 |

**默认 `.gitignore`**（`.verso/` 是纯派生数据，本就不该同步）：

```
.verso/
.DS_Store
Thumbs.db
```

**M0 就要做的一件小事**：新建 vault 时自动 `git init` 并写入 `.gitignore`。成本几乎为零，但避免了以后要求用户手动补救。

**初始分支必须是 `main`。** libgit2 默认还是 `master`，而且**不读 `init.defaultBranch` 这项 git 配置** —— 必须在 `RepositoryInitOptions` 里显式指定，否则新建的 vault 会和 GitHub 等平台的默认分支对不上，第一次推送就得手动改名。

对早期版本建出来的 `master` 仓库：**只在仓库完全空（无提交、无远端）时**迁移到 `main`。那种状态下分支名纯粹是个名字，改它不破坏任何东西；一旦有了提交或远端就绝不碰 —— 悄悄改分支名可能打断用户与远端的对应关系，也可能人家就是想用 `master`。

#### 多人协作：本地优先的异步协作

Verso 的多人协作不是 Google Docs 式的「所有人同时改同一段文字」，而是让一个小团队
各自在完整的本地 vault 上工作，需要时再交换变化。目标场景是 **2–10 人的研究小组、
共同写作者和小型项目团队**：有人在实验室电脑上补记录，有人在路上离线改论文，AI CLI
在另一台机器上整理资料；下一次同步后，每个人都看得懂发生了什么，也能安全处理重叠修改。

这不是一套新的存储模型，而是 §2.8 现有 Git 底座向上长出的产品体验：

- `.md` 仍是唯一真源，每台设备都有一份可脱离 Verso 使用的完整仓库
- Git 远端负责传输、留痕和权限；Verso 不建账号系统，也不运营协作服务器
- 离线时照常读写；上线后主动同步，不以「必须在线」换取协作能力
- 每台设备各自保管令牌和提交身份，凭据永不随 vault 同步
- `.verso/` 只保存「我看到哪里」之类可重建的本机状态；评论、任务、审批结论等
  团队事实不能偷偷只存在这里
- 远端服务商的仓库权限就是写权限边界。Verso v1 不再叠一层成员、角色和组织管理

**用户看到的是协作事件，不是 Git 操作。** 同步完成后应当说「林修改了《实验方案》，
你修改了《数据处理》，两边已合并」，而不是 `pull --rebase succeeded`。提交是底层耐久事件，
界面把它整理成按人、时间和文档分组的活动：

- 「协作动态」列出谁在何时修改、创建、移动或删除了哪些文档
- 它与本地版本历史共用一个「动态」入口：顶部是当前未记录改动，下方是带作者的活动流；
  没有远端时自然退化为个人历史，不为同一批提交再造第二个侧栏页面
- 默认突出上次同步后尚未看过的变化；已读位置是本机状态，丢了可以从历史重建
- 点一条动态进入已有的逐行 diff，必要时可恢复该版本
- 自动提交继续按时间窗聚合，但摘要必须说出文档名，不能只显示哈希或文件数
- 同一个人的多台设备用同一提交身份；身份缺失或改变时，在同步前明确提示

**加入共享 vault 的流程只需要三步**：接受 GitHub 邀请后粘贴远端地址、确认显示姓名与
邮箱、选择本地位置。已连接 GitHub App 的人直接复用本机安全凭据，不必再粘贴令牌；令牌
输入仅作为 GitLab / 自托管服务或逐仓库覆盖的备用方式。已有本地仓库增加远端时必须先预检
双方历史；不能把「远端已经有内容」伪装成普通首次同步，更不能静默覆盖任一边。

**共享边界不能被「vault」绑死。** 团队共同维护整个项目时，共享完整 vault 是合理的；
但把一篇草稿交给合作者，不等于允许他读取日记、模板和同库的其他研究材料。GitHub / GitLab
的权限边界是仓库，稀疏检出和前端隐藏都不是安全隔离。因此「共享这篇」必须创建一个
**独立的小型共享空间**（底层仍是普通 Git 仓库），不能从原仓库挑一个路径假装单篇授权。

第一版（v0.7.12）遵守以下边界：

- **共享对象是文档树节点，不是孤立文件。** `项目.md` 与同名 `项目/` 本来就是一个
  Verso 节点，因此迁移时必须包含主文档、整个同名子树，以及这些文档直接引用的全部
  本地附件；普通链接指向的树外笔记只列为「不会共享」，绝不沿链接扩散私人内容
- 整棵共享子树使用所在空间的成员集合，权限向下继承。Git 仓库里不存在可信的「某个
  子目录对仓库成员不可见」；子文档若要给另一组人，必须先移出当前空间
- 正文与子文档只在共享空间保留一份，原 vault 不留下会继续分叉的副本。附件可能同时
  被私人笔记引用，因此只复制、不从原 vault 删除
- **仓库按成员组复用，不按分享次数创建。** 分享时默认列出已有共享空间；把新内容加入
  某个空间，就继承该空间的成员。只有没有合适的成员组合，或用户明确选择「新建共享
  空间」时，才创建另一个仓库。A、C 都给 person 1 时共用一个空间；B 只给 person 2 时
  才需要第二个；A 与 B 权限不同就绝不能为了少建仓库而混在一起
- 共享弹窗不得默认选中第一个已有空间。用户必须亲手选择目标，提交前再次看到正文将从
  私人区移出以及谁会获得访问权；对无法自动核对成员的自托管仓库，要明确说明权限由远端
  管理，不能把空白成员列表伪装成“没有其他人”
- `.verso-space.json` 里的成员只是离线提示，不是权限真源。GitHub 空间在加入新内容前要
  读取实际协作者与待接受邀请，分别显示「已加入 / 等待接受」；远端核对失败时不得继续向
  该空间加入私人内容，避免按陈旧缓存分享给错误的人
- 新建空间流程只问两件事：协作者 GitHub 用户名，和**空间名称（可留空）**。
  名称是选填不是必填 —— 不把 Git 的命名细节变成每次分享前的功课；但它必须
  **摆在建库之前**（v0.7.33）：仓库一旦建出来就改不了名（改了所有人的远端地址
  就断了），事后补一个「重命名」入口是做不到的，而自动生成的
  `verso-space-01hf…` 在 GitHub 的仓库列表里没人认得出是哪一份内容。留空时
  仍然自动生成，行为和以前一样。
  这个名字同时是 GitHub 上的仓库名和本地目录名；界面名称仍按成员组自动生成，
  之后可在空间管理中修改。Verso 自动创建私有 GitHub 仓库、邀请成员，并在当前
  vault 同级的 `Verso Shared/` 下选择不会重名的位置；仓库 URL、本地目录等实现
  细节收进「使用已有仓库」高级流程，继续支持 GitLab 与自托管
- 创建过程先在临时目录完整建库，再验证远端、提交并推送，最后才换入目标位置并移走原
  子树；失败时原文档保持不动，不留下半截本地共享空间
- 用户界面借鉴 Notion，称为「私人 / 共享」，不称「我的仓库 / 共享空间」。共享区里的
  一个空间可以包含多棵顶层文档子树；拥有者能看到所有空间，协作者只看到获邀加入的那些

**分享之后必须能收尾，不能只有“创建”没有“管理”**（v0.7.14）。「管理空间」里可以查看一个共享
空间包含的顶层内容、已加入成员与待接受邀请；GitHub 空间由拥有者直接邀请或移除成员，
其他托管服务只展示远端地址并把权限管理交还服务商。成员变化必须重新读取远端权限，不能
只改本地缓存制造已经生效的假象。

共享内容可以整棵「移回私人」。用户先明确选择一个本机私人空间，Verso 预检同名内容、
同步共享远端，并在临时目录完成删除与提交；远端成功前不动当前正文，私人目标冲突时不
覆盖。正文与同名子树移回后在共享空间只保留一份，附件复制到私人空间，但保守地留在共享
空间，避免破坏仍引用它的其他共享笔记。完成后直接打开私人空间里的该篇。界面必须在确认
前说明：停止共享只会阻止今后的同步访问，**无法收回成员已经拉取的文件与 Git 历史**。
最后一棵内容移走后，共享仓库仍默认保留；删除本地目录或 GitHub 仓库是另一项破坏性操作，
不能被“移回私人”顺手执行。

`.verso-space.json` 记录空间包含的顶层内容根，旧版只有单个 `entry` 时自动迁移为一项清单；
它用于管理界面与安全迁移，不取代 Markdown 内容。标记丢失后内容仍完整可读，因而继续满足
`.verso/` 派生状态可删除重建的原则。

同步与共享共用设置里的一个「GitHub 连接」，分享弹窗不得再索要令牌和提交身份。GitHub
自动流程需要 `Contents: write`（推送）和 `Administration: write`（建库、邀请）；凭据只进
系统安全凭据。默认入口是 Verso GitHub App 的**设备授权**：点击连接后打开 GitHub 验证页，
输入一次短码即可；桌面端只内置公开的 Client ID，绝不包含 Client Secret、App 私钥或 Webhook
secret。应用拿到的是按用户与 App 权限交集受限的 `ghu_` 令牌，连同可轮换的刷新令牌只保存在
本机安全凭据中。用户仍须把 App 安装到自己管理的账号并选择仓库；「公开 App」只表示任何人
能安装，不会使任何私人仓库可见。当前仓库的窄权限令牌、GitLab 与自托管凭据仍作为高级覆盖
项保留，不能静默扩成账号级权限。不要用 Gist、分支或 sparse checkout 代替独立仓库：它们要么
放不下子树和附件，要么隔离不了原仓库历史。

不要在同一棵文档树里伪造跨仓库的普通子节点：那会让搜索、反向链接、终端工作目录和
版本历史同时跨多个根。首版在同一侧栏用「私人 / 共享」两个顶层区呈现，打开共享根时
后台切换实际 vault；用户看见的是内容边界，不是 Git 仓库切换。

**冲突要按用户理解内容的方式处理。** 当前逐段选边是安全底线，协作阶段在此之上增加
语义分组，但不改变 Markdown 文件：

| 内容 | 冲突粒度 | 默认行为 |
|---|---|---|
| 正文 | 标题下的段落 / 列表项 | 不重叠就自动合并；重叠时并排选边或两边都保留 |
| frontmatter | 单个属性键 | 显示属性名、双方值、作者和时间，再由用户决定 |
| database | 单个字段 / 单元格 | 不同记录或字段自动合并；同一格冲突才询问 |
| 文件树 | 新建、改名、移动、删除事件 | 尽量识别为一次移动；删除与修改相撞时绝不默认删除 |

解决界面必须始终提供「两边都保留」，且在全部冲突定稿之前不写回工作区。即使 Verso
无法理解某段内容，也要退化为现有逐行/逐段合并，而不是拒绝同步或丢弃一边。

v0.7.15 先把这条安全底线落实成可验证的三方合并。冲突结果必须带上分叉前的共同版本，
否则界面无法区分「本地改了、远端没动」和「两边把同一值改成了不同结果」。Markdown
frontmatter 按顶层键比较：不同键由两边修改时自动合并，同一键才出现选择；database 的
单元格本质上就是目标笔记的 frontmatter，因此沿用同一规则，不另造数据库专用副本。
「两边保留」不能写出重复 YAML 键：原键保留本地值，并新增一个不重名的
`属性名（远端）`，让两份值继续可见、可编辑。复杂键、重复键、损坏 YAML 或没有共同版本
时不猜，整篇退回文本冲突。

正文仍由 Git 先自动合并不重叠的行；只有真正重叠的 hunk 进入面板，每一段增加「两边都
保留」，共同上下文只写一次。删除与修改冲突明确标成一类，初始不选择删除：用户只能主动
确认删除，或恢复仍有修改的那一边。改名/移动由 Git 的 rename 检测先尝试把另一边的修改
重放到新路径；无法可靠识别时继续按路径冲突显示，绝不通过猜测静默移动或丢弃旧路径。
所有字段和段落都定稿之前不写工作区；重放期间远端再变化则重新给出一轮完整冲突。

**定稿跨轮累积。** 本地那一串提交是逐个接到远端后面的，冲突可能分散在好几步上，而下一步
撞上哪几篇要先知道这一步怎么定稿才算得出来 —— 所以一轮只报得出当前这一步的那一批。
撞上没有定稿的一批时整次合并撤回，因此重放必须带上**历轮全部**的定稿，否则前面那步会
重新拦一次，两批冲突交替弹、永远收不了尾。同一篇在两边被改成不同名字时 Git 会把两边都
已不存在的**旧路径**也算进冲突，那一条没有任何内容可选，自动按「删掉旧名字」定稿，不进面板。

**审阅模式是可选的安全阀，不取代直接同步。** 小团队默认仍可直接同步；需要把关时，贡献者
选择「提交修改建议」，审阅者再逐文件接受或退回。主界面只使用「修改建议 / 审阅 / 接受 / 退回」
这些概念，不要求用户理解分支或 Pull Request。

v0.7.16 的修改建议仍在同一个共享仓库里传递，不为每一批建议创建新仓库：

- 每批建议使用 `refs/heads/verso/suggestions/<id>`，提交成功后才把贡献者的工作区恢复到远端
  正式版本；因此建议已经耐久上传时才会离开当前正文，网络失败不会吞掉本地变化
- 动态面板先拉取这组命名空间并列出待审阅建议。提交者、时间、文件和原始提交都保留在 Git
  对象中；用户不需要手动创建、切换或清理分支
- 审阅决定没有默认值。每个文件必须明确选择接受或退回；改名作为「删除旧路径 + 写入新路径」
  一起决定，不能留下半次移动
- 接受前比较建议分叉点、当前正式版本和建议版本。正式版本与建议修改同一内容时进入 v0.7.15
  的三方冲突面板；二进制内容无法安全合并时拒绝猜测
- 审阅结果写入版本化的 `.verso-reviews/<id>.json`，其中记录接受和退回的文件。这个目录不出现在
  文档树、搜索和「本次改了哪篇」里，但会随正式版本同步，因此换设备后也不会把已审建议重新列出
- 审阅完成并同步正式版本后删除建议分支；删除失败只给出清理提示，结果记录仍可阻止陈旧建议重现

评论与 @提醒暂不顺带加入：它们一旦成为团队事实，就必须先决定可移植、可版本控制的存储格式，
不能为了做 UI 把唯一副本塞进 `.verso/`。

按价值和风险拆成三步，暂不占用 M6/M7 的版本号：

1. **协作可见** ✅ v0.7.11–v0.7.14：提交身份、协作动态、未读变化、引导式加入共享 vault，
   以及按成员组共享内容、管理成员、把完整内容树安全迁回私人空间。
   已能区分自己与协作者、只提示其他人的新提交、按作者筛选，并从文件直接打开差异或
   恢复版本。「加入共享仓库」向导收集远端地址、自己的令牌与身份、一个空的本地位置，
   Verso 完整克隆后直接打开；下载失败不在目标目录留下半截仓库，非空目录绝不覆盖。
   验收：两个人离线修改不同文档后同步，双方无需理解 Git，也能看到谁改了什么并打开差异。
2. **协作安全** ✅ v0.7.15：frontmatter / database 语义冲突、移动与删除冲突。验收：不同段落自动
   合并；同一属性冲突时可选任一边或两边保留，任何路径都不丢原始内容。
3. **修改建议** ✅ v0.7.16：可选审阅流程。验收：贡献者提交一批变化后回到正式版本，
   审阅者能逐文件接受或退回；重叠变化进入三方冲突，原始提交与审阅结论始终可追溯。

字符级实时共同编辑、在线光标、成员在线状态和大型组织权限不在 v1 范围内。它们需要
CRDT/OT、常驻服务和完全不同的故障模型，不能借「多人协作」之名悄悄引进来。

### 2.9 发布成网页

**默认不发布。** 只有 frontmatter 里 `publish: true` 的笔记会进入站点 —— 安全默认，避免手滑把日记推上公网。文档树上可对某节点执行「连同子文档一起发布」，批量写入该字段。

**阶段一：零开发，现在就能用**

直接用 [Quartz](https://quartz.jzhao.xyz/) —— 它就是为「把 Obsidian vault 发布成静态站」做的，支持 wikilink、反向链接、KaTeX、图谱，配个 GitHub Action 即可 push 部署。

**这是 §2.4「方言与 Obsidian 兼容」这个决定的直接回报**：因为没造自创语法，整个 Obsidian 工具生态开箱可用。所以内置发布功能可以排到很后面，不占用核心里程碑。

**阶段二：内置发布（M7）**

应用内「发布」按钮 → Rust 侧把 `publish: true` 的笔记转成静态 HTML → 推到 `gh-pages` 分支。自建的价值在于：

- **样式与应用完全一致** —— 复用 §6 的排版规范，网页和软件里看到的是同一个东西。第三方 SSG 做不到这点
- **公式构建时预渲染** —— KaTeX 在构建步骤跑完，产出纯 HTML + 一个 CSS 文件。访问者不加载任何 JS 就能看到公式，手机上秒开
- **转换逻辑本来就有** —— `[[链接]]` → 相对 URL、`![[嵌入]]` → 内联、反向链接区块，这些解析器 M3 已经写完了

**仓库结构**：GitHub 免费计划下 Pages 要求公开仓库，付费计划才能从私有仓库发布。若要免费又保私密，用两个仓库 —— private 存整个 vault（同步+备份），public 只放构建产物。软件应当支持这种「双远端」配置。

### 2.11 自动更新（桌面，v0.6.14）

一个装在自己电脑上的软件，如果升级要靠用户自己想起来去看仓库，那它实际上就是不会升级的。M7「发布」里本来包含这件事，提前做出来是因为它的成本几乎全在**流水线**上，而流水线越早建越好 —— 在只有一个用户的时候把发布跑通，代价比有一百个用户时低得多。

**底座是 Tauri 官方的 updater 插件 + GitHub Releases。** 不自建服务端，理由和 §2.8 不自建同步服务器一样：我们做客户端，不做服务端。发布流程是「推一个 tag → CI 编四份包 → 传到一个草稿 release」，客户端读 `releases/latest/download/latest.json`。

四条不能动的规则：

1. **更新包必须签名。** 私钥在作者手上（`~/.tauri/verso.key`，只作为 CI secret 存在于 GitHub），公钥编在应用里。不签名的自动更新等于给任何能插进这条链路的人一个「替换用户电脑上的可执行文件」的权限 —— 这比不做自动更新危险得多。**私钥丢了，所有已装出去的客户端就永远收不到更新了**（换公钥要重新发一个包，而那个包只能靠用户手动去装）。

2. **下载和安装分成两次点击。** 插件提供一步到位的 `downloadAndInstall`，但那意味着「点一下 → 几十秒后应用自己没了」，而这中间用户还在打字。所以先下完、停下来等，真正要重启时才落盘 + 记一个版本（和关窗那条路走同一个收尾函数）、再装。**更新的时机由正在写东西的人来定**，不由「下载刚好完成」这个与他无关的时刻来定。

3. **自动检查失败一律不打扰。** 没网、代理拦了、GitHub 挂了 —— 这些都不是用户此刻在做的事。只有他自己按了「检查更新」，失败才报出来。同理，有新版本时只在状态栏上挂一条，不弹窗。

4. **只有桌面能装。** updater 插件不支持移动端 —— 一个应用没有权限就地替换自己，安卓的包由应用商店或系统安装器管。所以那两个 crate 是 target 依赖，权限声明也必须是**分平台的 capability**（写进那个不分平台的文件会让安卓构建在 ACL 解析阶段直接失败）。

5. **但移动端要查得了（v0.7.21 补）。** 「装不了」和「不该知道」是两件事，
   而上一版把它们并成了一个判断：手机上「检查更新」按钮直接置灰，配一句
   「当前平台不支持应用内更新」—— 用户连「现在有没有新版本」都问不出来。

   查这件事只需要一个 HTTPS GET，和能不能就地安装毫无关系。所以移动端绕开
   插件，由 Rust 侧（`update_check.rs`，复用已有的 `ureq`）直接读 updater
   用的同一个 `latest.json`。

   - **给的是安装包直链，不是发布页。** 发布页上挂着十几个文件（各平台的
     安装包、各自的 `.sig`、清单本身），手机上还得先展开 Assets 再从里面挑
     对的那一个 —— 那不是「下载」，那是一道阅读理解题。直链走 GitHub 固定的
     `/releases/latest/download/<文件名>` 跳转，点一下浏览器就开始下，下完
     系统安装器接手。
   - **直链是拼出来的，所以拼完要先 HEAD 一次确认它在。** 拼错的结果是一个
     404 页面，比发布页更糟 —— 用户会以为是发布出了问题。确认不了就退回
     发布页，那个地址不依赖任何命名约定。文件名格式和 `scripts/android-apk.ps1`
     的改名那一段是同一份，两处漂了就是 404，Rust 那边有测试钉着。
   - **不自动跳浏览器。** 查到新版本只在状态栏和设置里显示，去不去下载是
     用户按的 —— 和 §2.11 那条「不弹窗、不打断」是同一个道理：更新永远没有
     「正在写的这句话」重要。

   - **状态机里 `manual` 和 `found` 分开**，不是给 `found` 加个布尔。两者
     能做的事完全不同（一个能下载安装，一个只能跳转），合在一起的话每个
     用到它的地方都要再判一次「这个 found 是能装的那种吗」。
   - **不验签名**，因为这条路不产生任何可执行的东西。最坏情况是有人骗你
     「有新版本」，而下一步是打开 GitHub 发布页 —— 安装包本身仍由 GitHub
     的 https 和用户自己把关。签名校验属于「装」那一半，那一半在移动端不发生。
   - **版本比较两边各一份**（`update_check.rs` 与 `lib/update.ts`），用例
     一致。都不能用字符串比大小：字典序下 `"0.10.0" < "0.9.0"`，那正是版本号
     跨过 x.10 时最容易出现的一次静默错判 —— 症状是「明明发了新版，手机上
     一直说已是最新」，而且要等真的发到 0.10 才现形。

**release 是草稿，要人点一下才发布。** 编译要十几分钟，中途发现哪个平台挂了还能删了重来；而 `releases/latest/download/…` 这个地址只认已发布、非预发布的那一个 —— 在点 Publish 之前，没有任何用户会被推到这一版。

**更新说明就是 CHANGELOG 里那一节**（`scripts/release-notes.mjs` 取出来 → release 正文 → `latest.json` 的 `notes` → 用户在更新界面里读到的那段话）。这条链决定了 CHANGELOG 那几段话不是流水账，是给人看的。

**macOS 的包没有签名和公证**，Gatekeeper 会拦。这一项要等有 Apple 开发者账号时再说，在那之前 mac 版是「能装但要手动放行」的状态。

---

## 3. 架构分层

```
┌─────────────────── WebView (React) ──────────────────┐
│  UI 层        文档树 / 标签页 / 快速切换器 / database   │
│  编辑器层     CodeMirror 6 + 扩展（§4）               │
│  数据层       Zustand store  ←→  Tauri IPC 客户端      │
└──────────────────────┬───────────────────────────────┘
                       │ IPC (JSON) / Event (Rust→前端推送)
┌──────────────────────┴───────────────────────────────┐
│  Rust 核心                                            │
│   VaultFs  ← 抽象接口，桌面/iOS/Android 三套实现        │
│   vault    扫描 / 文档树推导 / 原子写入 / 重命名事务     │
│   parser   pulldown-cmark + frontmatter → 结构化      │
│   index    SQLite 增删改查、FTS、反向链接、props 查询    │
│   watcher  notify + 去抖 + 回环抑制                    │
│   sync     git2-rs：同步、冲突检测、凭据、协作事件      │
│   pty      portable-pty：内嵌终端（desktop only）       │
│   publish  publish:true → 静态 HTML → gh-pages         │
└───────────────────────────────────────────────────────┘
```

**分工原则**：Rust 负责「全库级」操作（扫描、索引、搜索、批量重命名），前端负责「当前文档级」操作（编辑、渲染、增量解析）。当前打开的文档不走 Rust 解析 —— 每次按键都做 IPC 会毁掉输入手感。

**IPC 接口草案**

```ts
// —— vault 与文档树
vault_open(path: string): VaultInfo
tree_list(): TreeNode[]                              // 已合并 X.md + X/ 的节点树
note_read(id: string): { frontmatter: Record<string, unknown>; body: string }
note_write(id: string, content: string): void
note_create(parentId: string | null, title: string): NoteMeta   // 自动建同名文件夹
note_rename(id: string, newTitle: string): void      // 事务：同时改 .md 与同名文件夹
note_move(id: string, newParentId: string | null): void
note_delete(id: string, withChildren: boolean): void

// —— 检索与关系
quick_switch(query: string, limit: number): NoteMeta[]   // 模糊匹配名称+路径
search(query: string, limit: number): SearchHit[]        // FTS 全文
backlinks(id: string): Backlink[]
breadcrumb(id: string): NoteMeta[]

// —— database
query_view(spec: ViewSpec): Row[]
prop_set(noteId: string, key: string, value: unknown): void   // 表格内编辑回写

// —— 同步与发布
sync_status(): { ahead: number; behind: number; dirty: boolean; lastSync: string }
sync_now(): SyncResult                                   // pull --rebase → commit → push
sync_conflicts(): Conflict[]                             // 段落粒度
sync_resolve(path: string, choice: "ours" | "theirs" | "merged", content?: string): void
history(noteId: string, limit: number): Revision[]
publish_site(): { generated: number; url: string }

// —— 事件（Rust 推送）
"note:changed"   { id, path, source: "external" | "self" }
"note:deleted"   { id }
"tree:changed"   {}
"index:progress" { done, total }
"sync:state"     { state: "idle" | "syncing" | "conflict" | "error", detail?: string }
```

---

## 4. 编辑器设计

### 4.1 已确定：CodeMirror 6

一句话理由：**LaTeX 公式最终必须回落到源码编辑，而 WYSIWYG 块编辑器把源码当成外来物。**

在 TipTap/ProseMirror 里做公式，公式是一个 atom node，双击弹出输入框，框里是另一套编辑环境 —— snippet、括号跳转、语法高亮全要在那个小盒子里重新实现，且盒内盒外键位行为不一致。CM6 里公式就是文本的一部分，所有编辑能力天然生效。

**接受的代价**（写下来，别以后当成 bug）：
- 「所见即所得」要自己用 decoration 实现（§4.2），工作量比 TipTap 大
- 块拖拽重排、悬停手柄要逆着文本模型硬做，不会有 Notion 那么顺
- 多列布局做不了 —— 这也符合「不加自创语法」原则
- 移动端触摸编辑体验弱于块模型，需要额外投入（§5.5）

### 4.2 Live Preview 机制

```
光标不在该节点范围内  →  Decoration.replace(widget)  渲染成最终形态
光标进入该节点范围    →  移除 decoration，显示源码
```

用 `ViewPlugin` 监听 selection + doc 变化，遍历可视区域的 syntax tree，产出 decoration set。适用于：公式、图片、内部链接、粗斜体标记、高亮、callout、标题的 `#`、frontmatter 折叠。

跨行块公式必须用真正的 block replacement：源码有几行不能决定渲染态占几行，否则
CodeMirror 会在 KaTeX 上下各残留一个正文行盒。公式自己的垂直留白只由一层紧凑 padding
负责，KaTeX `.katex-display` 自带的上下 margin 要归零，不能把两套间距叠起来。

**性能红线**：只处理 `view.visibleRanges`，绝不遍历整个文档。KaTeX 渲染结果按「公式源码字符串」做 LRU 缓存（1000 条），滚动来回时不重复渲染。

**M1 实测补充 —— CM6 的一条硬约束逼出了两层架构**：

跨行的 replace decoration 与块级 decoration **都不能由 ViewPlugin 产出**，只能来自 StateField（报错 `Decorations that replace line breaks may not be specified via plugins`）；而「只扫 `visibleRanges`」又只有 ViewPlugin 能做到。两者不可兼得，最终按是否跨行分工：

| 层 | 负责 | 理由 |
|---|---|---|
| StateField | 跨行的块级公式 | CM6 唯一允许的方式。这类节点数量少，全文扫描代价可接受 |
| ViewPlugin | 行内的一切 | 只扫可视区，长文档里每次按键都不卡 |

**源码模式**（`Ctrl/⌘+E`）：把上面这两层连同表格、database 视图整个装进一个 compartment，切模式时 reconfigure 成空。这样**不必重建 `EditorView`** —— 光标、选区、撤销历史、滚动位置全部留在原处；一个切一次就把这些丢光的开关，没人会切第二次。

高亮**不进这个 compartment**：源码模式要看的是 Markdown 源码，不是纯文本。标题该大还是大、代码该等宽还是等宽，只是 `##`、`**`、`$` 这些标记不再被藏起来，公式与表格也不再渲染成最终形态。

它是**全局**开关而不是每篇一个：切进源码模式通常是为了做一件具体的事（改一段表格、抠一个链接、看 AI 到底往文件里写了什么），做完就切回来。每篇记一个状态的话，过两天打开某篇笔记会以为它坏了。

**属性条（§2.3）也跟着退。** 它是 frontmatter 的渲染结果，和正文里的表格、公式是一回事 —— 正文都退回源码了它还渲染着的话，页面上就只剩这一块自相矛盾。显示的是 `read_note` 带回来的 `frontmatterText`，即文件里两道 `---` 之间的**原文**，不是把解析后的映射再序列化一遍（那会丢掉键序、缩进和注释）。

**这一段可以直接改**，走 `frontmatter_write` 命令（v0.5.10）。它和正文是**两半各写各的**：正文走 `note_write`，这一段走 `frontmatter_write`，两条路都从磁盘读另一半再整篇写回，谁也盖不掉谁 —— 前端在写 frontmatter 之前会先把正文冲一次盘，避免两个「读—改—写」同时在飞。

没有把 frontmatter 并进 CodeMirror 的文档，尽管那样看起来更「整体」。代价是把正文的口径全搅乱：保存要换一条写入路径、大纲行号要整体偏移、字数统计会把 YAML 算进正文，而且**撤销一步就可能把 `---` 那一行撤掉，再一保存 frontmatter 就没了**。分成两半之后，这些问题一个都不存在。

三条保险都在 Rust 侧（`Vault::write_frontmatter`）：YAML 解析不过就报错、一个字节都不写；`id` / `created` 被删掉时从旧值补回来（换 id 等于换了这篇笔记的身份）；正文永远来自磁盘，不从前端传。

#### 输入法组词期间不许动 decoration（v0.7.38）

中文输入法在 contentDOM 里**就地**组词，CM 靠读回那段 DOM 才知道用户输入了
什么。这期间只要我们换掉一段 decoration，它读回的就是一段被我们改过的 DOM。

症状很唬人：在 `$x$` 后面打一个中文字，**整条公式被那个字顶掉**，变成 `$啊`。
机制是这样的 —— 光标停在公式末尾时显示的是源码；组词把字送进来之后光标已
经在公式外面了，于是那一刻公式「该」切回渲染态，而切换正好发生在输入法占着
那段 DOM 的时候。

- 判据用 `view.compositionStarted` 而不是 `composing`：后者要等 CM 真的数到
  一次变更才为真，而输入法从 `compositionstart` 那一刻起就已经占着 DOM 了
- 组词**结束后那一拍必须补一次重建**，否则跳过的那些更新永远补不回来
- `parseRefresh` 也要跟着停：它在解析推进时派发 effect，而组词过程中解析一直
  在推进
- **英文键盘永远复现不了**（没有组词这一步），所以只能靠模拟 composition 事件
  来守（`tests/browser/editor/composition.test.ts`）

### 4.3 需要写的 CM6 扩展清单

| 扩展 | 说明 | 里程碑 |
|---|---|---|
| `markdownExtended` | `@lezer/markdown` 上挂自定义解析：`InlineMath` / `BlockMath` / `WikiLink` / `Embed` / `Tag` / `Callout` / `Highlight` | M1（地基，第一个写） |
| `livePreview` | §4.2 的 decoration 引擎 | M1 |
| `frontmatterFold` | frontmatter 折叠成属性条 + 表单编辑 | M1 |
| `mathSnippets` | §5 的核心，单独一个模块 | M2 |
| `smartPairs` | `$` `(` `{` `[` 自动配对，`$` 仅在合适上下文生效 | M2 |
| `wikiLinkComplete` | 输入 `[[` 触发笔记名补全，模糊匹配 | M3 |
| `slashCommand` | 输入 `/` 弹出块插入菜单（标题/表格/callout/公式/database 视图） | M3 |
| `pasteHandler` | 粘贴图片 → 落盘 attachments → 插入 `![[]]` ✅；粘贴 HTML → 转 Markdown、粘贴 URL + 选中文本 → 变链接（未做） | M3 |
| `typography` | 中西文混排间距（渐进增强）、连字、标点挤压 | M4 |
| `listRenumber` | 有序列表在删除 / 粘贴后自动重排编号；起始编号保留（被代码块打断的列表靠它衔接），手改第一项编号则整体顺延 | v0.6.29 |

`slashCommand` 值得单独说明：它是「Markdown 也能有 Notion 手感」的关键 —— 用户不需要记语法，输入 `/` 选「三级标题」，编辑器插入 `### `。存储仍是纯文本。

**这份菜单可以改**（v0.5.42）：内置条目逐条可关，还能加自己的。放在**设置**里
而不是 vault 里 —— 它和公式 snippet、快捷键是同一类东西（个人的输入习惯，跟着
人走，换个库不该重配），而不是笔记内容。自定义条目用 `$0` 标光标位，和 snippet
一个写法；一条写坏了只跳过那一条并报出来，不连累其他（同 §6.4）。设置页不能把
内部 JSON 格式直接扔给用户：内置命令用带显示开关的表格，自定义命令按
「名称与说明 / 插入内容」逐行编辑，并提供新增、删除和上下排序。

内置条目的 **`label` 同时是它的 id**（设置里记的就是它），所以改内置条目的名字
等于让用户之前的隐藏设置失效。

### 4.4 图片

`Ctrl/⌘+V` 粘贴 → 落盘 `attachments/` → 光标处插入 `![[attachments/xxx.png]]`。
剪贴板里的图片常常没有文件名，按时间起一个（`粘贴-20260731-224500.png`）——
满 vault 的 `image.png` 谁也认不出哪张是哪张。重名不覆盖，加时间戳后缀：
覆盖掉的可能是别的笔记正在用的那一张，这类损坏没有撤销。

**尺寸写成 `![[图.png|300]]`**，即 Obsidian 的写法（不自创语法）。拖图片右边缘
改的就是这个数字 —— 宽度是内容的一部分，得跟着 `.md` 走，而且拖进 Obsidian
也要能看。只认宽度，高度交给浏览器按比例算：手写的高宽比几乎总是错的。

**显示走 Tauri 的 asset 协议**，作用域在打开 vault 时**只放行当前这个 vault 根**。
笔记可以来自分享（§2.9），`![[../../秘密.png]]` 不该能读到 vault 外面去；前端
拼路径前也会先挡掉 `..`。目标名不带斜杠时按 §2.3 的约定去 `attachments/` 找。

找不到的图不显示碎图标，而是一行「图片找不到：<文件名>」—— 用户得知道是**哪个**
文件没了。

### 4.5 代码块

三件事：**按语言高亮、内容行编号、右上角一个复制按钮**（v0.5.23）。围栏的
隐藏与底色见 §4.2 的 live preview 规则，光标一进块就退回源码，和别的标记一致。

**高亮按需加载。** 语法包走 `@codemirror/language-data`，每条都是
`LanguageDescription`，用到哪个语言才动态 `import` 哪个 —— 笔记里没有 Rust
代码块，Rust 的语法包就一个字节都不读。M1 当初关掉高亮的理由（「为几个片段
背一堆语言包」）到此不成立了。加载完语法树会变，块级 decoration 靠
`parseRefresh` 察觉后重算。

配色**只给会改变语义的东西上色**：关键字、字面量、注释、名字。标点和运算符
保持正文色 —— 每个 token 都染一遍的方案扫读时反而找不到重点，也和 §6.2
「黑白灰打底，颜色只做重音」冲突。深浅两套值在 `styles.css` 里各配一份。

**行号只在代码块里有，正文永远没有。** §4 拒绝 `basicSetup` 拒的是**文档**
的行号栏 —— 那会让一篇笔记看起来像一个源文件。代码块是另一回事：讨论代码
时「第几行」是刚需。实现上是行属性 + CSS 的 `::before`，不是 widget 也不是
gutter：生成内容不进选区也不进剪贴板，**框选整块代码复制出去是干净的代码**。
只有一行内容的块不编号，给 `npm i` 标个「1」纯属噪音。

**复制按钮不违反 §7.5。** 那一条否决的是**执行**笔记里的代码（「打开一篇
笔记 = 运行一个陌生程序」），把文本放进剪贴板不涉及执行。按钮常显但很淡，
不做成悬停才出现 —— 触摸屏上没有悬停（§5.5 移动端）。

### 4.6 模板（v0.5.25）

**模板就是模板目录下的普通 `.md`。** 默认 `templates/`，设置里可改，留空等于
关掉这个功能。不引入新的文件格式、不进 `.verso/`：模板也该能被搜索、被链接、
被别的编辑器打开（§0 第 1 条）。

**只做变量替换，不做代码执行。** Obsidian 的 Templater 能在模板里跑 JS
（`<%* ... %>`），这里不做 —— 模板是文件，会被分享、会由 AI 生成、会从别人的
仓库里抄来，一个能执行模板里代码的软件等于把「插入模板」变成「运行一个陌生
程序」，和 §7.5 否决「代码块运行按钮」是同一条理由。

| 变量 | 展开成 |
|---|---|
| `{{title}}` / `{{path}}` | **目标笔记**的标题与路径（不是模板自己的） |
| `{{date}}` / `{{date:YYYY年M月D日}}` | 日期，格式取 moment 那套 token 的子集 |
| `{{time}}` / `{{time:HH:mm:ss}}` | 时刻 |
| `{{selection}}` | 插入时选中的文字 |
| `{{cursor}}` | 插完光标停在这里，自身不留下字符 |

**认不出来的变量原样留着**，不替换成空。从别处抄来的模板里会有
`{{tp.file.title}}` 这类别的软件的写法 —— 替换成空的话，用户看到的是一篇少了
几块的笔记，还不知道少在哪。

**侧栏有一格给它**（v0.5.26）。只藏在 `/` 菜单和命令面板里的话，不知道它存在的人
永远不会用到，而且「我有哪些模板」这个问题没有任何地方能回答 —— 摆在侧栏里，
它同时是清单、入口和「这个功能存在」的提示。模板首先是一个可以管理的文件：单击
打开编辑，双击就地改名，右键菜单可插入、用它新建、重命名和删除。行尾另有明确的
「插入」「用它新建」「更多」按钮；这些按钮常显，保证没有右键和悬停的触摸屏也能
完成全部操作。打开模板本身仍走普通 `.md` 编辑器，没有第二套编辑器。

**模板面板里可以直接新建**（v0.6.33）。顶部的「新建模板」会自动建立尚不存在的
模板目录，创建「未命名模板」并在列表里就地改名；不能要求用户先去文件管理器或终端
准备目录。旁边有可展开的完整变量说明，列出全部变量、日期格式和「只替换正文」等
边界 —— 空态里只举三个例子不足以教会第一次使用的人，已有模板时更不能把说明藏掉。

四个入口，覆盖不同的手：侧栏面板、`/` 菜单里的「插入模板」（写作中途，手不离
键盘）、命令面板的两条命令、文档树右键的「用模板新建子文档」。

**模板目录不在文档树中重复显示。** 当前设置的模板目录及其子文档从树里隐藏，
但文件仍是普通 Markdown，继续参与搜索、链接和 Git 同步，也能从模板面板打开、
编辑、重命名和删除。模板目录为空时不做过滤。

默认键位：插入模板 `Mod+Alt+T`、用模板新建 `Mod+Alt+N`（和新建文档的 `Mod+N`
成一对，多按一个 Alt = 这次带模板）、模板面板 `Mod+Shift+M`（侧栏面板都是
`Mod+Shift+字母`）。**不用 `Mod+M`** —— macOS 上 Cmd+M 是系统级的「最小化窗口」，
抢不过来。

**用模板新建仍然是建一篇「未命名」再就地改名**，和普通新建同一条路
（v0.5.22：别在写之前先逼人想标题），模板只决定内容。模板自己的 frontmatter
会一并带过去 —— 「读书笔记」这类模板一半的价值就在那几个属性上。

### 4.7 思维导图（v0.5.28）

**导图不是新文件，是大纲的另一种编辑视图。**

导图有三条路，这里走第三条：

| 路子 | 数据存哪 | 为什么不走 |
|---|---|---|
| Canvas（Obsidian） | 新的 `.canvas` JSON，存坐标与连线 | 记事本打开是一堆 JSON，坐标从 Markdown 派生不出来 —— 它就成了第二份真源，违反 §0 第 1 条。这正是本文档批评思源 `.sy` 的那点 |
| 自创导图语法 | `.md` 里加标记 | 违反 §0 第 3 条 |
| **结构就是缩进** | 就是现有的标题和列表 | 无新格式、无新语法 |

一个节点 = 正文里的一行（标题 `#` 或列表 `-`）。**布局每次算出来，一个字节都不
额外存**，所以拿 `.md` 去别的软件打开什么都不缺。

**根是谁：正文里只有一个顶层标题、且它就是全部内容时，那个标题当根**，否则才是
笔记本身（v0.7.13）。一篇笔记常常有两个题目 —— 文件名，和正文第一行那个 `#` ——
画成两级就成了「recount → Errors Are Evidence → 真正的内容」：白占一列，还让人
以为那是一层结构。顶层有两个标题、或者标题前面还散着列表项时不能这么收，那时
笔记名是唯一能把它们兜住的东西。让位之后的根是**正文里的真行**，所以它能改字、
能加同级、能删；合成的那个根不能（改名走文档树）。笔记名一直在顶栏上写着，
收掉的只是图上那一格。

**收标题也收列表。** 只收标题的话它就只是大纲面板的图形版；而 AI 吐出来的一堆
要点粘进笔记就是列表，那正是「整理思路」最需要被看清的一层。列表按 Markdown
本来的语义挂在最近的标题下面、彼此按缩进嵌套 —— 这不是我们发明的规矩。

**改动一律走编辑器的 dispatch**（`EditorHandle.replaceLines`），不是「算出一整篇
新正文再整体替换」：后者会让 CM6 把光标、选区、滚动位置全部重算，撤销栈里也
只剩「换掉了整个文档」这么一步 —— 在图上敲错一个字，撤销回去正文就面目全非。
走 dispatch 之后撤销、自动保存、外部改动检测全都免费正确，导图不需要自己那一套。

新子节点的前缀**照抄第一个已有子节点** —— 用户已经在这一层表达过「这里是标题
还是列表」。没有子节点时标题给下一级标题（从空笔记搭骨架这条路要通），列表给
缩进深一级的列表。

键盘按 XMind 的习惯：Enter 加同级、Tab 加子级、F2 改字、Delete 删子树、
方向键走位、空格折叠。**每一条都有等价的鼠标操作**（§0：不能假设有键盘）。
导图整片盖住正文，所以它里面的按键不进全局命令表 —— 否则在导图里按 F2 会去
给看不见的文档树改名。

**节点改字服从普通就地编辑的确认语义**（v0.7.13）：Enter / Tab 确认，Esc 取消，
点背景、另一节点、搜索框、工具按钮或关闭导图也先确认；点搜索框之后焦点必须留在
搜索框，不能又被画布抢回去。新建后还没输入的空节点按 Esc 要撤掉这次创建，不能在
Markdown 里留下光秃秃的 `- `。中文输入法仍在组词时，Enter 只交给输入法选词，
不能提前提交节点；编辑框里的右键保留系统复制 / 粘贴菜单，不弹节点菜单。

背景平移有 **5px 启动阈值**：按下后轻微手抖仍算一次点击，先确认编辑但不移动画布，
越过阈值才显示抓取光标并开始平移。画布快捷键不能从顶栏、模式提示或右键菜单里的
按钮冒出来 —— 按缩放按钮上的 Enter 只能缩放，绝不能同时按导图语义新增同级节点。

**触摸屏上要再补一次同样的账**（v0.7.13）。手机上不存在的不只是键盘：悬停、
右键、滚轮、双击一并没有 —— 于是「双击改字」「滚轮缩放」这些桌面等价物在那边
同样等于不存在。补法是两条：

- **一个菜单管全部动作**（改字 / 加子级 / 加同级 / 勾选 / 折叠 / 回到正文 /
  删除）。桌面上右键点节点打开；没有右键的设备上，节点右端出现一个 `⋯`。
  **不靠长按**：长按菜单是 WebView 合成出来的，行上还有别的手势跟它抢，赌它
  能用不如给一个按得到的按钮。两边只有一套代码。
- **节点上平时一个按钮都没有。** 一张图几十个节点，每个都挂着一排图标的话，
  眼睛先看见的是按钮不是内容；而且它们必须一直占着位置 —— 只在悬停时占的话，
  鼠标一进来文字就重排、高度跟着变，整张图会在指针底下抖。所以 `⋯` 只在没有
  右键的设备上出现（`hover: none` 认硬件，`.mindmap.is-touch` 认平台，
  任一成立即生效）。唯一的例外是任务项的勾选框：它本来就是内容的一部分，
  而且**自己就是按钮** —— 旁边画个方框却点不动，谁都会先去点它。
- **折叠不在节点边缘另挂圆钮。** 那里是拖宽度的连续命中区，两个操作重叠后，
  最常用的结果不是「多一个入口」，而是两边都很难点。桌面用空格或右键菜单，
  触摸设备用 `⋯` 菜单；收起后在节点内部显示一个被动的子节点数量，说明还有内容，
  但不再制造第二个操作入口。
- **平移缩放走 pointer**：单指拖背景、双指捏合，加上工具条里的 `−` / `＋`。
  滚轮留着，但它只是桌面上的加速通道。画布必须 `touch-action: none`，否则手势
  会先被 WebView 拿去滚页面 —— 真机上的表现是「图纹丝不动，整个界面在晃」。

手指按在节点上时**不平移但照样计入手势**：捏合时有一根落在节点上是常事，漏掉
它就成了「两根手指在缩放，图却在跟着平移」。相机的真身放在 ref 里、state 只管
渲染，否则捏合结束顺势转单指平移时会跳一帧。

**节点里的文字换行，不截断**（v0.7.13）。原来一行放不下就收成省略号，而导图上
最需要看清的恰恰是那些长条目 —— AI 吐出来的要点、写给自己的一句话，截一半等于
没有。封顶四行：一整段贴进来的节点会把图撑成一堵竖直的文本墙，那时候该做的是回
正文读。

换行带来的代价是**高度不再是常数**，而一行能放几个字取决于字体、字号和用户调过
的排版设置 —— **算不出来，只能量**。所以布局走两趟：先按上一次的高度排一版，
渲染完在 `useLayoutEffect` 里量一遍 `offsetHeight`，变了就再排一版（浏览器画之前
完成，看不到中间态）。高度只跟内容有关、跟位置无关，所以这个循环一定收敛。
`layout()` 的 `heightOf` 参数不给就一律按 `NODE_H` 排，纯函数测试走的是那条路。

连带的两处：父节点对齐的是那一簇的**中线**而不是顶边（高矮不一之后这两者不再
是一回事），连线也从各自的中线出发；节点宽度被拖过以后，起点必须取实际宽度，
不能继续拿默认宽度算，否则线会从节点内部冒出来。父节点比整簇还高时算出来的 y
会是负的，整片挪回来。

**每个节点的宽度可以独立拖**（节点右边缘）。拖的是哪个框，就只改哪个框；让整图
一起变化会违背直接操作的预期。宽度仍然不是笔记内容，不写进 Markdown，而是按
「笔记路径 + 节点结构身份」落在这台机器的 localStorage 里，换设备不跟随；节点
改名或换父级后回到默认宽度，比把旧宽度误套给另一个节点安全。布局先统计每一列的
最大宽度，再给下一列让位，因此一列里宽窄可以不同，但不会压到后面的节点。拖的时候
仍要除以画布缩放：屏幕上拖 10px，图里不是 10px；双击边缘只复原当前节点。

**导图得能整理结构，而不只是看结构**（v0.7.13）。桌面直接拖节点：落在另一节点
上沿 / 中间 / 下沿分别表示排到它前面、成为它的子级、排到它后面；触摸屏从 `⋯`
选「移动到…」，再点新父级。同级重排保留原始前缀，换父级只调整整棵子树所需的
标题等级或列表缩进；标题与列表不互相暗改，父节点也不能塞进自己的后代。一次移动
最终仍是 `replaceLines` 的一次连续替换，所以 `Ctrl/⌘+Z` 一步就能退回来。右键菜单
同时提供上移 / 下移，让不想精确拖放的人也有确定入口。

**大图的定位分三层。** 选中节点时从根到它的节点和连线一起高亮；键盘或搜索选到
视口外时，相机只平移“刚好够”的距离把它带回来，不抢用户调好的缩放；搜索会淡化
无关支路，并临时展开命中项的折叠祖先，清空后恢复原折叠状态。右键「只看这一支」
可以把局部临时当成整张图，顶端始终有「显示全部」退路。这些都是视图状态，不写进
Markdown；切换笔记时组件按路径重新挂载，上一篇的折叠、选中与相机不会漏到下一篇。

折叠状态也**不再绑行号**：上方多插一行正文时，下面所有行号都会漂移。它按“祖先
路径 + 节点语义 + 同名序号”记临时身份，别处插删不影响；节点自己改名或换父级时
宁可丢掉它原来的折叠状态，也不把状态误套给另一支。折叠徽标显示整棵子树的后代数，
不是只数直接孩子。导图打开时正文的浮动目录必须隐藏 —— 两套导航叠在一起既重复又
会挡节点。

比例按钮显示真实百分比，点一下恢复 100%；「适应」在图比视口小时居中，图更大时
仍给根留出稳定边距。长节点的编辑框改成会随内容长高的 textarea（封顶后内部滚动）。
搜索是 `Ctrl/⌘+F`，导图内的 `Ctrl/⌘+Z` / `Ctrl/⌘+Shift+Z` 直接调用正文编辑器
同一份撤销栈。节点位置和连线必须同帧更新：不能只给 `left/top` 加过渡，否则节点
移动时线会暂时悬在旧位置；动效只用于边框、透明度等不改变几何关系的反馈，并尊重
`prefers-reduced-motion`。

横向树、根在左：中文长标题不撑碍，深层级也不会爆。**有意不做**自由连线画布
（要存坐标）和全库关系图谱（那是浏览与发现，不是整理思路；真要做也是另一个
视图，索引里的 `links` 表已经够用）。

### 4.7.1 结构化草稿台（v0.7.45）

草稿的问题不是「缺一块能随便画的地方」，而是「现在还没想好结构，但又不想
在开始前先取标题、选分类」。自由画板将它解成了精确摆坐标和拉连线；这些动作对
鼠标并不快，手指上更差，而且会生出 Markdown 无法表达的第二份数据。

所以做**自动排版的卡片草稿，不做自由坐标画布**：

- 全库只有一篇普通顶层草稿笔记，frontmatter 里是 `type: scratch`。默认名叫「草稿箱」；
  重名时顺延数字，不征用用户原有的同名笔记。它在文档树和其他编辑器里仍是普通 `.md`。
- 一张卡片就是一行标准 Markdown 标题或列表项；层级就是标题等级或列表缩进。
  卡片可加同级/子级、上下移动、缩进/提升和删除；修改仍走编辑器 `dispatch`，
  因此自动保存、撤销和外部改动检测没有第二套实现。
- 默认键为 `Mod+Shift+Space`。`Enter` 在同级记下下一条，`Tab` / `Shift+Tab`
  改层级，`Alt+↑/↓` 重排。每张卡片同时有 `⋯` 菜单，手指和不想记键位的人不需要拖拽。
  **菜单是卡片外部的 fixed 浮层**，不撑高卡片、不挤动草稿内容；通过 portal 挂到
  `document.body`，并按视口边缘换到卡片的另一侧，不受草稿滚动容器裁剪。
- 选中一张或多张卡片后点「生成文档」，把它们连同子级按原有 Markdown 结构复制进
  一篇新的未命名笔记，随后走文档树的就地改名。草稿原件不自动删：「整理好了」
  不等于「原始想法可以丢了」，要删时由用户显式删。
- 草稿可一键切到§4.7 的思维导图，也可回到普通正文处理多行段落、代码块和图片。
  三种界面是同一篇 Markdown 的投影，不互相导入导出。

### 4.8 行内格式的快捷键（v0.6.15）

`Ctrl+B` 加粗。这一条本该在 M1 就有 —— 它不是「高级功能」，是任何一个写字的
软件的默认预期，缺了它的代价是每次强调都要手打四个星号。同时补齐斜体、行内
代码、高亮、删除线。

**`Ctrl+B` 从「收起侧栏」手里拿了过来。** VS Code 用它收侧栏，但这是笔记软件：
加粗一天用几十次，收侧栏一天不到两次。侧栏挪到 `Ctrl+Alt+B`。

**粗体和斜体共用星号，这是唯一真正难的地方。** 只判断「旁边是不是 `*`」的实现
会在 `***又粗又斜***` 上全错。正确的判据是数**连续星号的个数**：粗体看够不够
两个，斜体看是不是奇数个 —— 偶数个星号全被粗体占了，没有余下的那一个给斜体。
`editor/format.ts` 里那张表和 `format.test.ts` 钉住了全部四种组合。

**没选中东西时插一对空标记、光标停中间，不去猜「当前这个词」。** 中文没有词
边界，`**` 该套住哪几个字是猜不出来的，而猜错的代价是撤销一次再手动改 ——
比自己敲两个星号还慢。

除粗体、斜体外的几种**不抄别家的键位**：那几个键各家都不一样，抄一个大多数人
没有肌肉记忆的组合，不如让它们成一族（`Ctrl+Alt+C/H/X`）。全部走命令表，
所以都能在设置里改（§6.6）。

### 4.9 在渲染好的表格上直接改（结构 v0.6.17，文字 v0.6.20）

表格是 Markdown 里**唯一一种手改源码代价不成比例**的块：插一列要在每一行的
同一个位置各加一根竖线，一张八行的表就得数八遍，还得回头把分隔行也补上。
公式、callout、代码块都没有这个问题 —— 它们改一处就是一处。

所以表格的渲染态上挂了把手：**每个表头右侧和每行第一格左侧各一个六点把手**，
点开是菜单（插入 / 移动 / 删除，列多一条对齐）。把手必须待在单元格已有的空间里，
不能为了列操作在表头上方另撑一条空白工具栏；点阵是通用的「这里可以操作」暗示，
比一条没有方向的细线更容易认出来。改的是文件里的那张表，不是界面状态。

**平时一个把手都不显示，鼠标进了这张表才淡淡浮现；当前经过的行或列再明确亮起。**
不能让所有入口同时抢眼，那会把一张正文表格变成后台管理表（同 §4 折叠箭头那条）。
把手默认只有点阵本身，没有边框、阴影和常驻底色；这些按钮外壳重复一整列后比正文
还显眼。只有悬浮或菜单打开时才给一小块淡底，扩大命中反馈而不增加永久视觉元素。
触屏上没有 hover，那里常显一份更淡的 —— §1.2 反过来的一半：不能假设有鼠标，
也不能假设有 hover。

菜单第一行明确写「第 N 行 / 第 N 列」，下面按**插入 → 移动 → 对齐 → 删除**分组，
删除永远在末尾并使用危险色。做不了的操作整项消失，不留灰按钮。菜单挂到视口边缘时
用实际尺寸决定向下还是向上展开，不靠预估行数；否则增加一个分组标题就可能把最后的
删除项挤到屏幕外。

**列边界可以直接拖宽**（v0.6.23）：鼠标进入表头时，列的右边缘出现一条细拖杆，
左右拖动只改变这张渲染表格的列宽，双击恢复按内容自动分配。拖动是视图操作，不能把
宽度塞进 Markdown（GFM 没有列宽语法，自创一条会违反 §0）；当前编辑会话里即使修改
单元格、表格 widget 重建，宽度也要保留。结构变化导致列数或表头身份改变时恢复自动
布局，比把旧宽度生搬到另一组列上可靠。

**结构和文字都不回源码。** 点把手和点单元格都**有意不动光标** —— 光标一进
表格范围整块就退回源码，而用户刚点的是渲染态上的东西，把他扔进另一个界面
等于这个功能白做。改完仍然是渲染态，可以接着改下一处。

**点一格就地改那一格**（v0.6.20，Notion / 思源的手感）：那一格换成一个
plaintext-only 的 contenteditable，显示这一格的**源码**（`**粗**` 就是
`**粗**`），失焦或按键导航时写回文件、重新渲染。Tab / Shift+Tab 在格子间走，
Enter 走到下一行同一列，方向键顶到格子边缘就跨格，**Tab 走过最后一格自动加
一行** —— 连续录入一张表从头到尾不用碰鼠标。Enter 到底**不**加行：手滑攒出来
的空行比少一个快捷路径烦人。输入法与格内撤销交给浏览器 —— 这只是一格纯文本，
不是把编辑器重写一遍；此前「点单元格退回源码改字」要求用户在一屏竖线里数
格子，实测不好用，是这一版翻案的原因。

代价是格内没有 `[[` 补全、公式 snippet 那套能力 —— 要用它们时用方向键把光标
走进表格（或 `Ctrl+E` 切源码模式），整块照旧退回源码，那条路没有堵。正在编辑
的字在点把手做结构操作时**先收进那次改动**，不会丢。

**每次改动把整张表重排一遍**（`editor/tableOps.ts`）。源码本来是不是对齐的、有
没有首尾竖线、分隔行几个减号，五花八门 —— 在这些形态上做局部插入，结果是一张
越改越乱的表。代价是用户手排的空格会被换成本模块的排法，这是有意的：Markdown
表格源码的可读性几乎全部来自竖线对齐，而人手维护它正是最烦的那件事。列宽按
**显示宽度**算，中文按两格，否则中文表格会全歪。

**做不了的操作不出现在菜单里**（第一列没有「左移一列」、只剩一列时没有「删除
这一列」）。判定只有一处 —— `applyOp` 做不了就返回 `null`，菜单据此过滤，
命令那条路据此报「光标不在表格里」。灰着的菜单项等于让人点两次才知道不行。

**表头那一行也有把手**，菜单里只剩「在下方插入行」—— 一张还没有数据行的表全
靠它才加得出第一行。表头自己删不得：没有表头的 GFM 表格不成立。

键盘那一路是命令面板里的「表格」那一组（七条，**默认一个键位都不绑**：谁也不
值得占一个全局快捷键，要的人在设置里绑）。它和把手共用同一套纯函数，区别只有
一处：命令改完**把光标留在表格里**那一格 —— 从键盘走这条路的人正要接着打字。

### 4.10 正文里的右键菜单（v0.7.31）

在正文里按右键，此前弹的是 **webview 自带的那个**：复制、粘贴、拼写检查、
「在必应中搜索」。一条 Markdown 相关的都没有 —— 而右键正是想加粗、想在这张
表里插一行的人第一个会试的地方。这不是缺一个功能，是那个位置被一份没用的
菜单占着。

**拦了它就得管到底：剪切 / 复制 / 粘贴要自己接。** 这三条是人最确定「它应该
有用」的三条，少了任何一条都会显得像半成品。写剪贴板有 `execCommand` 兜底，
**读没有** —— Chromium 明令禁掉了网页读剪贴板的老路（能读等于能偷走用户刚
复制的密码）。所以「粘贴」失败时必须说出还有哪条路（`Ctrl/⌘+V` 走浏览器
自己的通道，不受影响），不能静默。

菜单按四组分，第一组是三个**二级菜单**（照 Obsidian 的分法，那是这类软件里
最成熟的一套）：

| 二级菜单 | 里面是什么 | 从哪来 |
|---|---|---|
| 文本格式 | 加粗 / 斜体 / 行内代码 / 高亮 / 删除线 | `editor/format.ts`，和 §4.8 的快捷键同一套 |
| 段落设置 | 正文 / 一到三级标题 / 引用 / 无序 / 有序 / 待办 | `editor/paragraph.ts`（新） |
| 插入 | `/` 菜单的全部条目 | `core/slash.ts`，**直接复用那张表** |

后面三组是剪贴板（剪切 / 复制 / 粘贴 / 全选）· 表格（只在光标真的落在表格里
时出现）· 发给终端与折叠。**做不了的操作不出现，不留灰按钮** —— 和 §4.9
同一条；例外是剪切/复制，它们的灰态本身就在说「你还没选东西」。

**「插入」不另维护一份条目表。** 它和 `/` 菜单是同一件事（在光标处插一段
文本），各存一份迟早会分叉；复用之后，设置里隐藏掉的条目在这里也不出现、
用户自己加的条目在这里也有（§4.3 / §6.4）。

**「段落设置」改的是已经写好的那几行**，这是它和「插入」的分界：写完一段
回头想把它变成引用，此前只能手动删前缀再打新的。规则三条 ——
换一种是**替换**不是叠加（`- 一条` → `> 一条`，不是 `> - 一条`）；整段已经
全是这一种时再点一次**变回正文**（和行内格式的开关语义一致，不必去找一条
「清除格式」）；有序列表按选中的第几行编号，一段五行得到 1..5 而不是五个 1。

**每一条都要有图标**，全项目的菜单一律如此（正文 · 文档树 · 思维导图 ·
标签页 · 模板列表）。一列纯文字要逐行读才找得到想要的那条，图标让眼睛能
直接跳过去；图标压成 muted，它是定位用的锚点不是这一行的主角，只有危险项
自带颜色。行内格式那几条右侧还标着**当前生效的**快捷键（命令表是唯一真源，
§6.6 改过键位这里跟着变）。

两条随之而来的约束：菜单项**不许折行**（折了之后菜单宽度取决于它当前摆在
哪儿，而浮层是「先量后放」的，量到的尺寸一变位置就错了）；`fitFloatingMenu`
**量两遍**，第二遍在最终位置上量 —— 加图标那次菜单宽了 8px，贴着右下角打开
就有一小截探出屏幕，正是这个原因。

**右键点在选区外要先把光标挪过去**，点在选区里则保留选区。人的预期是「命令
作用在我刚点的地方」，而不是作用在上一次光标停的位置。

**触摸屏不另做一份。** 手指上这些动作走命令面板（格式那五条本来就是命令，
§4.8/§6.6），而正文里的长按已经被系统的文字选择占着 —— 在那上面再叠一层
自己的菜单，两边都会变得不可靠。

---

## 5. 数学公式子系统 —— 差异化重点


### 5.1 Snippet 引擎（桌面）

格式**刻意与 Obsidian Latex Suite 兼容**，让用户能直接粘贴现有配置过来：

```json
[
  { "trigger": "//",  "replacement": "\\frac{$0}{$1}$2", "options": "mA" },
  { "trigger": "sr",  "replacement": "^{2}",              "options": "mA" },
  { "trigger": "@a",  "replacement": "\\alpha",           "options": "mA" },
  { "trigger": "([A-Za-z])(\\d)", "replacement": "[[0]]_{[[1]]}", "options": "mAr" },
  { "trigger": "dint","replacement": "\\int_{$0}^{$1} $2 \\, d$3", "options": "mA" },
  { "trigger": "mk",  "replacement": "$$0$",              "options": "tA" }
]
```

**options 标志位**

| 标志 | 含义 |
|---|---|
| `m` | 仅在数学模式内触发 |
| `t` | 仅在数学模式外（正文）触发 |
| `A` | 自动展开，无需按 Tab |
| `r` | trigger 按正则解析，`[[n]]` 引用捕获组 |
| `w` | 要求词边界（避免 `sr` 在 `users` 中误触） |

**`w` 比预想的重要得多。** M2 开发中真的踩到：在公式里写变量名 `point`，末尾的 `oint` 命中了环路积分，变成 `p\oint`；`print` 变成 `pr\int`。凡是英文词形的触发词都必须带 `w`，只有那些**故意设计成紧贴前一个 token** 的缩写例外 —— `xsr` → `x^{2}`、`Ainv` → `A^{-1}` 正是它们的用法，加了词边界反而废掉。仓库里有一条回归测试钉住了一串常见标识符（`point` `print` `cost` `variance` …），新增 snippet 忘了加 `w` 会立刻失败。

**tabstop 语义**：`$0` `$1` … 为跳转点，`$0` 是展开后光标落点。Tab 依次前进；不在任何 tabstop 时，Tab 执行 **tabout** —— 跳到最近的右括号 / `$` 之外。这个小功能对手感的贡献被严重低估：写完 `\frac{a}{b}` 不需要按四次方向键。

**Tab 的完整优先级**（M2 实现时定的）：

1. 光标前匹配到**非自动**（无 `A`）的 snippet → 展开它
2. 有未走完的跳转点 → 去下一个
3. 在数学模式里 → tabout
4. 其余 → 交回默认行为（缩进、列表）

第 1 条不是可有可无的。`pmat` 与 `pmat3x3` 是前缀关系：两个都自动展开的话，打到第 5 个字符 `pmat` 就先炸了，带尺寸的那条永远轮不到。把 `pmat` 改成 Tab 触发，两者才能共存。**凡是「短触发词是长触发词前缀」的组合，短的那个都必须放弃 `A`。**

### 5.2 数学模式检测

**M2 实现时修正了这一节。** 原本写的是「不要数 `$` 的个数，查语法树就对了」。前半句的理由成立，后半句不够 —— 语法树有个它自己解决不了的问题。

**正在输入中的公式是未闭合的。** 敲下 `$` 之后、敲下配对的 `$` 之前，文档里只有一个孤零零的 `$`，解析器不会（也不该）为它建出 `InlineMath` 节点。而 snippet 恰恰要在这个时刻工作 —— 打 `$` 然后 `//`，此刻正是最需要判断「我在公式里」的时候。纯语法树方案在这里返回 null，snippet 一条都不会触发。

最终是**混合方案**：

| 步骤 | 判据 | 作用 |
|---|---|---|
| 1 | 语法树命中 `InlineMath` / `BlockMath` | 已闭合的公式，最可靠 |
| 2 | 语法树命中 `InlineCode` / `FencedCode` | **一定不是公式，直接否决** |
| 3 | 本行内数未转义、且不在代码里的 `$`，奇数即在公式中 | 覆盖「正在输入」的未闭合状态 |

**第 2 步是关键**：它让第 3 步的「数 `$`」变得安全。原先反对数 `$` 的理由 —— 代码里的 `$PATH`、代码块里的 `$x`、转义的 `\$` —— 前两者已被上一步挡掉，第三者在计数时按反斜杠奇偶显式处理。

残留的取舍：`$5 起，精装 $20` 这类货币写法，在两个金额之间的光标处会被判成「在公式里」。这**只影响 snippet 要不要触发，不影响渲染** —— live preview 仍按语法树走，不会把那段文字渲染成公式。Obsidian 也是这个行为。

这仍然要求 §4.3 的 `markdownExtended` 先把 Math 与 Code 节点解析出来 —— 它是整条链路的地基。

### 5.3 其他公式能力

- **块级公式对齐辅助**：`$$` 内识别 `align` 环境，Tab 补齐 `&` 对齐点
- **矩阵快速输入**：`pmat3x3` → 生成 3×3 `pmatrix` 骨架，Tab 在单元格间跳
- **符号面板**：`Ctrl+/` 唤起模糊搜索，输入「积分」「叉乘」「属于」列出候选，回车插入 —— 覆盖 snippet 记不住的长尾。候选显示真实 KaTeX 预览，来源是当前生效的内置 + 用户 snippet；插入仍保留 `$0` `$1` 跳转点，和直接敲 trigger 的手感一致。依赖正则捕获组或动态 build 的条目没有真实匹配上下文，不能拿空捕获组伪造预览，面板里不列
- **错误提示**：KaTeX 解析失败时在公式下方显示错误，而不是让整块变红消失
- **输入时预览**：光标一进公式，live preview 就退回源码（§4.2），恰恰是正在编辑的那一刻看不到结果 —— 于是在公式**上方**浮一个 KaTeX 实时渲染的 tooltip，随输入更新（`editor/mathPreview.ts`）。锚在公式起点而不是光标，输入时不横向乱跳；未闭合的行内公式不弹（§5.2 那个货币误判只许影响 snippet 触发）；源码模式下随 PREVIEW 一组整个摘掉
- **公式编号与引用**：`$$...$$ \tag{1}` 与 `[[#eq-1]]`，v1 延后

### 5.4 默认 snippet 库

内置 135 条（希腊字母、常用算符、分式根式、上下标、矩阵、微积分、集合逻辑），用户配置与之合并，同 trigger 时用户覆盖内置。**这份库的质量直接决定第一印象**，值得花整块时间打磨，不要边做边凑。

**这份库是要长期迭代的。** 盲测通过只说明它够用了，不说明它调好了 —— 触发词顺不顺手只能靠日常使用暴露。待办按发现顺序记在这里：

| 待调 | 说明 |
|---|---|
| `\text{...}` 内部仍算数学模式 | 在里面写英文句子会误触。根治要解析 LaTeX 内部结构；Latex Suite 也没解决 |
| 货币写法的误判 | `$5 起，精装 $20` 之间的光标处被判成「在公式里」。只影响触发，不影响渲染 |
| 触发词冲突检查 | 新增 snippet 时自动报告「与已有触发词构成前缀关系」，避免重演 `pmat` 那个坑 |

### 5.5 移动端公式输入（独立设计）

桌面那套在手机上完全不成立，需要另一套交互：

- **工具条只在焦点落在正文里时出现**（v0.7.21 改）。原来跟着「有没有打开的
  笔记」走，于是读一篇笔记也要白让出 76px —— 390×844 的屏上是一屏的 9%，
  而读的时候那些键一个都用不上。**这一条的判据是焦点，不是键盘**：焦点在
  编辑器里就是在打字，而键盘可能被用户手动收起来（那时工具条仍然该在，它
  是回到公式的入口）。工具条自己也算「在编辑」（切分页那一下不该让整条栏
  消失）。下面那条「底部让位」问的是另一件事 —— 屏幕还剩多少 —— 所以判据
  也不同。
- **它排在最底下，不是「状态栏上面」。** 要贴着软键盘的上沿，而键盘顶掉的是
  视口底部 —— 中间的任何一行都会把它推离键盘一截。
- **键盘弹起来时，底部导航和状态栏一起让位**（v0.7.25 改）。三条叠着是
  52 + 34 + 52 = 138px，加上键盘之后，390×844 的屏上留给正文的只剩两百出头
  —— 比底部这堆还少。

  这条以前是反过来的（「打字时导航不让位」），当时的理由是**判据不可靠**：
  藏它的条件若是「焦点在正文里」，而收起软键盘并不一定让正文失焦，用户就会
  落到「既没有键盘也没有导航」那一格里。

  判据换成**视口真的缩了没有**之后那个陷阱就不存在了（`lib/keyboard.ts`）：
  安卓侧 `MainActivity.fitSystemBars()` 把 IME 的 inset 加成 WebView 的底部
  内边距，键盘一上来可视高度就真的少一截，一收同帧涨回来，两条跟着回来。
  基线取见过的最大高度，宽度一变（转屏）就重置 —— 否则横屏会被一直判成
  「键盘开着」。缩不到一个键盘那么多（120px）不算，地址栏和手势条都在这个
  数以下。**量不出键盘的设备上不让位**，界面退回原来的样子：判据失灵时
  宁可挤一点，也不能把导航藏起来找不回来。
- **工具条只有一行（52px）。** 分页标签原来单占一行，而页名平时不需要看见
  —— 只有想换一页时才需要。收成行首一个写着当前页名的胶囊，点开才列出全部
  页：换页从一下变成两下，换来的是每一次打字都省下的 30px。菜单靠 window 上
  的 pointerdown 关，**不加盖屏的遮罩** —— 遮罩要么盖住工具条自己（换完页
  去点符号得先被吃掉一下），要么为了不盖它而给那一行加 z-index，而那会把
  菜单和长按变体一起夹进这条栏的层叠上下文，被底部导航盖掉一半。
- **软键盘上方常驻工具条**，分页：
  - 页 1：`$` 切换公式模式、上标、下标、分式、根式、括号
  - 页 2：希腊字母
  - 页 3：算符与关系符（`\sum` `\int` `\leq` `\in` `\forall` …）
  - 页 4：矩阵与环境
- **tabstop 导航按钮**：工具条最右侧常驻 `◀ ▶`，替代 Tab 键在占位符间跳转
- **长按出变体**：长按 `\int` 出 `\iint` `\oint` `\int_a^b`
- **最近使用**：工具条第一格自动填充最近用过的 8 个符号 —— 实际使用中命中率极高
- **手写识别**：不做（v1）。第三方 API 会违反本地优先原则，本地模型体积不可接受。

**v0.6.3 落地时定下来的几条**：

- **插入的写法和 snippet 完全一致**（`$1` `$2` 是跳转点，`expand()` 认它）。
  于是工具条插出来的东西和用户自己敲展开出来的**在编辑器眼里没有区别** ——
  `→` 走的就是 Tab 那条路（`tabSpec`），不用维护第二套跳转逻辑
- **按下时必须 `preventDefault`**。不拦的话焦点会从编辑器挪到按钮上，软键盘
  随之收起、光标丢失 —— 点一个 `α` 的代价是键盘弹上弹下一轮。这条在桌面上
  完全看不出来，是移动端最容易漏的一类
- **长按出变体，但手指一动就取消**。横滑工具条是这一条上的主要操作（一页
  放不下），滑动时手指必然在某个键上停留超过 500ms —— 不取消的话每滑一次
  都弹一个面板
- **变体面板要夹回屏幕内**。它挂在被长按那个键的正上方，而 390px 宽的屏上，
  一行最后两三个键会让它整个伸出去。只能等它渲染出来再量宽度，用
  `useLayoutEffect` 在绘制前挪好
- 「最近用过」存的是 `insert` 不是整个对象：以后改了某个键的键面，用户那边
  不该一直留着旧的

---

## 6. 视觉与排版规范

### 6.1 排版尺度（美观的实际来源）

| 项 | 桌面 | 移动 | 说明 |
|---|---|---|---|
| 正文字号 | 16.5px | 17px | 中文比英文需要更大字号 |
| 行高 | 1.85 | 1.85 | 中文密度高，1.5 会显得拥挤；设置中可调 |
| 内容宽度 | `max-width: 42rem`（约 34 汉字） | 满宽 - 20px 边距 | 超过 40 汉字眼睛回扫会丢行 |
| 段间距 | `0.35em` | 同 | 独立于行高、设置中可调；段内换行不增加 |
| 标题层级 | 1.0 / 1.25 / 1.5 / 1.85em | 同 | 层级靠字重和留白区分，不靠字号暴涨 |
| 中文字体 | 思源黑体 / 苹方 / 微软雅黑（按平台回退） | | |
| 西文字体 | Inter（正文）、JetBrains Mono（代码/公式源码） | | |

正文统一使用可调行高。普通 `Enter` 产生的正文行之间另加可调段落间距；间距使用
CodeMirror 块级 widget，不修改文字行盒，避免光标与输入位置错位。`Shift+Enter`
插入 Markdown 硬换行（行尾两个空格加换行），只使用正文行高；代码块内仍插入普通
换行，不能把 Markdown 控制字符写进代码内容。浏览器自动折行同样只使用正文行高。

### 6.2 色彩

不用纯黑纯白 —— 纯白背景长时间阅读刺眼，纯黑在 OLED 上文字边缘发虚。

```css
@theme {
  --color-bg:      oklch(98.8% 0 0);       /* 纸张，不是纯白 */
  --color-surface: oklch(96.8% 0 0);       /* 侧栏 */
  --color-rail:    oklch(94.8% 0 0);       /* 图标栏再退半层 */
  --color-text:    oklch(21% 0 0);         /* 石墨，不是纯黑 */
  --color-muted:   oklch(48% 0 0);
  --color-border:  oklch(88.5% 0 0);
  --color-accent:  oklch(52%  0.11  195);  /* 唯一强调色，默认青绿 */
}
```

黑白灰不是把一切压成同一块灰：正文、侧栏、图标栏依次降一档明度，浮层和当前
标签回到亮面。层级主要靠这四块中性材质、留白和半像素影子完成，分隔线只负责
收边。这样有论文工作台的克制，也不会因为「少用颜色」变成一张没做完的线框图。

**只用一个强调色。** 多彩配色是业余感的最大来源。层次靠明度和留白拉开，不靠颜色。深色主题用同一套 oklch 色相、翻转明度即可 —— 色相一致保证两套主题观感统一。

**颜色只做重音，落到实处是这几条**（v0.5.32 把跑偏的地方收了回来）：

| 用主题色 | 不用主题色 |
|---|---|
| 链接、焦点环、光标 | 标签（`#tag`、属性条上的标签）—— 那是**内容**，不是交互 |
| 选中项左边那根竖条 | 图标栏选中态的底 —— 中性灰底就够了，「就是它」交给竖条 |
| 复选框勾上之后 | callout 的整块底色 —— 颜色收在左边那条线和图标上 |

判断标准很简单：**这块颜色说的是「你可以点它」还是「它是什么」？** 前者留，
后者退成中性。一屏黑白灰里，一块青绿填充永远是最抢眼的东西 —— 它不该被一个
状态占用。

**但「退成中性」不等于「退成灰块」**（v0.5.33 修正）。中灰底配黑字是最廉价的
一种块，那正是「办公软件」的观感。简洁的贵气来自三样东西，都不是「少用色」：

1. **极淡的彩底 + 同色系深一档的字**。标签用 9% 的主题色底、38% 明度的同色字
   （`--accent-wash` / `--accent-ink`）—— 几乎不占视觉重量，却让它「成块」
2. **该没有底的地方就一点底都不要**。callout 去掉底色，只留左边一条类型色的
   线和带色的标题 —— 一整块底会把旁注变成「提示框控件」
3. **饱和度不能低到发灰**。主题色的 chroma 从 0.085 提到 0.11：低饱和不等于
   高级，**发灰才是真的不高级**

同一条原则也管着字：正文永远是 `--text`，只有链接、当前项、勾选态才带色。

**唯一的例外是语义色**（`--warn` / `--danger` / 代码高亮 / 状态分档）。它们的
色相写死，不跟着主题色走：「危险」「完成」这类含义不能因为用户把主题调成橙色
就变成另一种意思。判断标准仍是那一条 —— 这块颜色说的是「它是什么」，而这一类
「是什么」正是需要一眼扫出来的那种。详见 §2.10 的状态四档。

### 6.3 布局

- **桌面** 图标栏 ｜ 侧栏 ｜ 编辑区。侧栏用左侧图标栏切换视图（文档树 / 搜索 / 标签），点已选中的图标收起它
  - 原计划是「文档树/搜索/标签」并排成一栏、右边再加一栏大纲/反向链接。实际做下来并排放不下 —— 三块内容各自都需要纵向空间，挤在一栏里每块都只剩几行。改成一栏多视图之后每个视图都能占满高度，图标栏还顺带解决了动作按钮堆在标题栏里的问题
  - **搜索从弹窗改成了侧栏面板。** 弹窗会挡住正文，而搜索结果恰恰是要一条条点开看的 —— 每点一次就得重新打开弹窗、重新输入。放侧栏之后结果留在原地
  - 反向链接仍挂在编辑器底部，没有独立的右栏
  - **大纲做了两个入口**：侧栏的第四个视图（与文档树/搜索/标签并列），以及贴在正文右侧的**浮动目录** —— 收起时只是一排右对齐的短横线，长度按层级递减；鼠标移上去才展开成标题文字。两个都要的理由是：侧栏一次只能显示一个视图，而写长文时最想同时看见的恰恰是「文档树」和「我在第几节」。浮动目录不占版面，正好补上这一格。少于两条标题时不显示 —— 那不叫目录，只是一块挡视线的东西。侧栏大纲在标题前放一个 **16px 的数字圆点**，直接标出真实的 Markdown 标题等级（1–6）；缩进只负责表达父子关系，而且每层不得超过 12px，不能让层级标识反过来挤掉标题正文。当前标题仍沿用全局的中性选中底与左侧主题色细线，只有等级圆点使用极淡主题色强调。两个入口里的标题都按安全的行内规则渲染，尤其标题里的 `$…$` 必须显示成 KaTeX，不能把 `\widehat` 一类源码直接摊开；公式块本身永远不进大纲，其中独占一行的 `=` 也不能把上一行 LaTeX 误认成 setext 标题
- **移动** 单栏 + 抽屉：编辑区占满，抽屉盖在正文上，图标栏横过来钉在底部（v0.7.21 落地）
  - **图标栏必须从左边挪走。** 竖排在手机上要付两笔账：横向白白扣掉 44px（390px
    的屏上是 11%），而且整条落在屏幕左上角 —— 单手握持时最够不到的地方。
  - 代价是一行排不下十二个图标。划分**按语义不按频率**：条上只放视图组
    （文档树 / 搜索 / 标签 / 模板 / 动态 / 大纲），动作组（源码模式 / 导图 /
    项目中心 / 终端 / 命令面板 / 设置）收进最后那个 `⋯` 弹出的面板。理由是
    选中态 —— 把两个视图塞进弹出层的话，切到「标签」时整条栏上没有任何一处
    亮着，用户会以为没切成功。
  - 弹出面板里的动作**带文字**，不是又一排图标：这些动作彼此不同类，光看图标
    猜不出来，而弹出层有横向余量写得下四个字。
  - 抽屉停在底部导航的上沿，不铺到 `bottom: 0` —— 铺过去会压着那排图标；
    导航靠 z-index 仍能点，但看着是「抽屉从图标底下钻过去」。
  - 标签页在窄屏收到 132px（桌面 168），并把「新建」按钮 `sticky` 在右端：
    跟着标签条一起滚的话，标签一多它就滚出屏幕，而那是手机上新建笔记最短的路。
- 编辑区**内容居中且限宽**，窗口拉宽时留白增加而不是行变长 —— 这是 Obsidian 默认体验最刺眼的问题之一
- 标签栏按「薄纸片」处理：未选中项没有描边，当前项用亮面、极淡投影和底部 2px
  主题色标记。不要画成浏览器那种一格挨一格的页签，也不要靠大块主题色表示当前项
- 所有右键菜单都按**渲染后的真实尺寸**收进视口：靠右或靠下时自动向内翻转；菜单
  本身高于可用区域时限制高度并内部滚动。不能按菜单项数量预估高度，也不能让应用
  外壳的 `overflow: hidden` 把末尾操作（尤其删除）裁掉
- 动效：只在 120–180ms、`ease-out` 范围内。超过 200ms 会让软件显得慢

**圆角规则**：凡是有背景或边框的「盒子」都要有圆角，**只有贴着窗口边缘的那一边除外** —— 贴边的圆角会露出后面的桌面/窗口背景，看着是脏而不是精致。所以侧栏、图标栏、状态栏保持方角（三面贴边）。终端吸底时只圆两个上角；靠右时从窗口顶边通到底边，是结构分栏，四角都保持方角、只用左侧细线分隔。若只圆靠编辑区的两个左角，另外两个贴窗角又必须是方的，整块会明显向左鼓出。

四档就够，写成令牌，不要现填数值：

| 令牌 | 值 | 用在哪 |
|---|---|---|
| `--r-xs` | 4px | 行内小件：标签、`kbd`、行内代码 |
| `--r-sm` | 6px | 列表行、按钮、输入框、引用块 |
| `--r-md` | 8px | 代码块、表格、内嵌卡片 |
| `--r-lg` | 12px | 浮层：命令面板、设置、补全菜单 |

尺寸越大的盒子圆角越大 —— 同一个半径放在 24px 的按钮和 600px 的面板上，观感完全不同。

### 6.4 设置系统（M4 落地）

**设置存在应用配置目录，不在 `.verso/` 里。** 设置是跟着人走的（字号、主题、自己写的 snippet），换一个 vault 不该重来一遍；而且 `.verso/` 按第 6 条铁律必须能整个删掉重建，放设置进去就违规了。文件是 `settings.json`，与 `recent.json` 同一目录。

**落地方式是把设置写成 CSS 变量挂在 `<html>` 上**，而不是把值逐个传给组件。理由：排版尺度散布在侧栏、编辑器、终端和各种浮层里，传参一定会漏；而 CodeMirror 的主题本来就全部走 CSS 变量，顺着这条路走，深浅主题和字号调整都是免费的。

设置面板采用**左侧分类、右侧内容**的结构（v0.6.19）。八个分类横排会迫使短标签、
中英混排和很小的点击区域同时出现；竖排导航允许使用完整中文名称，当前页再用标题、
一句范围说明和组标题建立层级。窄屏退回横向可滚动分类栏，内容行改成上下排列。

设置文案只说明三件事：**设置对象、直接效果、必要限制**。不在界面里解释实现过程，
不使用「悄悄问一次」「自作主张」「合上电脑就走」这类口语，也不把设计判断写成对
用户的劝说。按钮使用明确动作（「启用」「停用」「删除」「应用更改」），避免「记 / 不记」
这类脱离上下文就失去含义的短词。

唯一的例外是 **xterm**：它的配色和字体是建终端时传进去的 JS 对象，不参与 CSS 级联。所以要（a）把 CSS 变量的**计算值**读出来再交给它 —— 直接塞 `var(--term-font)` 会让它的字符宽度测量失败、整屏错位；（b）用 `useEffectiveTheme` 把「系统换主题了」变成一次 React 重渲染。

主题三态用 `data-theme` 属性：`system` 不打属性、走 `@media (prefers-color-scheme)`，明确选了浅/深才用 `:root[data-theme=…]` 盖过去。**因此「跟随系统」不需要监听任何事件。**

**数值设置前后端都夹紧（clamp）而不是报错。** 字号填成 0 之后，唯一能改回来的地方恰恰是那个已经看不见的设置界面 —— 宁可悄悄夹紧，也不要把人锁在外面。`NaN` 要单独挡一道：它的所有比较都是 `false`，普通范围检查穿得过去。

**自定义 snippet 逐条校验，坏的跳过并报告、好的照常生效。** 用户的 snippet 表会有几百条，为第 87 条的拼写错误让前 86 条失效，等于让人在最熟的输入法上突然失去手感，而且完全不知道为什么。格式与 Latex Suite 相同（§5.1），存成原始文本、由前端解析 —— 不在 Rust 侧再建一份会各自漂移的 schema。**存储格式不等于编辑界面**（v0.6.26）：日常设置必须是逐行表格，直接编辑触发词、展开内容、范围、自动/正则/词界、优先级与说明；JSON 只留在收起的 Latex Suite 迁移入口里。旧设置打开时自动转换成表格，应用后仍写回兼容格式。

### 6.5 macOS 适配的边界

已做且可验证的部分：快捷键提示按平台渲染（`Mod` → `⌘`，Mac 上用符号且不加分隔符）、字体回退栈含苹方、`-webkit-font-smoothing`。

**没做**：透明标题栏 + 交通灯留白。它需要在真机上看红绿灯有没有压住侧栏内容，而这台开发机是 Windows。留着默认的原生标题栏是安全的 —— 布局不会错，只是不够精致。等能在 Mac 上跑起来再说，不要凭想象改窗口装饰。

### 6.6 快捷键 —— 命令表是唯一真源

**每一条命令都可以被绑快捷键，键位不写死在派发逻辑里。** 命令表（`App.tsx` 的 `commands`）给出命令 id、名字、分组和一个**默认键位**；用户在设置里改过的记进 `settings.keybindings`（只存与默认不同的那几条，空串 = 显式解绑），派发、命令面板里的提示文字、图标栏的 tooltip 全部从这一份算出来。

这样做的直接理由是：以前键位写在一条 `if / else if` 链里，而提示文字在命令表里**又写一遍**，两处迟早对不上；顺带的结果是「加一条命令」和「让它可改键」变成同一件事。**只存差异**这一点也是有意的 —— 将来调整某条命令的默认键位时，没动过它的人会跟着一起变，而不是被自己的配置钉死在旧值上。

**键位认的是 `KeyboardEvent.code`（物理按键），不是 `.key`（字符）。** `.key` 在按下 Shift 时报的是上档字符（`Shift+[` 报 `{`），录出来的键位和键帽对不上；中文输入法激活时它还可能变成 `Process`，那会让所有快捷键在打字时随机失灵。`Ctrl` / `Cmd` 一律折成 `Mod` 存储，Mac 上存的配置到 Windows 才认得。

**改键位靠录制，不靠填字符串。** 让用户填 `Mod+Shift+O` 的前提是他知道这个软件管 Ctrl 叫 Mod、管方向键叫 ArrowUp —— 他没有任何办法知道。**撞键不拦，只标红**：改键位天然是「先占上、待会儿把原来那个挪走」的来回过程，拦下来这个过程根本走不完。不带 Ctrl/Alt 的普通键**不收**（F1–F12 除外）：那种键绑上去会把正常打字吃掉，而用户发现问题时正在编辑器里，第一反应是「这软件坏了」。

**全局快捷键挂在 capture 阶段并掐断传播**，因为 CodeMirror 自己也绑了 `Mod-s`、`Ctrl-Shift-[`；不掐断的话两处各跑一遍（折叠那条正好互相抵消，表现成「按了没反应」）。两个例外：浮层（`.overlay`）里的按键归浮层自己管 —— 否则设置里正按下的那个组合键会顺手把对应命令也执行一遍；**终端里键盘归 shell**（`.term`）—— `Ctrl+N/P/E/B` 在 readline、vim、tmux 里各有各的意思，被界面截走的话终端就只剩半个，而在终端里跑 AI CLI 正是这个软件的主路径（§7）。终端里只放行**对象是终端面板自己**的那些命令（`term.*`：开关面板、换停靠位置、把笔记或选区发进来）加上命令面板 —— 判据是「这条命令作用在终端上」而不是一张越列越长的白名单。「把笔记发给终端」尤其不能挡：送完一次焦点就落在终端里（§7.6），挡掉的话再送第二篇就得先点回正文，把刚省下的那一步又加了回来；而这几个键位本来也不在 shell 的键位空间里。

---

## 7. 终端与 AI 协作

### 7.1 为什么这件事比听起来重要

vault 是一堆纯 `.md` 文件 —— 这意味着**任何 AI CLI 工具（Claude Code、aider 等）都能直接在上面工作**，Verso 不需要提供任何 AI 接口、插件或 API。

这是 §0 原则 1（数据可脱离本软件存在）一个意料之外的回报，也是对「要不要做 AI 功能」更好的回答：**不是我们做 AI 功能，而是让用户自带 AI。** 好处是我们不需要预判用户想要什么能力、不绑定任何模型、不处理 API key、不承担模型能力变化的维护成本，也不会因为某个内置 AI 功能做得差而拖累产品口碑。

而 AI 在纯 markdown vault 上能做的，恰好是做成内置功能会极其昂贵的那类事：批量整理笔记结构、生成索引页与摘要、修复失效链接、批量改写 frontmatter、把一堆散记合并成一篇、翻译、生成 `verso-view` 查询。

**如果内容存在数据库里（Notion / 思源 的路线），没有任何 AI 工具能碰它。** 这一条应当写进对外宣传。

### 7.2 与 Git 的乘法效应

单独看，放一个 AI 在你的笔记上跑批量修改是件可怕的事 —— 它可能改坏 50 个文件而你毫无察觉。

但 §2.8 已经让 vault 成为 git 仓库，于是：

```
AI 跑之前   工作区是干净的
AI 跑之后   git diff 一眼看清它改了什么
不满意      git restore .  全部回退
满意        正常同步
```

**终端和 git 互相放大**：git 让 AI 编辑变得可审阅、可回滚；终端让 git 和 AI 都触手可及。缺任何一个，另一个的价值都要打折。所以这两件事应当在同一个里程碑落地。

UI 上要把这层关系呈现出来：终端面板旁常驻一个「未提交改动 · N 个文件」的入口，点开即 diff。

### 7.3 两种实现，分两步走

**方案 A：调起系统终端**（M1，成本几乎为零）

快捷键把系统默认终端（Windows Terminal / iTerm2 / Terminal.app）拉起来，cwd 设为目标目录。一个 `Command::spawn` 的事。先有再好。

**方案 B：内嵌终端面板**（原定 M5，已提前到 v0.2.1 实现）

底部可折叠面板，`Ctrl + \`` 切换（沿用 VS Code 的肌肉记忆）。面板只占编辑区下方，侧栏保持通高 —— 与 VS Code 一致。

| 层 | 选型 |
|---|---|
| 前端 | `@xterm/xterm` + fit / webgl / web-links / search 插件（VS Code 用的同一套） |
| 后端 | `portable-pty`（WezTerm 出品）—— Windows 走 ConPTY，macOS/Linux 走 openpty |
| 传输 | PTY 输出经 Tauri event 推给前端 |

**真正的成本在这些细节上，不在 xterm.js 本身**：

| 问题 | 方案 |
|---|---|
| 输出洪水（`cat` 大文件、AI 流式输出）淹死 IPC | Rust 侧按 16ms 时间窗聚合再推送，单帧上限 64KB，超限则丢弃并标记 |
| resize 同步 | 前端 fit 后把 rows/cols 传回 Rust，调 `pty.resize()` |
| 进程生命周期 | 关闭面板**不杀进程**（AI 任务可能正在跑）；退出应用时提示有活动进程 |
| Windows ConPTY | 与 Unix PTY 行为差异大，需单独测试；shell 默认取 `pwsh` → `powershell` |
| 复制粘贴 | `Ctrl+C` 有选区时是复制、无选区时是中断 —— 这个细节做错，终端就废了 |

**实现时才发现的两个坑**（都会导致「面板打开了但一片空白」，且都不报错）：

1. **DSR 握手的时序。** shell 启动时先发 `ESC[6n`（问光标在哪），**收到回答才打印提示符**。
   xterm 会自动生成回答，但那份回答从 `term.onData` 出来 —— 若等 `pty_open` 返回后
   才注册 `onData`，这段窗口里的回答就丢了，shell 永远停在等待状态。
   **`term.onData` 必须在建立 PTY 之前注册**，PTY 未就绪时把输入攒起来后补。
2. **初始 fit 的时机。** 不能在 `term.open()` 之后立刻 `fit()` —— 面板刚插入 DOM，
   布局未完成，算出来是 0 列 0 行，PTY 会以 2×2 启动。用 `ResizeObserver` 统一处理
   「首次布局、拖拽改高、窗口缩放」，并且**等拿到真实尺寸再开 PTY**。

**与原设计的一处偏离**：上表写「关闭面板不杀进程」，实现改成了**杀掉**。
留一个用户看不见也管不到的孤儿 shell 比中断一个任务更糟；想让 AI 长跑就别关面板。

**cwd 规则**：**默认 vault 根目录**。原本写的是「当前笔记所在目录，根目录作可切换项，待实测后定」——
实测答案是反过来的：终端里跑的多半是 AI CLI，而它得看见整个 vault 才做得了跨笔记的事
（改链接、并散记、生成索引）；起在某一篇的目录里，第一件事永远是 `cd ..`。

**停靠位置：吸底或靠右**（v0.7.7）

吸底是 shell 的形状 —— 敲一行命令、看几行输出。但 §7.1 那条主路径里跑的是 AI CLI，
它的输出是一条不断往下长的会话流，吃的是**竖向**空间：吸底面板要么矮到只剩八行，
要么把正文挤没。所以终端可以改成靠右竖排，与编辑区左右分栏。

这不是补做一个「AI 对话框」，**恰恰相反 —— 它说明那个对话框不必做**：别的编辑器
要装插件才能得到「一边看文稿一边看 AI」，是因为它们的终端和编辑器之间没有别的通道；
而这两样东西本来就在同一个窗口里，缺的只是一个停靠方向。

- 靠右时终端从标签栏那一行直接通到底 —— 竖向空间正是换布局的理由，不该再分给别的东西
- **两个方向各记各的尺寸**：「面板高 280」和「分栏宽 280」之间没有可比性，共用一个数的话每切一次都要重调
- **窄屏强制吸底**：左右分栏在窄窗口上等于把正文压成一条缝。窄屏只是不让它靠右，**不改用户存的偏好** —— 窗口拉宽回来，靠右还在
- **切换停靠不能重建终端**：那等于把跑着的 AI 任务杀掉。实现上是同一个 DOM 节点换到另一个网格区域，xterm 实例和 PTY 原样留着

**最大化与会话数。** 终端标题栏提供「最大化到编辑区」：它临时覆盖正文区，保留标签栏、侧栏与图标栏；再次点击回到原有的吸底或靠右布局。这个状态不跨会话保存，也不能触发 xterm 或 PTY 重建，否则正在运行的 AI 任务会被中断。首版只保留**一个**与当前 vault 绑定的内嵌终端，不增加“新建终端”：多会话需要再定义上下文归属、进程生命周期和会话切换，当前没有足以抵消复杂度的需求；需要独立会话时，仍可使用「在系统终端中打开」。

**移动端不做终端。** iOS/Android 没有可用的 PTY，移动端直接隐藏入口。

### 7.4 对文件监听的影响 —— 优先级要提前

有了终端，「文件被外部程序修改」从**偶发边界情况**变成了**日常主路径**：AI 每跑一次就是一批外部修改。

因此 §2.7 的外部修改处理必须提前：

- **M1 就要有最简版**：窗口重新获得焦点时比对已打开文件的 mtime，变了就提示或重载。成本很低，但没有它，用完 AI 回到编辑器会直接覆盖掉 AI 的修改 —— 这是会丢数据的 bug。
- 完整的 `notify` 监听 + 去抖仍在 M3。
- AI 一次改动几十个文件时，索引要能承受突发批量更新，不能逐文件全量重建。

### 7.5 一条安全边界

内嵌终端拥有完整 shell 权限。这本身没有问题 —— 那是用户自己的机器、自己主动敲的命令。

但必须守住一条：**终端只能由用户主动触发，永远不能由笔记内容触发。**

具体说，**不做「代码块旁边一个运行按钮」这类功能**。因为笔记是可以被分享、被发布、被从别人那里拿来的（§2.9），一个能执行笔记里代码的软件，等于把「打开一篇笔记」变成了「运行一个陌生程序」。

同理：AI 工具由用户自己安装和调用，Verso 不代为下载、不代为执行、不内置任何模型或 API key。

### 7.6 把上下文交给终端

装一个 VS Code 那类的 AI 插件，真正买到的东西不是聊天框，是**它知道你正开着哪一篇、
选中了哪一段**，一个键就把这份上下文塞进对话。聊天框只是那份上下文的容器，
而容器我们已经有了（§7.3 的靠右终端）。所以只补那一个键：

| 命令 | 送出去的东西 |
|---|---|
| 把当前笔记发给终端 | `@相对路径` |
| 把选中内容发给终端 | `@相对路径` + 换行 + 选中的原文 |

- **只插入，不回车。** 送过去的是上下文不是指令 —— 指令由用户接着自己打。代发一条命令
  就越过了 §7.5 那条边界：终端里跑什么，必须是人自己按下去的
- **送进去的永远是一行，且不含任何控制字符。** 这是条无条件的不变量，不是按情况
  调整的策略 —— 它换来的是：这个功能**无论对面跑着什么都提交不了任何东西**。

  中间试过一版「看对面开没开括号粘贴模式（bracketed paste）再决定留不留换行」，
  两次被现实打脸（都在 v0.7.7 开发中实测到）：

  | 对面 | 括号粘贴 | 结果 |
  |---|---|---|
  | PowerShell 提示符 | 没开 | `\n` 原样送过去就是回车，`@项目/Latent.md` 那一行被 shell **执行**了，回了个 ParserError |
  | Codex 的 TUI | 开着 | xterm 的 `paste()` 里那道 `prepareTextForTerminal` **总是**把 `\n` 换成 `\r`，连括号粘贴里的也换；而 `\r` 在它的输入框里是「发送」—— 那一行被当成整条消息提交了出去 |

  教训是同一个：**对面怎么理解换行，我们猜不准也不该猜。** 于是一个换行都不送。
  代价是多行选区（列表、代码）会被并成一行，但引文前面那条 `@路径` 已经把出处
  给了对面，它自己能去读原文。控制字符（含 `ESC`）一并去掉：笔记可以是分享来的、
  也可以是 AI 生成的（§2.9），而一段 `ESC[201~` 就能提前关掉括号粘贴、把后面的
  内容从「文本」变成「按键」——那正是 §7.5 那条边界的另一种破法
- **`@` 是可改的前缀，不是硬编码。** Claude Code 和 Codex 都用 `@` 引文件，但 §7.1 说了
  不绑定任何工具 —— 前缀在设置里，用别的 CLI 的人清空它就得到裸路径
- **给 vault 相对路径**：终端的 cwd 就是 vault 根（§7.3），绝对路径既长，又把用户的
  目录结构一并泄进了对话
- **终端没开就先开**，但 PTY 起来要几十毫秒，这期间送出的文本必须**排队补发**——
  丢了的话「关着终端时按这个键」就是个哑操作，而它恰好是最自然的用法
- 「有选区就发选区、没选区就发路径」这种合并**不做**：两条命令各绑一个键，
  按下去的人得知道自己会得到什么

### 7.7 让 AI 看得懂这个 vault

§7.1 说「让用户自带 AI」，但自带的那个 AI 打开 vault 只看见一堆普通 markdown。
它猜不出来的东西恰恰是最容易被它改坏的：

| 它会怎么做 | 因为它不知道 |
|---|---|
| 把 ` ```verso-view ` 当成看不懂的 YAML 重排，或者「顺手清理掉」 | 那是一张 database 视图的定义（§2.6） |
| 给每篇笔记补齐 `id` / `title` / `created` | §2.3 定的是「Verso 不往你的文件里写任何你没要求的字段」，AI 补的每一行都是噪音，还会在逐行 diff 里淹掉真正的改动 |
| 把 `.verso/` 当成要维护的东西 | 那是可整个删除的派生数据（§0 铁律 6） |
| 顺手 rebase 一下把历史理干净 | 那条历史是用户在侧栏里读的一条直线（§2.8） |

**但这件事不需要我们做「识别」。** AI CLI 早就会自动读仓库根目录的约定文件 ——
Codex 等读 `AGENTS.md`，Claude Code 读 `CLAUDE.md`。我们要做的只是把那份文件
放进去。这是 §7.1 的自然延伸：**不提供 AI 接口，只提供上下文**。它们在磁盘上仍是
普通 Markdown，会被 git 追踪、跟着同步走，换台机器、换个 CLI 都还在；但在 Verso
里属于**仓库基础设施**，不是用户的笔记。

- **和 `.gitignore` 同一条规矩**（`vault/git.rs` 那条路）：打开 vault 时缺了就补，
  **已经有了绝不覆盖**。用户很可能在里面加了自己的话 —— 那正是这份文件该有的
  样子。想要最新版的走「设置 → AI 协作 → 恢复默认说明」，那是**唯一**会覆盖的路径；
  覆盖前必须明确提示自定义规则也会被替换，并让用户确认
- **根目录里的两个精确文件名不进入普通文档树及其下游索引**：快速切换、全文搜索、
  标签和 database 都不应把基础设施当成笔记。只隐藏 vault 根的 `AGENTS.md` / `CLAUDE.md`，
  子目录里同名的文件仍按普通文档处理，避免吞掉用户内容
- **入口在「设置 → AI 协作」**：那里解释两个文件各自的职责；「打开并编辑」直接在
  正常编辑器标签中打开根目录那份真实的 `AGENTS.md`，不在 `.verso/` 或设置数据里维护
  副本。`CLAUDE.md` 只作为兼容入口展示，不鼓励用户维护第二份重复规则。命令面板的
  「打开 AI 仓库说明」也只负责跳到这一页，不直接执行覆盖。这种标签只在本次明确
  打开后存在，不写入“上次阅读”或下次启动的标签恢复；基础设施不能从普通文档的恢复
  链路里自动复活
- **写失败不算致命**：只读介质、权限不足都可能失败，那时正确的行为是照常打开
  vault。一份说明文件不该成为「笔记打不开」的理由
- **两个文件名都写**，第二个只有一行指针。§7.1 说了不绑定任何工具，但**约定文件
  的名字是各家自己定的** —— 漏掉那一行等于对最常用的那个 CLI 无效，而它的成本
  就是一行
- **不写 vault 的内容摘要**（有哪些标签、哪些 database 视图、目录长什么样）：
  那种东西写进去的第一天就开始过期，而 AI 自己 grep 一遍比读一份陈旧的清单准。
  只写不看文件就猜不出来的**约定**和**边界**
- 正文里带生成时的版本号。约定会随版本变，而那份文件已经躺在用户的 vault 里 ——
  留个标记，人和 AI 才知道它是什么时候的
- 老 vault 下次打开时也会补上，于是「当前改动」里会多出两个文件。虽然文档树隐藏，
  版本历史里仍必须看得见、退得掉：隐藏的是笔记入口，不是文件或改动事实

---

## 8. 路线图

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| **M0 地基** ✅ | Tauri 桌面壳、`VaultFs` 抽象、vault 打开、同名文件夹文档树、`.md` 原子读写、`git init` + `.gitignore`、路径越界拒绝、记住上次 vault、聚焦时 mtime 比对 | ✅ 已达成。代码在 [verso/](verso/)，20 个单测通过 |
| **M1 编辑器** ✅ | CM6 集成、`markdownExtended` 解析器、live preview、KaTeX 渲染、frontmatter 属性条、快速切换器、可点击面包屑、`[[链接]]` 跳转、树节点重命名/移动/删除、「在系统终端中打开」 | ✅ 已达成。38 前端测试 + 28 Rust 测试通过 |
| **M2 公式** ⭐ ✅ | snippet 引擎（Latex Suite 兼容格式）、数学模式检测、tabstop 与 tabout、135 条默认 snippet 库、矩阵按尺寸生成、中文可搜的符号面板 | ✅ **盲测已通过**（作者手测，v0.3.0）。默认库的手感继续按 §5.4 打磨 |
| **M3 索引与 database** ✅ | SQLite 索引、文件监听、全文搜索、反向链接、`[[` 补全、`/` 命令、可写的 database 表格/看板视图 | ✅ 已达成。500 篇索引 < 1s、搜索 < 50ms；论文清单表格可直接改属性 |
| **M4 打磨** ✅ | 视觉规范落地、深浅主题（三态）、命令面板、设置界面（含自定义 snippet 与终端字体）、快捷键提示按平台渲染 | ✅ 已达成。134 Node + 21 浏览器 + 86 Rust 测试通过。macOS 的透明标题栏留到能在真机上验证时再做（§6.5） |
| **M5 同步** | `git2-rs` 集成、同步按钮与状态、自动 commit 聚合、冲突解决 UI、凭据钥匙串、版本历史、未提交改动 diff 入口<br>（~~内嵌终端~~ 已提前到 v0.2.1） | 两台桌面设备改同一个 vault 不丢数据；用 AI 改完能一眼 diff、一键回退 |
| **M6a 移动端（安卓）** | Android 打包、`VaultFs` 移动实现、移动端公式工具条、触摸交互（同步直接复用 M5） | 安卓手机上能读、能改、能输入公式、能同步 |
| **M6b 移动端（iOS）** | iOS 打包与签名 | iPhone 上同一套验收标准 |
| **M7 发布** | 内置静态站点生成、公式预渲染、gh-pages 部署、双远端配置 | 一键把 `publish: true` 的笔记变成网页 |
| *候选：异步协作* | 加入共享 vault + 协作动态 + 按内容共享与空间管理 ✅ v0.7.11–v0.7.14；语义冲突 ✅ v0.7.15；可选修改建议 ✅ v0.7.16（§2.8） | 2–10 人离线工作后能看懂并安全合并彼此的变化，无需理解 Git |
| *M8+* | 块引用、图谱、导出 PDF、日历/画廊视图 | |

**M2 结束是关键决策点**：如果那时公式输入体验没有明显超过 Obsidian，这个项目就没有存在理由，应该停下重新想。把最难、最能证伪的部分放在最前面，而不是先做让人有进展感的 UI。

**移动端排在 M6 但架构从 M0 就要留出口**（`VaultFs` 抽象、无键盘可达性、git init）—— 移动端本身可以晚做，但不能到时候才发现做不了。

**M6 拆成 a/b 两段**（照 M5a/M5b 的先例）：iOS 打包需要 Mac + Xcode，而开发机是 Windows —— 拿不到那台机器之前，整个里程碑会被一件外部条件卡住不结。安卓那一段自己立得住（上面那条验收标准就是照它写的），先收。

**M7 之前想发布网页，直接用 Quartz**（§2.9 阶段一），零开发成本。内置发布是为了样式统一和公式预渲染，不是为了「能发布」。

---

## 9. 明确不做

写下来是为了对抗范围蔓延：

- ❌ **插件系统**（v1）—— 见设计原则 4，也是 Obsidian 不够美观的根因
- ❌ **块级属性与块级查询** —— 见 §2.6，代价是放弃纯 Markdown
- ❌ **多列布局 / 任意字号字色** —— 无法用标准 Markdown 表达
- ❌ **字符级实时共同编辑**（v1）—— 在线光标、CRDT/OT 和常驻服务与本地优先的异步协作是两套故障模型，复杂度是数量级差异；小团队异步协作见 §2.8
- ❌ **自建云同步服务器** —— 用 git + 用户自己的 GitHub/GitLab（§2.8）。我们做客户端，不做服务端
- ❌ **白板 / 手绘** —— 自由坐标、连线和笔画无法用 Markdown 表达，破坏第一原则；
  由 Markdown 标题/列表自动排版的草稿卡片不属于这一类（§4.7.1）
- ❌ **公式手写识别** —— 需要云端 API 或过大的本地模型
- ❌ **内置 AI 功能** —— 不绑模型、不管 API key、不做「AI 续写」按钮。用户通过终端自带 AI CLI 工具，效果更好且零维护成本（§7.1）。**§7.3/§7.6/§7.7 那三件事不算破例**：靠右的终端、把笔记发过去的那个键、写给 AI 的约定说明 —— 三件都是**把上下文送到用户自己的 AI 面前**，没有一件是我们去调模型
- ❌ **笔记内容触发的代码执行** —— 不做代码块「运行」按钮。笔记可被分享和发布，能执行笔记内代码 = 打开一篇笔记等于运行一个陌生程序（§7.5）

---

## 10. 待定问题

1. **`id` 是否真的写进 frontmatter？** 折中方案见 §2.3。替代方案是把 id 映射存在 `.verso/` 里、文件保持干净，代价是映射丢失后重命名历史就断了，违反「`.verso/` 可删除」原则。**倾向：写进 frontmatter，编辑器里默认折叠。**
2. ~~附件路径~~ **已定**：默认统一放 vault 根目录的 `attachments/`，路径可在设置里配置。附件重名时自动加时间戳后缀，不覆盖。
3. **打包思源黑体子集**：全量约 16MB，会让安装包从 10MB 涨到 26MB，移动端更敏感。评估按常用字集裁剪 vs 直接依赖系统字体。
4. **iOS vault 位置**：有了 git 同步后，iOS 上可以直接用 App 私有目录 + git 拉取，不再必须依赖 iCloud Drive ubiquity container —— 这大幅简化了 §1.2(b) 那条约束。但用户若想在 iOS「文件」App 里直接看到笔记，仍需 ubiquity container。倾向：**默认 App 私有目录 + git，iCloud 作为可选项**。
5. **database 视图定义存哪**：目前设计存在笔记的代码块里（可移植、可版本控制）。另一种是存在 `.verso/views.json`（笔记更干净，但视图不随笔记迁移，且 `.verso/` 不进 git）。**倾向前者**。
6. **自动 commit 的时间窗**：空闲 5 分钟是否合适？太短则历史噪音大，太长则崩溃丢的多。可能需要「自动保存文件」和「自动提交 git」两个独立节奏。
   - M0 实测补充：frontmatter 的时间戳已改成秒级精度。默认的 `to_rfc3339()` 带 9 位小数，每次保存都会在 git diff 里制造一行纯噪音。
7. ~~终端默认 cwd~~ **已定**：vault 根目录。AI CLI 得看见整个 vault 才做得了跨笔记的事，起在单篇目录里的话第一件事永远是 `cd ..`（§7.3）。
8. **AI 跑动期间是否自动 commit**：若 AI 正在批量改文件，自动 commit 会把半成品提交进去。可能需要检测到终端有活动进程时暂停自动 commit。
9. **异步协作之后还要不要做评论与 @提醒？** v0.7.11–v0.7.16 已完成无需 Git 的加入流程、协作动态、按内容共享、语义冲突和可选审阅；评论一旦加入就必须成为可移植、可版本控制的团队事实，不能只做一个留在本机的界面壳。
